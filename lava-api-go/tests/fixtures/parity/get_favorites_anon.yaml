# GET /favorites — anonymous bookmarks list.
# Both backends gate /favorites on a non-empty Auth-Token (Ktor via
# withTokenVerificationUseCase, Go via the matching empty-cookie
# short-circuit added in d60c599). An anon request returns 401 + {}
# from both — the parity gate verifies that both backends agree.
name: get_favorites_anon
method: GET
path: /favorites
query: {}
headers: {}
body: ""
content_type: ""
expected_status: 401
compare: json_unordered
header_allowlist:
  - Content-Type
