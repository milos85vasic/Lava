# GET /torrent/1 — anonymous torrent metadata fetch.
# Topic id 1 doesn't exist on rutracker.org — the rutracker scraper
# emits ErrNotFound which both backends map to HTTP 404 + {}.
name: get_torrent_id_anon
method: GET
path: /torrent/1
query: {}
headers: {}
body: ""
content_type: ""
expected_status: 404
compare: json_unordered
header_allowlist:
  - Content-Type
