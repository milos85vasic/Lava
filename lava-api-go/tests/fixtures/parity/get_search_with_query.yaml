# GET /search?query=test — anonymous search.
# Same story as /favorites: both backends gate /search on a non-empty
# Auth-Token (Ktor's withTokenVerificationUseCase pattern; Go's matching
# empty-cookie pre-check). Anon → 401 + {} on both.
name: get_search_with_query
method: GET
path: /search
query:
  query: linux
headers: {}
body: ""
content_type: ""
expected_status: 401
compare: json_unordered
header_allowlist:
  - Content-Type
