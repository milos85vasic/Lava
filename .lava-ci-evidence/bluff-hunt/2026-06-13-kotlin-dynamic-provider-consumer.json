{
  "date": "2026-06-13",
  "kind": "§6.N production bluff-hunt + coverage verification",
  "scope": "Defect-B CLIENT consumer path — fetch + parse GET /providers body → on-device provider list",
  "base_commit": "d7f3ad00",
  "worktree_branch": "worktree-agent-a156fbf281c252c80",
  "rationale": "The Go side now serves 10 providers (BitSearch + Knaben aggregator + curated set). This hunt verifies the Kotlin client GENUINELY consumes that catalogue body — DTO decode → toRemoteDescriptor mapping → RemoteTrackerDescriptor fields the UI renders → registry replace — not a bluff.",
  "baseline": {
    "command": "./gradlew :core:data:testDebugUnitTest :core:domain:testDebugUnitTest :core:network:api:test :core:tracker:api:test :core:tracker:registry:test --max-workers=2",
    "result": "BUILD SUCCESSFUL in 4m 22s (exit 0)"
  },
  "files_sampled": [
    "core/network/api/src/main/kotlin/lava/network/dto/ProviderDescriptorDto.kt",
    "core/data/src/main/kotlin/lava/data/provider/ProviderCatalogRepository.kt (toRemoteDescriptor bridge)",
    "core/domain/src/main/kotlin/lava/domain/usecase/FetchProvidersUseCase.kt",
    "core/tracker/api/src/main/kotlin/lava/tracker/api/RemoteTrackerDescriptor.kt (from())",
    "core/tracker/registry/src/main/kotlin/lava/tracker/registry/DefaultTrackerRegistry.kt (populateFrom)",
    "core/tracker/client/src/main/kotlin/lava/tracker/client/ApiBackedTrackerClient.kt"
  ],
  "mutations": [
    {
      "id": "mutation-1",
      "target_file": "core/tracker/api/src/main/kotlin/lava/tracker/api/RemoteTrackerDescriptor.kt",
      "target_function": "RemoteTrackerDescriptor.from() — capability string→enum mapping",
      "user_visible_break": "Every provider loses ALL capabilities → no search/download/browse buttons; getFeature() returns null everywhere; UI shows inert providers.",
      "mutation": "mappedCapabilities = ...toSet().let { emptySet<TrackerCapability>() } — drop all parsed capabilities",
      "verdict": "GENUINE",
      "tests_run": ":core:tracker:api:test + :core:data:testDebugUnitTest",
      "observed_failure": "RemoteTrackerDescriptorTest.mapsAllKnownFields FAILED + unknownCapabilityIsDroppedNotThrown FAILED — java.lang.AssertionError: expected:<[SEARCH, BROWSE, TORRENT_DOWNLOAD, CAPTCHA_LOGIN]> but was:<[]>  (and expected:<[SEARCH, MAGNET_LINK]> but was:<[]>). :core:tracker:api:test FAILED, 11 tests completed, 2 failed.",
      "reverted": "yes (git checkout — clean, no diff)",
      "post_revert": "covered by final full-module GREEN sweep"
    },
    {
      "id": "mutation-2",
      "target_file": "core/data/src/main/kotlin/lava/data/provider/ProviderCatalogRepository.kt",
      "target_function": "ProviderDescriptorDto.toRemoteDescriptor() — the DTO→RemoteTrackerDescriptor bridge",
      "user_visible_break": "DTO encoding dropped (hardcoded UTF-8) → RuTracker's Windows-1251 charset lost → Cyrillic torrent titles render as mojibake for every Russian-tracker result.",
      "mutation": "encoding = \"UTF-8\"  (was: encoding = encoding)",
      "verdict": "COVERAGE GAP (bluff) — then CLOSED",
      "tests_run_pre_fix": ":core:data:testDebugUnitTest",
      "observed_pre_fix": "BUILD SUCCESSFUL in 47s — NO test failed. No assertion guarded dto.encoding flowing through toRemoteDescriptor to descriptor.encoding. RemoteTrackerDescriptorTest asserts encoding only via from() positional param (not the DTO bridge); ProviderCatalogRepositoryTest.fetchOverSelfSignedTlsWithAuthSucceeds asserted authType/capabilities/baseUrls/supportsAnonymous but NOT encoding.",
      "fix": "Added two assertions to ProviderCatalogRepositoryTest.fetchOverSelfSignedTlsWithAuthSucceeds: assertEquals(\"Windows-1251\", native.encoding) + assertEquals(\"UTF-8\", jackett.encoding). This exercises the real repository's toRemoteDescriptor bridge end-to-end at the DTO boundary.",
      "falsifiability_proof": {
        "red_under_mutation": "org.junit.ComparisonFailure: expected:<[Windows-1251]> but was:<[UTF-8]> — ProviderCatalogRepositoryTest.fetchOverSelfSignedTlsWithAuthSucceeds FAILED; BUILD FAILED in 17s",
        "green_after_revert": "see final sweep"
      },
      "reverted": "yes (production mutation reverted via git checkout; the NEW test assertion is kept as the gap-closing fix)"
    }
  ],
  "bug_found_and_fixed": "No production bug. Mutation-1 proved existing capability-mapping coverage is GENUINE. Mutation-2 surfaced a TEST coverage gap (encoding mapping unasserted at the DTO→descriptor boundary), now closed with a falsifiability-proven assertion. Production code unchanged.",
  "host_citizenship": "All gradle runs used --max-workers=2 (§6.T.2); no parallel gradle invocations."
}
