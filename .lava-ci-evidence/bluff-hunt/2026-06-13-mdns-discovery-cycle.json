{
  "date": "2026-06-13",
  "cycle": "mdns-discovery-cycle",
  "trigger": "§6.N incident-response hunt — mDNS LOCAL-NETWORK DISCOVERY path (feeds onboarding ApiSelection / the /providers fetch shipped tonight); area not yet hunted this cycle",
  "scope": "SAFE evidence-only — test files unchanged; production code deliberately mutated then byte-reverted during each rehearsal",
  "protocol": "§6.N / Seventh-Law clause 5 + clause 5.b — pick gate-shaping discovery files where a bug would be invisible to existing tests; mutate user-visible discovery behavior; run module test; confirm RED on observable state; revert → GREEN",
  "host": "Mistborn.local (darwin/arm64)",
  "build_note": "Worktree submodule submodules/tracker_sdk was uninitialized (offline clone unavailable); materialized its working tree from the shared checkout at the IDENTICAL pinned SHA 7afc37aaa6048210a90b5160059f2fcba2d2eee0 so Gradle includeBuild(':tracker_sdk') resolves. No code change; gitlink unchanged.",
  "files_hunted": [
    {
      "index": 1,
      "production_file": "core/data/src/main/kotlin/lava/data/impl/service/LocalNetworkDiscoveryServiceImpl.kt",
      "production_symbol": "matchesServiceType(watchedServiceType, foundServiceType)",
      "why_gate_shaping": "Decides which mDNS listener accepts a resolved NsdServiceInfo. The `.local.` trailing-dot suffix normalization + dev-vs-prod (`_lava-api._tcp` vs `_lava-api-dev._tcp`) discrimination are exactly the real NsdManager behaviors the Third Law (core/CLAUDE.md 'Service Fakes' / 'TestLocalNetworkDiscoveryService MUST simulate ... .local.') calls out; a wrong match emits the wrong Engine tag → wrong Endpoint variant → wrong base URL for the /providers fetch (the SP-3.4 forensic anchor: a lava-api-go service appeared as Endpoint.Mirror routed to :8080 instead of GoApi :8443).",
      "covering_test": "core/data/src/test/kotlin/lava/data/impl/service/LocalNetworkDiscoveryServiceTypeTest.kt (:core:data:testDebugUnitTest)",
      "sut_wiring": "GENUINE — calls the REAL production `matchesServiceType` (same package `lava.data.impl.service`, internal fun, NO copy). Contrast: core/domain/.../LocalNetworkDiscoveryContractTest.kt re-implements a PRIVATE `matches()` copy (a Third-Law SUT-copy gap, see notes) — that file was NOT the one relied on here.",
      "mutation": "Stripped the dev/prod discriminator: appended `.replace(\"-dev\", \"\")` to BOTH the `watched` and `found` normalizations so `_lava-api-dev._tcp` and `_lava-api._tcp` collapse to the same normalized stem and cross-match. (Two weaker mutations were tried first and SURVIVED because the `-dev` token is an INFIX not a suffix — `found.contains(watched)` and `found.startsWith(watched)` both still reject dev-vs-prod; recorded honestly as part of the rehearsal, not a bluff finding.)",
      "result": "RED",
      "observed_failure_verbatim": [
        "LocalNetworkDiscoveryServiceTypeTest > matchesServiceType_distinguishesProdFromDev FAILED — java.lang.AssertionError: watcher for _lava-api-dev._tcp must reject _lava-api._tcp (LocalNetworkDiscoveryServiceTypeTest.kt:84)",
        "LocalNetworkDiscoveryServiceTypeTest > matchesServiceType_distinguishesDevFromProd FAILED — java.lang.AssertionError: watcher for _lava-api._tcp must reject _lava-api-dev._tcp (LocalNetworkDiscoveryServiceTypeTest.kt:72)"
      ],
      "primary_assertion_user_visible": "Whether a watcher accepts a service-type — directly determines which mDNS listener resolves the endpoint and therefore the discovered API's Engine tag / base URL.",
      "reverted": "yes — production file byte-identical to shared checkout (diff empty)",
      "green_after_revert": "yes — :core:data:testDebugUnitTest LocalNetworkDiscoveryServiceTypeTest failures=0 (final clean run BUILD SUCCESSFUL)",
      "verdict": "GENUINE"
    },
    {
      "index": 2,
      "production_file": "core/domain/src/main/kotlin/lava/domain/usecase/DiscoverLocalEndpointsUseCase.kt",
      "production_symbol": "DiscoverLocalEndpointsUseCaseImpl.DiscoveredEndpoint.toEndpoint() — Engine.Go / Engine.GoDev branch building Endpoint.GoApi",
      "why_gate_shaping": "Derives the host of Endpoint.GoApi from the discovered service (DiscoveredEndpoint.host carries the resolved `ip:port` string). The `host.substringBeforeLast(\":\")` strip is the SP-3.3 forensic-anchor fix: if the embedded port leaks into Endpoint.GoApi.host the base URL becomes `https://ip:port:port/...` → malformed → the /providers fetch (and every data call) hits the wrong address. A bug here is invisible unless a test asserts the parsed host equals the bare IP.",
      "covering_test": "core/domain/src/test/kotlin/lava/domain/usecase/DiscoverLocalEndpointsUseCaseTest.kt (:core:domain:testDebugUnitTest)",
      "sut_wiring": "GENUINE — Integration Challenge Test wires the REAL DiscoverLocalEndpointsUseCaseImpl to TestLocalNetworkDiscoveryService (behaviorally-equivalent fake per Third Law: non-suspending trySend-mirroring UNLIMITED channel) + TestEndpointsRepository + TestSettingsRepository. Primary assertions on the persisted/derived Endpoint.GoApi.host + .port (user-visible state), not on mock calls.",
      "mutation": "Removed the port strip: `host = host.substringBeforeLast(\":\").ifEmpty { host }` → `host = host` for the Go/GoDev branch (re-introduces the SP-3.3 ip:port-in-host bug).",
      "result": "RED",
      "observed_failure_verbatim": [
        "DiscoverLocalEndpointsUseCaseTest > engine Go discovery creates Endpoint GoApi with port preserved FAILED — org.junit.ComparisonFailure: expected:<192.168.1.100[]> but was:<192.168.1.100[:8443]> (DiscoverLocalEndpointsUseCaseTest.kt:260)",
        "DiscoverLocalEndpointsUseCaseTest > engine Go discovery on non-default port preserves the port FAILED — org.junit.ComparisonFailure: expected:<10.0.0.42[]> but was:<10.0.0.42[:9443]> (DiscoverLocalEndpointsUseCaseTest.kt:299)"
      ],
      "primary_assertion_user_visible": "Endpoint.GoApi.host — the host the network layer builds the base URL from; an embedded ':8443' here is the exact wrong-base-URL defect that breaks the discovered-API /providers fetch.",
      "reverted": "yes — production file byte-identical to shared checkout (diff empty)",
      "green_after_revert": "yes — :core:domain:testDebugUnitTest DiscoverLocalEndpointsUseCaseTest 11/11 pass (final clean run BUILD SUCCESSFUL)",
      "verdict": "GENUINE"
    }
  ],
  "bluffs_found": 0,
  "gaps_found": 0,
  "new_tests_written": 0,
  "third_law_observation": {
    "file": "core/domain/src/test/kotlin/lava/domain/contract/LocalNetworkDiscoveryContractTest.kt",
    "finding": "NON-BLOCKING / informational. Its private `matches()` helper (lines 37-41) is a hand-copied DUPLICATE of the production `matchesServiceType`, yet its KDoc claims 'Mirrors ... verbatim. If the production code drifts, this test breaks.' That claim is false at the mechanical level — the test asserts on the copy, so production drift would NOT break it (classic SUT-copy pattern). NOT escalated to a BLUFF finding because the SAME production logic (including the `.local.` suffix + the SP-3.4 cross-match regression guard) IS genuinely covered by the core/data LocalNetworkDiscoveryServiceTypeTest hunted above, which calls the real production fn — so the production behavior is not unguarded. Recommendation for a future cycle: replace the core/domain copy with a call to the real `matchesServiceType` (already `internal` + top-level in core/data) OR delete the duplicate and rely on the core/data contract test, to eliminate the misleading 'breaks on drift' claim."
  },
  "tracker_sdk_materialization_note": "Worktree gitlink submodules/tracker_sdk shows 'modified content' only due to the offline materialization (files copied from shared checkout at the same SHA); it is NOT committed and carries no code change."
}
