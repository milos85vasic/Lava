{
  "date": "2026-06-14",
  "hunt_type": "§6.N defect-driven bluff-hunt — mock-served-endpoint / dead-path class",
  "clauses": ["6.J", "6.L", "6.N", "Seventh Law clause 4 (mock-served endpoint)"],
  "trigger": "Cycle exposed SearchResultSseErrorRetryTest as a mock-served-endpoint bluff (incident 2026-06-14-sse-search-v1search-unserved-bluff.json). That test was removed; its follow_up_owed named two sibling tests on the same dormant /v1/search path. This hunt confirms + remediates them with real mutation evidence.",
  "head": "c78116d2",

  "served_route_inventory": {
    "method": "Read lava-api-go/internal/router/router.go (the EMBEDDED on-device api-app router, shared by cmd/lava-api-go + internal/mobile), internal/handlers/handlers.go (legacy rutracker handlers), internal/handlers/v1/handlers.go (provider-agnostic v1).",
    "embedded_api_app_AND_standalone_lava_api_go": {
      "note": "router.Build() is the SINGLE shared construction; the embedded on-device api-app and the standalone lava-api-go binary serve the SAME routes.",
      "public_pre_auth": ["GET /health", "GET /ready", "GET /providers"],
      "legacy_rutracker_handlers.Register": [
        "GET /forum", "GET /forum/:id",
        "GET /search",
        "GET /topic/:id", "GET /topic2/:id", "GET /comments/:id",
        "POST /comments/:id/add",
        "GET /torrent/:id", "GET /download/:id",
        "GET /favorites", "POST /favorites/add/:id", "POST /favorites/remove/:id",
        "GET /", "GET /index",
        "POST /login",
        "GET /captcha/:path"
      ],
      "v1_group_/v1/:provider (v1handlers.Register)": [
        "GET /v1/:provider/search", "GET /v1/:provider/browse/:id",
        "GET /v1/:provider/forum", "GET /v1/:provider/topic/:id",
        "GET /v1/:provider/torrent/:id", "GET /v1/:provider/download/:id",
        "GET /v1/:provider/comments/:id", "POST /v1/:provider/comments/:id/add",
        "GET /v1/:provider/favorites", "POST /v1/:provider/favorites/add/:id",
        "POST /v1/:provider/favorites/remove/:id",
        "POST /v1/:provider/login", "GET /v1/:provider/captcha/:path"
      ],
      "jackett_conditional": ["GET /jackett/search (ONLY when JackettEnabled + configured)"]
    },
    "routes_NO_backend_serves": [
      "GET /v1/search  — would match /v1/:provider with provider='search', then 404 (unknown provider) or 501. THIS is the route the dead client SSE consumer (observeSseSearch) targets.",
      "GET /sse, /sse/search — no SSE aggregator endpoint exists on either backend."
    ]
  },

  "candidates": [
    {
      "test": "feature/search_result/src/test/kotlin/lava/search/result/SearchResultSseConnectivityTelemetryTest.kt",
      "claims_to_cover": "SearchResultViewModel.applySseError telemetry severity (recordWarning vs recordNonFatal) for SSE Error events.",
      "production_owner": "applySseError (SearchResultViewModel.kt:246) — its ONLY production caller is observeSseSearch (line 423).",
      "reachability": "observeSseSearch (line 377) has ZERO live callers. onCreate dispatch (line 100) routes GoApi to observeStreamMultiSearch; onRetryClick (line 510) routes to observeStreamMultiSearch. observeSseSearch was AMPUTATED this cycle because it issued GET {base}/v1/search (line 411) — a route NO backend serves. applySseError + SearchResultContent.Error are therefore UNREACHABLE in production. The live path (observeStreamMultiSearch → applyMultiSearchEvent) never produces SearchResultContent.Error.",
      "verdict": "BLUFF (dead-path / mock-served-endpoint class)",
      "evidence": "BASELINE: test PASSES. MUTATION: in observeStreamMultiSearch (the LIVE user search path) replaced `val providerIds = filter.providerIds` with `val providerIds = emptyList<String>()` — every real multi-provider search now early-returns, rendering nothing for users. RE-RUN: SearchResultSseConnectivityTelemetryTest STILL PASSES GREEN (BUILD SUCCESSFUL). The test is blind to a total break of the path users actually hit — the textbook bluff signature. MUTATION REVERTED (git checkout), baseline green restored.",
      "recommendation": "remove"
    },
    {
      "test": "feature/search_result/src/test/kotlin/lava/search/result/SearchResultRetryTest.kt",
      "claims_to_cover": "SearchResultViewModel.onRetryClick dispatch-by-state: retry from SearchResultContent.Error re-dispatches; retry from non-Error pushes Paging3 retry.",
      "production_owner": "onRetryClick (line 487). Its Error branch (line 496) is reachable in production ONLY if state can BE SearchResultContent.Error.",
      "reachability": "The non-Error branch (retry_in_non_error_state_does_not_re_dispatch) IS a live path (paging retry). BUT the Error branch (retry_in_error_state_re_dispatches_and_clears_error) reaches Error via vm.applySseError(...) directly — and Error is unreachable in production (see above). So the load-bearing Error-retry case asserts behavior on an unreachable state; it is a dead-path bluff. The non-Error case alone is too thin to keep as a distinct file (its dispatch is identical to onCreate, covered elsewhere) and the file's stated purpose (LVA-069 onRetryClick Error+non-Error) is half dead.",
      "verdict": "BLUFF (the Error-branch test reaches an unreachable production state via the dead SSE reducer)",
      "evidence": "Same mutation run (observeStreamMultiSearch broken to empty providers). RE-RUN: SearchResultRetryTest STILL PASSES GREEN (BUILD SUCCESSFUL) — both its tests, including retry_in_non_error_state, are blind to the live-search break. MUTATION REVERTED, baseline green restored.",
      "recommendation": "remove"
    }
  ],

  "mutation_rehearsal_verbatim": {
    "file": "feature/search_result/src/main/kotlin/lava/search/result/SearchResultViewModel.kt",
    "live_path_mutated": "observeStreamMultiSearch (the production GoApi multi-provider search consumer reached from onCreate line 100 + onRetryClick line 510)",
    "mutation": "val providerIds = filter.providerIds  ==>  val providerIds = emptyList<String>()  (function then early-returns at the next isNullOrEmpty guard — user search renders nothing)",
    "command": "GOMAXPROCS=2 nice -n 19 ./gradlew :feature:search_result:testDebugUnitTest --no-daemon --tests SearchResultSseConnectivityTelemetryTest --tests SearchResultRetryTest",
    "baseline_result": "BUILD SUCCESSFUL (both tests pass)",
    "mutated_result": "BUILD SUCCESSFUL (both tests STILL pass while live search is broken) — BLUFF CONFIRMED",
    "reverted": "yes — git checkout on the VM file; no diff remains"
  },

  "non_bluffs_confirmed_genuine": [
    {
      "test": "core/data/src/test/.../ProviderCatalogRepositoryTest.kt",
      "reason": "Enqueues + asserts recorded.path == '/providers' (served at root, pre-auth, router.go:92). Real served route."
    },
    {
      "test": "core/domain/src/test/.../RepopulateProvidersOnStartupUseCaseTest.kt",
      "reason": "Issues real GET {base}/providers against the MockWebServer socket; the /search reference is a comment for the orphan-host negative case, not an enqueued served-path assertion."
    },
    {
      "test": "feature/onboarding/src/test/.../OnDeviceApiFlowTest.kt",
      "reason": "Probes GET {host}:{port}/health (served, router.go:74). Real served route."
    },
    {
      "test": "feature/onboarding/src/test/.../OnboardingViewModelDynamicProvidersTest.kt",
      "reason": "Drives GET /providers catalogue fetch (served). Real served route."
    },
    {
      "test": "core/domain/src/test/.../ProbeMirrorUseCaseTest.kt",
      "reason": "Generic reachability probe against server.url('/') — asserts 2xx→Reachable / 5xx→Unhealthy on the mock root; not a backend-specific route assertion."
    }
  ],

  "scan_result": "No test in feature/*/src/test or core/*/src/test contains a /v1/search literal (the removed SearchResultSseErrorRetryTest was the last). The mock-served-endpoint bluff is isolated to the 2 SSE sibling tests, both removed by this hunt.",

  "removed": [
    "feature/search_result/src/test/kotlin/lava/search/result/SearchResultSseConnectivityTelemetryTest.kt",
    "feature/search_result/src/test/kotlin/lava/search/result/SearchResultRetryTest.kt"
  ],

  "dead_production_code_note": "observeSseSearch + applySseError + applySseEvent + the sse* constructor params remain dead in SearchResultViewModel.kt. This hunt removes only the bluff TESTS (anti-bluff scope). Removing the dead client SSE consumer (or implementing a real server-side /v1/search SSE aggregator and reviving the path) is the follow_up_owed from incident 2026-06-14-sse-search-v1search-unserved-bluff.json — tracked, not shipping-blocking.",

  "green_after_removal": "GREEN — `./gradlew :feature:search_result:testDebugUnitTest --no-daemon` BUILD SUCCESSFUL after `git rm` of both bluff tests. The remaining search_result tests (multi-search consumer, paging, applySseEvent parser, etc.) all pass."
}
