# LVA-095 + LVA-082 closure evidence (2026-08-20)

## LVA-095 — on-device API keyless mDNS endpoint key-persistence

Already fixed by commit `2c96a993` ("fix(§6.R/LVA-095): isLocalHost() misclassified
localhost.localdomain as remote — broke ALL on-device API auth"), which added a
`localhost.localdomain` branch to `String.isLocalHost()` in
`core/models/src/main/kotlin/lava/models/settings/HostUtils.kt`. The bug: MockWebServer's
(and some real Android resolvers') canonical hostname for 127.0.0.1 is
`localhost.localdomain`, which the pre-fix `isLocalHost()` did not recognize as local,
so `OnboardingViewModel.withLocalApiKeyIfMissing()` never attached the on-device
api-app key to a keyless mDNS-discovered endpoint.

Re-verified this session (forced re-run via `--rerun-tasks`, not a cached UP-TO-DATE
result):

    ./gradlew :feature:onboarding:testDebugUnitTest \
      --tests "*OnboardingViewModelDynamicProvidersTest*" --rerun-tasks

    ...
    > Task :feature:onboarding:testDebugUnitTest

    BUILD SUCCESSFUL in 7m 5s
    414 actionable tasks: 414 executed

Per-test breakdown from the JUnit XML report
(`feature/onboarding/build/test-results/testDebugUnitTest/TEST-lava.onboarding.OnboardingViewModelDynamicProvidersTest.xml`):

    <testsuite name="lava.onboarding.OnboardingViewModelDynamicProvidersTest" tests="6" skipped="0" failures="0" errors="0" timestamp="2026-08-20T09:40:12" hostname="nezha" time="4.713">

    testcase: dynamic discovery populates provider list from the API including an API-only provider (PASS, 4.137s)
    testcase: selecting an API persists it as the active settings endpoint the search path reads (PASS, 0.165s)
    testcase: onboarding shows ONLY the chosen API's providers replacing bundled ones the API lacks (PASS, 0.055s)
    testcase: selecting a keyless on-device API reads and persists the local api key for search auth (PASS, 0.126s)
    testcase: catalogue fetch failure falls back to bundled providers with a non-blocking notice not a blank list (PASS, 0.076s)
    testcase: selecting a keyless REMOTE cloud API does NOT attach the local api key (PASS, 0.147s)

Result: 6 tests / 6 passed / 0 skipped / 0 failures / 0 errors, including the target test
`selecting a keyless on-device API reads and persists the local api key for search auth`.
LVA-095 is genuinely fixed; confirmed live in this worktree, not trusted from the prior
report.

## LVA-082 — Crashlytics read not available via Firebase CLI

Investigated: Firebase CLI 14.17.0 exposes only `crashlytics:symbols:upload` and
`mappingfile:*` — there is no `issues:list` or non-fatal-read subcommand. `bq` (BigQuery
CLI) is not installed on this host, and no BigQuery Crashlytics export is configured for
this project. This is an external-tool capability limit, not a Lava code defect — no
fix is possible in this repository.

Fallback already in place and verified still wired: in-repo §6.AC non-fatal telemetry
(`scripts/check-non-fatal-coverage.sh`, `LAVA_NONFATAL_STRICT=1` by default, wired into
`scripts/verify-all-constitution-rules.sh`) plus the operator manually pasting Console
items for triage when needed. No further action is owed on this item.
