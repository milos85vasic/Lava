================================================================================
 lava-api-go STRESS + CHAOS — real router.Build /providers + /health
 §11.4.85 (Lava equivalent) Stress + Chaos Test Mandate — LVA-007
 Date: 2026-06-13 | git SHA: 7d5e8941743db0190ea5f7aa6bad593cbf5d826d
 Host: Mistborn.local | go1.26.2 darwin/arm64
================================================================================

TEST: TestStressProvidersRouter  (tests/stress/providers_router_stress_test.go)
TARGET: internal/router.Build — the SINGLE production router that both
        cmd/lava-api-go/main.go AND internal/mobile (on-device embed) call.
        Driven over a real loopback HTTP socket (httptest), Cfg=nil/AuthLadder=nil
        so /providers + /health + /ready are served pre-auth exactly as production
        does for an un-paired onboarding client.

--------------------------------------------------------------------------------
RUN COMMAND (constitutional resource limits per §6.T.2):
  cd lava-api-go
  GOMAXPROCS=2 nice -n 19 go test -tags stress -count=1 -v \
      -run TestStressProvidersRouter ./tests/stress/...
  (also run clean with -race: RACE-EXIT clean — no data race in the registry read)

--------------------------------------------------------------------------------
CANONICAL RUN — VERDICT PASS (captured verbatim, firebase-noop log lines elided):

=== RUN   TestStressProvidersRouter
INFO firebase: no credentials configured — running in no-op mode (per §6.H, dev/CI default)
    providers_router_stress_test.go:415: evidence written: evidence/2026-06-13T19-29-13Z/stress-chaos.json (verdict=PASS)
    SR1 router-providers-sustained:                status=PASS reqs=500 2xx=500 5xx=0 p50=0.137ms p95=0.219ms p99=0.322ms max=1.012ms
    SR2 router-health-sustained:                   status=PASS reqs=300 2xx=300 5xx=0 p50=0.109ms p95=0.184ms p99=0.249ms max=0.305ms
    SR3 router-providers-concurrent:               status=PASS reqs=64  2xx=64  5xx=0 p50=0.960ms p95=1.606ms p99=1.770ms max=1.770ms
    CR1 router-dependency-down-isolation-recovery: status=PASS reqs=500 2xx=0  5xx=0 (chaos dimension — see below)
--- PASS: TestStressProvidersRouter (0.19s)
PASS
ok  	digital.vasic.lava.apigo/tests/stress	0.504s

Leak guards: goroutines 3 -> 4 (clean — httptest server worker); open FDs 0 -> 0.

--------------------------------------------------------------------------------
SLO ASSERTED (the gate):
  - SR1/SR2 sustained (N=500 / N=300 >> §11.4.85 N>=100): 0 x 5xx; every /providers
    returns 200 WITH the right provider count in the parsed JSON body (50-sample
    body-correctness check, not just status); p99 < 250ms.
  - SR3 concurrent (N=64 >> §11.4.85 N>=10): all complete, all 200, all bodies carry
    the right provider count under contention (no torn registry read); -race clean.
  - CR1 chaos (dependency-down fault injected at the REAL readiness seam, N=500):
        * /ready returned a clean 503 for ALL 150 requests during the fault
          (graceful — no panic, no hang);
        * /health + /providers STAYED 200 with correct bodies throughout
          (bad=0, 5xx=0 — the fault is ISOLATED, no cascade);
        * /ready recovered to 200 within 1 request after the fault cleared.
    The firebase-noop telemetry firing "503 on GET /ready" during the fault window
    is REAL production §6.AC non-fatal recording — proof the chaos request crossed
    the actual production middleware chain, not a synthetic handler.

MEASURED NUMBERS (verbatim from evidence JSON 2026-06-13-providers-stress-run.json):
  SR1: 500 req, throughput ~6.6k req/s (wall 0.0756s), p50 0.137ms p95 0.219ms p99 0.322ms max 1.012ms, errRate 0.000, 5xx=0
  SR2: 300 req, p50 0.109ms p95 0.184ms p99 0.249ms max 0.305ms, errRate 0.000, 5xx=0
  SR3: 64 concurrent, all complete, p50 0.960ms p95 1.606ms p99 1.770ms, errRate 0.000, 5xx=0
  CR1: /ready 503=150/150 during fault, /health+/providers bad=0 5xx=0, recovery=1 req

================================================================================
FALSIFIABILITY REHEARSAL (Sixth Law clause 2 / §6.J / §11.4.85) — all REVERTED
================================================================================

MUTATION A (latency budget — production code):
  internal/handlers/v1/providers.go GetProviders():
      + time.Sleep(300 * time.Millisecond)
  Observed (verbatim):
    --- FAIL: TestStressProvidersRouter (212.41s)
    SR1 router-providers-sustained FAILED: 2xx=500/500 5xx=0 errRate=0.000 p99=305.83ms(SLO<250) badBody=0
    SR1: status=FAIL p50=302.413ms p95=303.492ms p99=305.825ms max=309.091ms
  => The p99<250ms SLO trips on a real bottleneck in the catalogue hot path.
  Reverted: yes (git diff providers.go empty).

MUTATION B (chaos recovery — test seam mirroring a stuck production dependency):
  tests/stress/providers_router_stress_test.go chaosReadiness.probe():
      forced to always return the dependency-unavailable error (never recovers)
  Observed (verbatim):
    --- FAIL: TestStressProvidersRouter (0.37s)
    CR1 chaos FAILED: ready503=150/150 healthyBad=0 healthy5xx=0 postReady200=0 recov=0 (want ready503=150, healthyBad=0, recov=1)
    SR1/SR2/SR3 still PASS  <-- failure LOCALIZED to the recovery path
  => The recovery-after-fault assertion fires when /ready stays stuck-down.
  Reverted: yes.

MUTATION C (body correctness — production code):
  internal/handlers/v1/providers.go GetProviders():
      all = nil   // catalogue returns an empty provider list
  Observed (verbatim):
    --- FAIL: TestStressProvidersRouter (1.00s)
    SR1 router-providers-sustained FAILED: 2xx=500/500 5xx=0 ... badBody=50
    SR3 router-providers-concurrent FAILED: completed=64/64 2xx=64 5xx=0 concBad=64
    CR1 chaos FAILED: ... healthyBad=150 ...
  => Status codes ALL still 200, but every PARSED BODY has the wrong provider
     count. The test asserts on the USER-VISIBLE catalogue content the onboarding
     wizard reads (§6.AB rendering-correctness-not-just-presence), so an empty-but-
     200 response — the exact shape of Crashlytics 47b000d5's fallback class — FAILS.
  Reverted: yes (git diff providers.go empty; clean re-run -> ok / PASS).

================================================================================
After all three mutations reverted, the canonical run is PASS (verdict=PASS,
EXIT=0). No fabricated numbers — every field above is from the actual run
(§6.J / §11.4.6). C3 Postgres-kill + C4b pool-exhaustion remain OPERATOR_GATED
in the sibling api_stress_test.go (need real Postgres under podman) — recorded
ran=false there, NOT faked.
================================================================================
