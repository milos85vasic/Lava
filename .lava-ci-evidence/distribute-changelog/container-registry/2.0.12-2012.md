lava-api-go 2.0.12 (2012) — 2026-05-06

What's new vs 2.0.11 (2011):

REMOVED:
- Legacy Ktor proxy removed from the codebase. lava-api-go is the only API
  from this point forward.
  * proxy/ module deleted (29 files: build.gradle.kts, Dockerfile, src/, etc.)
  * settings.gradle.kts: `include(":proxy")` removed
  * build_and_release.sh: stage [4/5] proxy build removed; phases re-numbered
  * scripts/tag.sh: `api` target + `read_api_version_*` + `write_api_versions`
    removed; supported apps now `(android api-go)`
  * docker-compose.yml: `lava-proxy` service + `legacy` / `both` profiles removed
  * start.sh / stop.sh: `--legacy` / `--both` flags removed; default and only
    profile is api-go

CONFIGURATION CHANGES:
- None — the Android client already discovers lava-api-go via mDNS.

CONSTITUTIONAL BINDINGS:
- §6.P — this entry is the per-version snapshot; CHANGELOG.md gains a matching
  Lava-API-Go-2.0.12-2012 entry.

Versions in this build:
- lava-api-go: 2.0.12 (2012)
- Android: 1.2.6 (1026) — unchanged
