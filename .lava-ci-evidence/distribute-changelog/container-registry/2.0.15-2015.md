lava-api-go 2.0.15 (2015) — 2026-05-06

What's new vs 2.0.14 (2014):

REFACTOR (post-Ktor naming cleanup):
- tools/lava-containers/internal/proxy → internal/orchestrator
  Renamed the Go package: the "proxy" package name carried orchestrator-
  meaning since SP-2 Phase 11, but post-Ktor-removal the name was just
  confusing — every reader had to disambiguate "this 'proxy' is the
  orchestrator, not the Ktor proxy". Renaming closes the ambiguity.
  Files: orchestrator.go, orchestrator_test.go, manager.go (was: proxy.go).
  Package: `package orchestrator` everywhere.
- Manager.BuildJar() removed: ran `./gradlew :proxy:buildFatJar` which
  would fail post-:proxy-removal. os/exec import removed (was only
  used by BuildJar).

CONTEXT:
- Workstation-side Go CLI only; the api-go binary is unchanged.
- git mv preserves the file rename history.
- All tests in tools/lava-containers pass after the rename:
    cmd/lava-containers     PASS
    internal/orchestrator    PASS
    internal/runtime         (no tests)

VERIFICATION:
- `go build ./...` in tools/lava-containers: green
- `go vet ./...` in tools/lava-containers: green
- `go test ./...` in tools/lava-containers: 2/2 packages PASS
- thinker.local API still healthy (unchanged binary)

Versions in this build:
- lava-api-go: 2.0.15 (2015)
- Android: 1.2.6 (1026) — unchanged
