lava-api-go 2.0.14 (2014) — 2026-05-06

What's new vs 2.0.13 (2013):

REMOVED (post-Ktor cleanup, second pass):
- tools/lava-containers/cmd/lava-containers/main.go: dropped `legacy` /
  `both` profile branches. The CLI now accepts only --profile=api-go.
- main_test.go: TestValidateProfile_Accepts reduced to api-go only;
  legacy/both moved to TestValidateProfile_Rejects.
- runStart: removed the legacy Gradle JAR build + image build
  fall-through. Now a single delegate to orch.ComposeUp().
- autoDetectProjectDir: removed the proxy/ probe.
- KDoc / comment refresh: "legacy Ktor proxy and/or the new Go API
  service" → "the lava-api-go service".

CONTEXT:
- The Ktor :proxy module was removed in 2026-05-06 (see lava-api-go
  2.0.12 changelog entry). This 2.0.14 cycle cleans up the
  workstation-side CLI that still accepted legacy/both profiles.
- This change is workstation-side only; the api-go binary itself is
  unchanged. The version bump keeps the §6.P distribute gate happy
  and the per-version snapshot chain intact.

VERIFICATION:
- `go test ./...` in tools/lava-containers: 2/2 PASS
- `go build ./...` in tools/lava-containers: green
- thinker.local API still healthy at https://thinker.local:8443/health
- mDNS announcement intact (verified via podman logs)

Versions in this build:
- lava-api-go: 2.0.14 (2014)
- Android: 1.2.6 (1026) — unchanged
