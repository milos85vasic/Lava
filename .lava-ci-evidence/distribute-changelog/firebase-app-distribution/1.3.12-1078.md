# Lava-Android-1.3.12-1078

**Previous published:** Lava-Android-1.3.12-1077.

This build is the **autonomous-QA dev cycle** version code (§6.Y bump-first,
1077 → 1078). versionName HELD at 1.3.12 — there is no user-visible behavior
change in this cycle.

What changed since 1077:

- **Autonomous-QA harness (no app change).** New self-driving QA tooling under
  `scripts/autonomous-qa/` (containerized-KVM emulator boot, backend bring-up,
  fresh-install, marker-based anti-bluff verdict) plus
  `Challenge70AutonomousQaProviderMatrixTest` (onboard → search → details →
  download device journey) and 5 HelixQA matrix banks.
- **Off-main navigation guard (production NO-OP).** `core/navigation`
  `NavigationController` now marshals `navigate(...)` / `navigateUp()` /
  `popBackStack()` onto the main looper, enforcing NavHostController's
  documented main-thread contract. On the production happy path this runs
  inline (no behavior change); it only diverges under the Compose UI test
  harness.

See the canonical `CHANGELOG.md` entry "Lava-Android-1.3.12-1078" for full
detail and the §6.AK cycle-coverage map. §6.Z device gate + §6.AA two-stage
distribute are enforced.
