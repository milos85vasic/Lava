# §6.Z device-gate evidence — Lava-Android 1.3.8 (1065)

**Commit:** `9751e1ef` (code SHA; the build was produced at `6bca1891` and the only commits
after are docs/evidence — app/Kotlin source byte-identical to HEAD).
**Device:** Genymotion Pixel 9 / API 35 / arm64 (`127.0.0.1:6555`, §6.AH VM path,
§6.AG Containers-submodule-driven).
**Artifacts:** `app-debug.apk` (34 MB) + `app-release.apk` (6.7 MB R8) embedding the
1.3.8 client fixes: per-provider on-device-API auth thread-through (search fix),
server-list de-dup, onboarding select-all, password masking + eye toggle.
**Date:** 2026-06-14. **Mode:** overnight autonomous (operator delegated decisions;
the device gate serves as the §6.AA stage-1→stage-2 verifier). Device: android_release=15 sdk=35 model=Pixel 9 abi=arm64-v8a 

## connectedDebugAndroidTest on the real VM (verbatim, runner = containers-submodule)

Single invocation, all 5 classes (`-Pandroid…class=C00,C41,C42,C43,C44`):

```
BUILD SUCCESSFUL in 57s
```
| Challenge | Covers | Result |
|---|---|---|
| C00 `Challenge00CrashSurvivalTest` | cold-start survival | PASS |
| C41 `Challenge41OnboardingSelectAllProvidersTest` | select-all / deselect-all | PASS |
| C42 `Challenge42OnboardingPasswordMaskingTest` | password mask + eye toggle | PASS |
| C43 `Challenge43ServerListNoDuplicateTest` | server-list de-dup (real Room) | PASS |
| C44 `Challenge44ApiSearchAuthTest` | on-device-API per-provider auth (search) | PASS |

0 failures, 0 unexplained skips. Evidence: `.lava-ci-evidence/genymotion/1.3.8-1065-client-gate/`.

## R8 release cold-start canary (§6.AA stage-2 / R8-only crash-class check)

```
package: digital.vasic.lava.client
apk: releases/1.3.8/android-release/lava-1.3.8-1065-release.apk  (R8-minified, 6.7 MB)
==> RELEASE CANARY PASS — process alive, no fatal
```
Evidence: `.lava-ci-evidence/release-canary/1.3.8-1065-client/`.

## §6.Z compliance
- Debug + R8 release artifacts EXECUTED on device (not source-compiled): ✅
- Cold-start canary GREEN on debug (C00) and R8 release: ✅
- All 4 operator-reported fixes device-verified via C41–C44: ✅
- Paired with api-app 0.2.8-12 (FGS specialUse device-verified — see that channel's evidence).
