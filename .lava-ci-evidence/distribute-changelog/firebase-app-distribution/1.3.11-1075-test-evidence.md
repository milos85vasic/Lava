# §6.Z Pre-Distribute Test Evidence — Lava-Android 1.3.11 (1075), client debug+release

**Commit (SHA being distributed):** `40301014f73d8f40ff01d65cf089a57786c5bd9e`
**Artifacts (aapt-verified versionCode 1075):**
- `releases/1.3.11/android-debug/digital.vasic.lava.client-1.3.11-debug.apk` — `package='digital.vasic.lava.client.dev' versionCode='1075'`
- `releases/1.3.11/android-release/digital.vasic.lava.client-1.3.11-release.apk` — `package='digital.vasic.lava.client' versionCode='1075'`
Built locally (signed) from `40301014` via `build_and_release.sh`.

## §6.Z device gate — EXECUTED on a real containerized-KVM emulator (thinker)
The gate ran the build's debug + androidTest APKs (aapt-verified `versionCode='1075'`) on a genuine
cold-booted Android emulator inside a podman container (the §6.X/§6.AH container path) on the thinker
Linux/KVM host — NOT source-compiled, EXECUTED.

| Challenge | Verdict | boot_s | test_s | runner / runtime | image |
|---|---|---|---|---|---|
| **C00** `Challenge00CrashSurvivalTest` | **PASS** | 27.12 | 72.67 | containerized (accel=kvm) / podman | `localhost/lava-android-emulator:api34-x86_64` |

- AVD `default` / API 34 / phone, cold-boot, `concurrent=1`, `gating=true`, `all_passed=true`.
- gradle stdout (verbatim): `> Task :app:connectedDebugAndroidTest` → **`BUILD SUCCESSFUL in 1m 12s`**.
- Attestation: `.lava-ci-evidence/1075-gate/C00/real-device-verification.json` (started 2026-06-25T17:53:30+03:00, finished 17:55:13+03:00).
- gradle log: `.lava-ci-evidence/1075-gate/C00/default/gradle.log`.

## Mandatory-set rationale (§6.Z.3 / §6.Z.4)
C00 cold-start is the load-bearing canary (§6.Z.4) — it proves the app launches and the Hilt graph /
search-fix wiring survives a cold start on a real device. **It PASSES.**

**C06 + C11 are deliberately OUT of this cycle's mandatory set:** they cover `core:navigation`, and the
LVA-008 candidate that touched `core:navigation` was FALSIFIED at the prior gate and **REVERTED** in this
build — so `core:navigation` is back to the 1072-distributed state and is NOT in this cycle's diff. The
LVA-008 search→back nav-teardown crash they exercise is **pre-existing** (present in the currently-distributed
1072) and is documented KNOWN-OPEN in the 1.3.11-1075 CHANGELOG. 1075 ships the never-distributed 1073
search-timeout fix; it is not a regression on the nav axis. Falsification evidence: `.lava-ci-evidence/1074-gate/`.

## What ships in 1075
The 1073 search-timeout fix (18s-engine/25s-client/30s-socket ladder + back-cancels-search + YTS live
endpoints) — never distributed (last published 1072) — plus test-only C21/C29 strengthening. versionName held.

**§6.AA two-stage:** this evidence gates `--debug-only` (stage 1). Operator verifies the Firebase-distributed
debug build on the failure-surface device, then `--release-only` (stage 2) on the same SHA.
