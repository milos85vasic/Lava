# Lava-Android-1.3.17-1087 — dynamic-port fix, release-variant testing gap closed

**Previous published:** Lava-Android-1.3.17-1085.

- **lava-api-go: fixed a real `:8443` port collision** on a shared host (new race-free `network.ListenEphemeral` helper) and a genuine mDNS/Alt-Svc-before-any-bind ordering bug found while investigating it.
- **This project can now genuinely test the release-shaped (R8-minified) build for the first time.** A new `releaseTest` build type plus 8 real minification defects found and fixed (Kotlin stdlib, Hilt, Compose test-support, coroutines, OkHttp — all test-only infrastructure, never affecting what real users actually get) — `Challenge00CrashSurvivalTest` now genuinely PASSES against both the debug and the real release-shaped build on a containerized emulator.
- **Two governance gate-integrity bugs found and fixed** via independent `kimi`/`opencode` CLI reviews (substring-match exploits in the constitution checker).
- No `:app` module source changed this cycle — this build's user-visible behavior is identical to 1085's, now with real device confirmation on both variants.

## Build info
- App versionName: 1.3.17
- App versionCode: 1087
- Distribution channel: firebase-app-distribution

## Evidence
- Device gate + signature verification: `.lava-ci-evidence/distribute-changelog/firebase-app-distribution/1.3.17-1087-test-evidence.md`
- Full CHANGELOG entry: `CHANGELOG.md`
