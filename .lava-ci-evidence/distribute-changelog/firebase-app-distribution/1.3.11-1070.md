## Lava-Android-1.3.11-1070 — 2026-06-23 (Clean re-spin of 1.3.11 — ships the right binary through the fixed distributor)

**Previous:** 1.3.11-1069 — the corrected binary was built (sha 80370352) but the 1069
distribute published the WRONG binary because the old `firebase-distribute.sh` APK picker
used `find … | head -1` and uploaded the lexically-first stale APK in the directory (the
wrong-binary saga). `last-version-debug` advanced to 1069, so §6.P forbids re-publishing 1069.

1070 is the clean re-spin of the SAME 1.3.11 content, distributed through the now-fixed picker
(`134d0180`: `_pick_apk_by_version` matches `*-${code}-${bt}.apk`, FATAL on ambiguity). The
user-facing version name is unchanged (still 1.3.11); only the build code + rotated auth material
change. Release identity: **1.3.11 (1070)**.

The binary carries the intended 1.3.11 content:

- `cfe838bc` — `fix(search): failed multi-provider stream renders Error+Retry, not a dead-end
  "Nothing found"`.
- `922ecbca` — `fix(§6.AC search): record per-provider streaming-search failures instead of
  dropping them silently`.
- `134d0180` — `fix(§6.Z): firebase-distribute selects APK by exact version code` (the
  wrong-binary distribute bug — so 1070 ships the exact binary built for code 1070).
- Rotated `LAVA_AUTH_OBFUSCATION_PEPPER` + `android-1.3.11-1070` client identity appended to the
  allowlist (append-only, 36 active clients — no existing install is forced to upgrade). The new
  pepper is embedded in the APK (regenerated `LavaAuthGenerated.kt`; the rotated material changes
  the APK SHA vs the 1069 artifact).

**Channel:** firebase-app-distribution (`digital.vasic.lava.client.dev`, §6.AA stage 1 / debug).
**Build path:** `./gradlew :app:clean :app:assembleDebug` → `releases/1.3.11/android-debug/lava-1.3.11-1070-debug.apk`.
