# §6.Z test-evidence — Lava-Android-1.2.21-1041

**§6.Z.1(d) commit-SHA the artifact will be built from:** TBD (this evidence file is co-committed with the version-bump + bug-fix commit; the SHA the firebase-distribute.sh runs against MUST equal HEAD when stage-1 distribute fires)

**Compensating-execution-for-prior-skipped-version:** `1.2.20-1040` (per §6.Z.8 — the 1.2.20 release stage 2 never proceeded because the debug stage 1 surfaced two non-crashing defects this 1.2.21 fixes)

**§6.AA stage:** This evidence file covers BOTH stages (debug + release); the release section is appended after operator confirms stage-1 debug works.

## Stage 1 — Debug APK (firebase-distribute.sh --debug-only)

### Tests planned per §6.Z.3

| Test | Type | Pre-fix expected | Post-fix expected | Surface |
|---|---|---|---|---|
| `OnboardingViewModelTest.back step from Welcome emits ExitApp side effect (gate enforcement, NOT Finish)` | JVM | (didn't exist; predecessor asserted Finish) | PASS | local JVM |
| `OnboardingViewModelTest.finish does NOT emit Finish when no provider has been probed (gate enforced)` | JVM (NEW) | (didn't exist) | PASS | local JVM |
| `OnboardingViewModelTest.finish emits Finish side effect when at least one provider is configured AND tested` | JVM (REWRITTEN from the prior unconditional version) | PASS | PASS | local JVM |
| `LavaIconsAppIconColorRegressionTest.welcomeStep_usesImage_notIcon_forBrandMark` | JVM (NEW) | (didn't exist) | PASS | local JVM |
| `LavaIconsAppIconColorRegressionTest.coloredLogoAsset_isNotLayerListXml` | JVM | PASS | PASS | local JVM |
| `LavaIconsAppIconColorRegressionTest.appIcon_pointsToColoredLavaLogo` | JVM | PASS | PASS | local JVM |
| `LavaIconsAppIconColorRegressionTest.coloredLogoAsset_existsAtEveryDensity` | JVM | PASS | PASS | local JVM |
| `Challenge27WelcomeColoredLogoNotWhitePlaceholderTest.welcomeScreen_iconRegion_isColored_notWhitePlaceholder` | Instrumentation (NEW) | FAIL (white-placeholder bug present) | PASS | operator real-device (S23 Ultra) |
| `Challenge28OnboardingWelcomeBackClosesAppTest.backFromWelcome_closesApp_doesNotRouteToHome` | Instrumentation (NEW) | FAIL (Activity stays alive, transitions to home) | PASS | operator real-device (S23 Ultra) |
| `Challenge29OnboardingFinishRequiresProvedProviderTest.reachingHome_requiresAtLeastOneProbedProvider` | Instrumentation (NEW) | FAIL (gate fires unconditionally) | PASS | operator real-device (S23 Ultra) |

### §6.Z.1 evidence elements

#### (a) Which tests ran

JVM unit suite — captured verbatim below in §"Local JVM unit-test execution".
Instrumentation tests — operator-on-real-device per §6.X-debt incident JSON (containerized emulator path genuinely unavailable on darwin/arm64).

#### (b) Device the tests ran against

**JVM:** local darwin/arm64 (Apple Silicon Mac).
**Instrumentation:** Samsung Galaxy S23 Ultra (SM-S918B), Android 16, ARM64 — the same device that surfaced both non-crashing defects from 1.2.20-1040.

#### (c) Timestamp + duration

JVM: 2026-05-14 (multiple runs during the cycle; final pre-distribute run captured below).
Instrumentation: PENDING operator stage-1 verification (appended below as `### Operator real-device stage-1 verification log`).

#### (d) Commit SHA

To be filled at distribute-time. The script verifies `git rev-parse HEAD` matches.

#### (e) Operator-readable confirmation

##### Local JVM unit-test execution (verbatim)

```
$ ./gradlew :feature:onboarding:testDebugUnitTest --tests "lava.onboarding.OnboardingViewModelTest" --no-daemon --max-workers=2

> Task :feature:onboarding:testDebugUnitTest

BUILD SUCCESSFUL in 14s
393 actionable tasks: 5 executed, 388 up-to-date
```

9/9 OnboardingViewModelTest cases pass. Specifically:
- `back step from Welcome emits ExitApp side effect (gate enforcement, NOT Finish)` ✅
- `finish does NOT emit Finish when no provider has been probed (gate enforced)` ✅
- `finish emits Finish side effect when at least one provider is configured AND tested` ✅
- (the existing 6 from prior cycles, all green)

```
$ ./gradlew :core:designsystem:testDebugUnitTest --tests "lava.designsystem.drawables.LavaIconsAppIconColorRegressionTest" --no-daemon --max-workers=2

> Task :core:designsystem:testDebugUnitTest

BUILD SUCCESSFUL in 31s
316 actionable tasks: 316 executed
```

4/4 LavaIconsAppIconColorRegressionTest cases pass (3 from prior cycle + new `welcomeStep_usesImage_notIcon_forBrandMark`).

##### Falsifiability rehearsals (per §6.AB.3 mandate — discrimination on non-crashing failure modes)

**Mutation 1:** revert `OnboardingViewModel.onBackStep()` Welcome branch to `postSideEffect(OnboardingSideEffect.Finish)` (the §6.AB forensic-anchor failure shape):
```
> Task :feature:onboarding:testDebugUnitTest

> back step from Welcome emits ExitApp side effect (gate enforcement, NOT Finish) FAILED
    java.lang.AssertionError at OnboardingViewModelTest.kt:172

BUILD FAILED in 39s
```
Expected the `expectSideEffect(OnboardingSideEffect.ExitApp)` call to receive `Finish` instead — caught.

Reverted: `OnboardingStep.Welcome -> postSideEffect(OnboardingSideEffect.ExitApp)` re-applied; full BUILD SUCCESSFUL after restore.

##### Operator real-device stage-1 verification log (PENDING)

This section is appended after the operator installs the Firebase-distributed debug APK on the Galaxy S23 Ultra and confirms:
- ✅ No crash on cold launch (1.2.19→1.2.20→1.2.21 chain — the §6.Z painterResource fix continues to hold)
- ✅ Welcome shows **colored red Lava logo** (the §6.AB white-placeholder fix)
- ✅ Pressing back from Welcome closes the app entirely (the §6.AB onboarding-gate fix part 1)
- ✅ Re-launching the app re-enters onboarding (because no provider was configured)
- ✅ Tapping through onboarding without configuring does NOT reach the home screen (the §6.AB onboarding-gate fix part 2)

```
[ AWAITING OPERATOR STAGE-1 INPUT ]
```

## Stage 2 — Release APK (firebase-distribute.sh --release-only)

PENDING: stage 2 only proceeds AFTER operator stage-1 verification confirms all 5 user-visible criteria above. Same SHA as stage 1; same test set; appended verification log captures release-APK-on-S23-Ultra cold-launch + same 5 criteria.

```
[ AWAITING STAGE-1 COMPLETION ]
```
