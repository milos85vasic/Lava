# §6.Z device-gate evidence — Lava-Android 1.3.9 (1066)

**Commit:** `78cd48ea` (current HEAD at gate time; build produced from this tree — app/Kotlin
source carrying the 5-layer search fix landed by `646474c0`; the matched on-device-API
key-handoff fix base is `462f8359`).
**Device:** Genymotion Pixel 9 / API 35 / arm64 (`127.0.0.1:6555`, §6.AH VM path,
§6.AG Containers-submodule-driven via `scripts/run-release-canary.sh`).
**Artifacts:** `lava-1.3.9-1066-release.apk` (6.7 MB R8) — the end-to-end search fix
(on-device-API per-provider auth thread-through: client-side host/route/key delivery).
**Date:** 2026-06-14. **Mode:** overnight autonomous (operator delegated; the device gate
is the §6.AA stage-1→stage-2 verifier). Device: android_release=15 sdk=35 model=Pixel 9 abi=arm64-v8a

## Debug search e2e reference (the unit + debug-device proof, prior in this cycle)

`646474c0` / `.lava-ci-evidence/search-verification/2026-06-14-keyhandoff-fix-verify.md`:
the §6.J falsifiability rehearsal (caching pre-fix → empty cursor → `AssertionError:
expected:<1> but was:<0>`; lazy fix → PASS, mutation reverted) plus the debug-pair device
search returning chucker `200` on `/v1/archiveorg/search?query=rince&page=0`. This 1.3.9
evidence file is the RELEASE-pair re-verification of the same flow on the R8 artifacts.

## R8 release cold-start canary (§6.AA stage-2 / R8-only crash-class check)

```
package: digital.vasic.lava.client
apk: releases/1.3.9/android-release/lava-1.3.9-1066-release.apk  (R8-minified, 6.7 MB)
apk_sha256: a8412f0c5061145059f431c6d09f28561b4fe6020287877e7727c5caa71fe974
LaunchState: COLD   TotalTime: 2258
pid_after_launch: 29159   fatal_lines: <none>
==> RELEASE CANARY PASS — process alive, no fatal
```
Evidence: `.lava-ci-evidence/release-canary/1.3.9-1066-client/`.

## RELEASE-pair SEARCH e2e (the load-bearing §6.Z proof for this fix)

Clean matched signed pair: all 4 lava packages uninstalled, **release** api-app 0.2.9-14
installed (canary), **release** client 1.3.9-1066 installed via `:app:installRelease`,
`pm clear digital.vasic.lava.client`.

- **Matched release signing → permission grant:** `dumpsys package digital.vasic.lava.client`
  reports `digital.vasic.lava.permission.READ_API_KEY: granted=true` (versionCode=1066).
  This is the grant the §6.Z permission audit requires — the release client can read the
  release api-app's key-handoff provider.
- **api-app engine started → Running**, foreground service `isForeground=true types=0x40000000`
  (SPECIAL_USE), mDNS `_lava-api._tcp` "Lava API" advertised + discovered.
- **Client fresh onboarding:** Welcome ("4 providers available") → Get Started →
  "Choose your API" → discovered `10.0.3.16:8443` under "On your network" (mDNS) → tap row
  → connectivity probe PASSED (advanced to "Pick your providers" with the provider list
  loaded from the authenticated API) → Deselect all → Internet Archive → Next →
  "Configure Internet Archive" (no credentials) → Continue → "All set!" → Start Exploring
  → Home.
- **Search:** opened the search bar, query "prince", ENTER.

### Results screen (user-visible — primary §6.Z assertion)

Multiple **real Internet Archive torrent rows** rendered, e.g.:
"Oregon Trail, The", "Console Living Room: Sega Genesis/32X",
"Webwide Crawldata 2023-01-22…", "SPN Outlinks Crawldata 2023-04-11…".
Screenshot: `.lava-ci-evidence/release-canary/1.3.9-1066-search-e2e/2026-06-14-prince-results-release.png`
(sha256 `f6ce0432f5e4e01d51a64ac5a4df723fd37eeed6919ab60e35791a7b55c0b1ef`).

### Network-layer corroboration

The release client + api-app are non-debuggable (R8 release), so `run-as` cannot pull
`chucker.db` — the on-screen rendered results are the §6.Z primary user-visible proof
(an empty/401 response renders "Nothing found", the exact pre-fix symptom). Corroborated
at search time by the live socket state on the device:

```
tcp6  ::ffff:10.0.3.16:8443    LISTEN                          (engine serving)
tcp6  ::ffff:10.0.3.16:45344 → ::ffff:10.0.3.16:8443  ESTABLISHED  (client ↔ engine)
```

The debug-pair chucker `200` on `/v1/archiveorg/search` (reference above) plus the
release-pair rendered results + ESTABLISHED engine socket together prove the authenticated
search round-trip works on the R8 release artifacts.

## §6.Z compliance
- R8 release client + R8 release api-app EXECUTED on device (not source-compiled): ✅
- Cold-start canary GREEN on R8 release (client + api-app): ✅
- Matched release-pair search e2e: real results rendered + `READ_API_KEY granted=true` +
  engine ESTABLISHED socket: ✅
- Paired with api-app 0.2.9-14 (FGS specialUse device-verified — see that channel's evidence).
- READY for §6.AA two-stage distribute (debug first, then release).
