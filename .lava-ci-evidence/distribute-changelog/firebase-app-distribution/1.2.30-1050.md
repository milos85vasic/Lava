## Lava-Android-1.2.30-1050 / Lava-API-Go-2.3.19-2319 — 2026-05-18 (§6.AD-debt FULLY DRAINED + T7 disk migration)

**Previous published:** Lava-Android-1.2.29-1049 (debug + release distributed 2026-05-17 evening).

**User-visible release:** TOOLING + INFRASTRUCTURE cycle. No app code changes from 1.2.29 — the Android APK + lava-api-go binary are functionally byte-identical to 1.2.29 (modulo build timestamp + Spotless re-formatting). Test the upgrade smoke-path (cold launch survives + cold-launched home screen renders) to confirm version bump didn't regress.

### What landed

- **§6.AD-debt FULLY DRAINED.** All three originally-OWED HelixConstitution `CM-*` gate items closed this cycle:
  - `CM-SCRIPT-DOCS-SYNC` (commit `11820734`): standalone scanner + 7-fixture hermetic test + companion `docs/scripts/check-script-docs-sync.sh.md` + wrapper integration.
  - `CM-COMMIT-DOCS-EXISTS` (commit `977630c3`): scanner + 8-fixture hermetic test + companion guide. Verifies every file-path citation in commit message bodies resolves at HEAD. Self-discovery during dev caught its OWN commit body's Bluff-Audit phantom path; tightened indent-skip heuristic.
  - `CM-SUBAGENT-DELEGATION-AUDIT` (commit `2a0e11f4`): scanner + 8-fixture hermetic test + audit-dir README. Verifies subagent-dispatch commit messages have matching audit entries. Effective-from cutoff 2026-05-19 grandfathers ~30 historical subagent-referencing commits.
- **§6.J forensic anchor** (commit `6b6cc358`): `check_constitution_test.sh` slow-cp discovery — the hermetic test copies the entire 8GB monorepo per fixture run, blocking sweep wall-time at ~5 minutes per test. Three remediation options recorded for follow-up cycle.
- **CONTINUATION.md updates** (commits `7dc20171` + `2af3829a`): §6.S compliance for the cycle's milestones.
- **T7 USB disk migration:** 5G → 108G free on main disk. 7 home-dir caches symlinked to T7 (~/.cache, ~/.npm-new-cache, ~/.lmstudio, ~/.ollama, ~/.local/share/opencode, ~/.android, ~/Library/Developer/Xcode). `~/.zshrc` updated with `GRADLE_USER_HOME=/Volumes/T7/Gradle`, `XDG_CACHE_HOME`, `NPM_CONFIG_CACHE`. Pixel_8 emulator zombie (12h33m runtime) cleanly stopped via `adb -s emulator-5554 emu kill` per §6.M.

### Falsifiability rehearsals (§6.J anti-bluff)

Bluff-Audit stamps recorded in each commit body. For the three new gates: each scanner exercised against a deliberately-broken fixture (orphan-doc / phantom-commit-ref / post-cutoff-subagent-no-entry) producing the exact violation output documented. Reverted via per-test self-cleanup.

### Test execution evidence (§6.Z)

- **C00 Challenge00CrashSurvivalTest** + **C01 Challenge01AppLaunchAndTrackerSelectionTest** both EXECUTED against the new 1.2.30-1050 debug APK on Pixel_8 / API 35 (host-direct emulator due to §6.X-debt darwin/arm64 gap). BUILD SUCCESSFUL in 28s; `Finished 2 tests on Pixel_8(AVD) - 15`. Test-evidence file: `.lava-ci-evidence/distribute-changelog/firebase-app-distribution/1.2.30-1050-test-evidence.md`.
- Cold-launch verification: `am start -W -n digital.vasic.lava.client.dev/digital.vasic.lava.client.MainActivity` returns `Status: ok / LaunchState: COLD / TotalTime: 1828ms`. No FATAL/AndroidRuntime exceptions in logcat post-launch. MainActivity is mFocusedApp.
- Both APIs running new binary v2.3.19: PROD `:8443 → HTTP 200 (55ms)`, DEV `:8543 → HTTP 200 (3.5ms)`.

### Distribute-readiness

- ✅ §6.P versionCode 1050 > last-version-debug 1049 + > last-version-release 1049
- ✅ §6.Y bump-first applied (1049 → 1050 + 2318 → 2319; bumps happened at start of cycle)
- ✅ §6.W mirrors converged on github + gitlab (8 commits this session, all pushed)
- ✅ §6.Z evidence file present with matching commit SHA + recent timestamp + BUILD SUCCESSFUL for C00 + C01
- 🟡 §6.AA two-stage: this Stage 1 debug distribute. Stage 2 release pending operator confirmation.

`Classification:` project-specific.
