Lava-Android 1.2.4 (1024) — 2026-05-05

What's new vs 1.2.3 (1023):

FIXED:
- The 2 Crashlytics crashes from 1.2.3 are mitigated:
  * Removed redundant FirebaseApp.initializeApp() call (FirebaseInitProvider auto-init covers it)
  * Extracted Firebase init into FirebaseInitializer with per-SDK runCatching guards (Crashlytics, Analytics, Performance)
  * Added Firebase keep rules to ProGuard so R8 cannot strip reflective entry points
  * Closure log: .lava-ci-evidence/crashlytics-resolved/2026-05-05-firebase-init-hardening.md
  * Validation: 5 JVM unit tests + Challenge13FirebaseColdStartResilienceTest

ADDED:
- AnalyticsTracker wired into real user paths so events fire from the actual Login/Search/Topic/ProviderLogin flows
- lava-api-go gains a FirebaseTelemetry Gin middleware that records 5xx + panics as non-fatals
- §6.P Distribution Versioning + Changelog Mandate (this notes file is the inaugural application)
- §6.O Crashlytics-Resolved Issue Coverage Mandate propagated to all 16 vasic-digital submodules

Tester action requested:
- Re-install on the same device(s) where 1.2.3 ran (debug or release).
- Cold-start the app several times — assert no crash dialog appears.
- Run normal user flows: open the app, switch trackers, search "ubuntu", view a topic, download a .torrent.
- Confirm in the Firebase Console (https://console.firebase.google.com/project/lava-vasic-digital/crashlytics) that no NEW crash signatures appear within ~10 minutes of installing 1.2.4.

Versions in this build:
- Android: 1.2.4 (1024)
- Ktor proxy: 1.0.5 (1005)
- ServiceAdvertisement.API_VERSION: 1.0.5
- lava-api-go: 2.0.10 (2010)

Full changelog: see CHANGELOG.md at the repo root.
