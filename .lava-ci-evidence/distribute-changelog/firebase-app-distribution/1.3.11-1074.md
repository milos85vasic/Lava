## Lava-Android-1.3.11-1074 — 2026-06-25 (LVA-008 nav-teardown candidate fix + anti-bluff test strengthening)

**Previous published:** 1.3.11-1072. (1073 built + canary-gated, never distributed.)

1074 bundles the LVA-008 search→back nav-teardown candidate fix (6th attempt) + Challenge strengthening.
**versionName HELD at 1.3.11** — LVA-008 is gate-verified (C00 + C06 + C11 on the thinker
containerized-KVM emulator) BEFORE ship; no user-facing "fixed" claim until the gate confirms it.

- **LVA-008 (candidate):** inner nested NavHost provides an Activity-scoped `LocalLifecycleOwner`
  (`core:navigation` `NavigationHost.activityScopedLifecycle` + `NestedMobileNavigation`), so the
  Activity drives inner search/search_input entries to ≥CREATED before teardown → no `IllegalStateException`
  process crash at activity-destroy. ViewModelStore/SavedStateRegistry untouched. Verify: Challenges C06+C11.
- **Test-only:** Challenge21 (real back-press + isFinishing + onboarding-pref-false), Challenge29
  (home empty-state absent when onboarding unproved). §6.AB WEAK 10→8.
- **§6.N bluff-hunt:** 7/7 GENUINE, 0 bluffs.

**Channel:** firebase-app-distribution (debug `.dev` + release). §6.AA two-stage: debug → operator verify → release.
