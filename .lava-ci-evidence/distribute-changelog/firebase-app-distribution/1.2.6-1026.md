Lava-Android 1.2.6 (1026) — 2026-05-05

What's new vs 1.2.5 (1025):

FIXED (Crashlytics-driven):
- The "Opening Trackers from Settings crashes the app" bug surfaced by a real tester is fixed. Root cause: TrackerSelectorList Composable used LazyColumn nested inside the parent screen's Column(verticalScroll(rememberScrollState())) — Compose throws IllegalStateException at measure time when a lazy layout receives unbounded height. Fix: replaced LazyColumn with plain Column (the tracker list is bounded, ≤ 6 entries — no virtualization needed).
- Closure log: .lava-ci-evidence/crashlytics-resolved/2026-05-05-tracker-settings-nested-scroll.md
- Validation: 2 structural unit tests catching LazyColumn import/usage regression.
- Challenge: Challenge14TrackerSettingsOpenTest drives the real screen on the §6.I matrix.

ADDED:
- §6.Q Compose Layout Antipattern Guard — root constitution forbids the nested-scroll antipattern that produced this crash. Per-feature structural tests + Challenge Tests are the mechanical gates.

Tester action requested:
- Re-install on the same device(s) where 1.2.5 ran.
- Open the bottom-tab Menu → tap "Trackers" — the previous build crashed here. Post-fix the screen renders the tracker list (rutracker, rutor, etc.).
- Tap each tracker row — confirm "Switched to <tracker>" snackbar appears + the row marker indicator moves.
- Confirm in Crashlytics no NEW signature for "Vertically scrollable component was measured with an infinite maximum height constraint" appears within 10 minutes of using 1.2.6.

Versions in this build:
- Android: 1.2.6 (1026)
- Ktor proxy: 1.0.5 (1005) (unchanged)
- lava-api-go: 2.0.10 (2010) (unchanged)
