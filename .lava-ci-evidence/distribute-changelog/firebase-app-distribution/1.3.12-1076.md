## Lava-Android-1.3.12-1076 — 2026-06-25 (search works again + Sync-toggle crash fixed + display fixes)

**Previous published:** 1.3.11-1075.

Real user-facing fixes (versionName bumped 1.3.11→1.3.12):
- **Search works again** — was querying a hardcoded 4-provider list divorced from the onboarded set
  → 0 results → "Something went wrong"; now uses exactly the onboarded providers (single source of truth),
  deterministic chip ordering, loading indicator + empty-state. §11.4.146 RED→GREEN.
- **Sync/bind/mirror toggle crash fixed** — release-only SerializationException at
  ProviderConfigViewModel.kt:92 (missing kotlinx-serialization plugin + R8 keep-rules); all 3 wire classes.
- **Display fixes** — friendly provider-chip names, Welcome count==picker, discovered-API friendly name,
  select-all skips credential providers, cloud-preset subtitle corrected.

Known open: LVA-008 search→back nav-teardown crash (upstream androidx defect, 8 candidates falsified).
Coverage: 13 new/rewritten Challenges (C48–C57, C31–C35). Auth rotated android-1.3.12-1076 (fresh pepper).

§6.Z gate: coordinated compile GREEN (all Challenges + fixes); on-device R8-release + search execution on
thinker containerized-KVM. **Channel:** firebase-app-distribution (debug `.dev` + release).
§6.AA: standing operator policy — ship prod every cycle, operator tests prod (combined-distribute-authorization:
standing-operator-policy-2026-06-25).
