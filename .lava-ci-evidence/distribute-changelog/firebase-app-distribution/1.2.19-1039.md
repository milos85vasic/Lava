## Lava-Android-1.2.19-1039 / Lava-API-Go-2.3.8-2308 — 2026-05-14 (Welcome-screen colored logo fix + §6.Y Post-Distribution Version Bump Mandate)

**Previous published:** Lava-Android-1.2.18-1038 / Lava-API-Go-2.3.7-2307

### Fixed (Android client)

- **Welcome screen now renders the colored Lava logo** instead of
  the monochrome notification glyph. Pre-fix:
  `LavaIcons.AppIcon = Icon.DrawableResourceIcon(R.drawable.ic_notification)`
  surfaced the Android-required monochrome notification icon as
  the brand mark on first-launch users' Welcome screen. Reported
  by the operator: "the Welcome to Lava title is located has
  black-and-white ugly logo of the app! It MUST BE our nicely
  colored red log in full color!". Fix: introduced
  `R.drawable.ic_lava_logo` (layer-list compositing the colored
  launcher background + foreground PNGs at 5 densities — mdpi
  through xxxhdpi) in `core:designsystem`, rewired
  `LavaIcons.AppIcon` to it, preserved the monochrome icon as
  `LavaIcons.NotificationIcon` for the AndroidManifest.

### Constitutional (25th §6.L invocation)

- **§6.Y Post-Distribution Version Bump Mandate added.** After
  every successful distribution of any artifact, the FIRST commit
  in the new development cycle that touches code MUST bump the
  artifact's versionCode (and per-artifact equivalent). The
  versionName MUST be bumped when warranted (patch/minor/major).
  §6.Y-debt opened for mechanical enforcement via pre-push hook
  + check-constitution.sh. Propagated to root CLAUDE.md, root
  AGENTS.md, lava-api-go/CONSTITUTION.md.

  Self-application: this release IS the first §6.Y enforcement —
  the bump from 1038→1039 in app/build.gradle.kts is the first
  hunk of the icon-fix commit, demonstrating the rule's intent.

### Tests + falsifiability evidence (per §6.J / §6.N.1.1)

- **JVM unit:** `core/designsystem/.../LavaIconsAppIconColorRegressionTest.kt` —
  reads `LavaIcons.kt` source and asserts `AppIcon` references
  `R.drawable.ic_lava_logo`; verifies colored PNG layer assets
  exist at every density (10 files: 5 densities × 2 layers);
  verifies the composite XML drawable exists. Falsifiability
  rehearsed: reverted `AppIcon` to `R.drawable.ic_notification`;
  observed AssertionError with full guidance message; restored;
  pass.

- **Compose UI Challenge Test C26:**
  `app/src/androidTest/.../Challenge26WelcomeColoredLogoTest.kt`
  drives the real Welcome screen on the gating matrix; samples
  the rendered bitmap and asserts measurable RGB variance per
  channel (rangeR/G/B > 32 each, rgbDelta > 32) — i.e., the
  icon renders as a colored composite, not a single-tone
  monochrome glyph. Source compiles via
  `:app:compileDebugAndroidTestKotlin`. Instrumentation gating
  run owed at next §6.X-mounted gate host.

### Distribute readiness

All operator inputs satisfied (carried over from 1.2.18-1038):
real Firebase project + token + 3 testers, real keystores, real
tracker credentials. Phase 1 Gates 4+5 prerequisites bumped:
`LAVA_AUTH_OBFUSCATION_PEPPER` rotated, `LAVA_AUTH_CURRENT_CLIENT_NAME`
set to `android-1.2.19-1039`, `LAVA_AUTH_ACTIVE_CLIENTS` appended
with `android-1.2.19-1039:<UUID>`. §6.P gates pass.

### What's NOT in this version

- §6.X-debt remains open (Linux x86_64 gate-host provisioning).
  Same caveat as 1.2.18-1038: darwin/arm64 podman host-net
  isolation prevents mDNS LAN auto-discovery from the dev
  container; phone testers downloading the .dev APK MUST
  manually configure the dev endpoint after install (Settings
  → Endpoint → host-IP:8543).
- §6.Y-debt is the new entry: pre-push hook + check-constitution.sh
  enforcement of bump-first ordering documented but not
  mechanically enforced yet.
