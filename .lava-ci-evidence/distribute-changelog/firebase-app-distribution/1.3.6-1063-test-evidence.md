# §6.Z device-gate evidence — Lava-Android 1.3.6 (1063)

**Commit:** `2ea37a02` (code-bearing commits: `9ae9ab90` server fix + `132d1b07`
e2e + `99e5893a`/`6ce100bc` client tests; commits after `6ce100bc` are docs/evidence
only — the built APK's app/go source is byte-identical to HEAD).
**Device:** Genymotion Pixel 9 / API 35 / arm64 (`127.0.0.1:6555`, §6.AH VM path,
§6.AG Containers-submodule-driven via `scripts/run-genymotion-challenges.sh`).
**Artifact under test:** `app/build/outputs/apk/debug/app-debug.apk` (built
`BUILD SUCCESSFUL in 2m30s`, embeds the router-fixed `.so`; api-source.hash
`dbe891a4…` matches the committed Go source).
**Date:** 2026-06-13.

## connectedDebugAndroidTest on the real VM (verbatim BUILD SUCCESSFUL, runner = containers-submodule)

| Challenge | Tests | Skipped | Failed | Result | Evidence dir |
|---|---|---|---|---|---|
| C00 `Challenge00CrashSurvivalTest` (cold-start canary) | 1 | 0 | 0 | `BUILD SUCCESSFUL in 5m52s` | `genymotion/1.3.6-1063-20260613T183519Z` |
| C26 `Challenge26ApiDiscoveryAndConnectivityTest` (ApiSelection rendered-UI) | 6 | 0 | 0 | `BUILD SUCCESSFUL in 1m32s` | `genymotion/1.3.6-1063-onboarding-Challenge26ApiDiscoveryAndConnectivityTest` |
| C25 `Challenge25OnboardingFreshInstallTest` | 1 | 0 | 0 | `BUILD SUCCESSFUL in 1m12s` | `genymotion/1.3.6-1063-onboarding-Challenge25OnboardingFreshInstallTest` |
| C20 `Challenge20OnboardingWizardFullFlowTest` | 1 | 0 | 0 | `BUILD SUCCESSFUL in 59s` | `genymotion/1.3.6-1063-onboarding-Challenge20OnboardingWizardFullFlowTest` |

All four `==> GENYMOTION CHALLENGE PASS` on `Pixel 9 - 15` (API 35). 0 failed, 0
unexplained skips across 9 tests.

## Why this set gates the 1.3.6-1063 change

The headline fix is the onboarding provider-catalogue load (public `/providers`).
C00 is the mandatory cold-start canary (the `LavaApplication.onCreate` / Hilt /
`setContent` crash class). C26 drives the exact ApiSelection screen from the operator's
bug report. C25 + C20 traverse the full onboarding wizard. The API-sourced
replace-not-merge LOGIC is additionally proven deterministically off-device by
`OnboardingViewModelDynamicProvidersTest` (real repository→use-case→registry→VM over
MockWebServer) and the server-side `/providers` public-vs-gated boundary by
`TestProviders_PublicCatalogue_PerProviderStillGated` (real `router.Build` + full auth
chain) — both falsifiability-rehearsed by the main stream.

## §6.Z compliance

- Tests EXECUTED (not source-compiled) against the exact 1.3.6-1063 debug artifact: ✅
- Commit SHA matches HEAD (code-identical): ✅
- Cold-start canary (load-bearing): ✅ C00 GREEN
- Runner = containers-submodule (Genymotion VM, §6.AH/§6.AG): ✅
- Stage: this is the §6.AA **stage-1 (debug)** evidence. The release (R8) cold-start
  canary is appended as a `release-stage` section before the stage-2 release distribute.
