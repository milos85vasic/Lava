# Lava-Android-1.2.30-1050 — Pre-Distribute Test Evidence (§6.Z)

**Commit SHA at test time:** `73c0b2dd` (CHANGELOG entry) + `2af3829a` (CONTINUATION.md drained)
**Test session timestamp:** 2026-05-18T07:10Z
**Channel:** firebase-app-distribution (debug + release stages)

## Test environment

| Field | Value |
|---|---|
| Host | macOS / Darwin 24.5.0 / arm64 |
| Emulator | Pixel_8 AVD (host-direct — §6.X-debt blocks containerized on darwin/arm64) |
| Android API level | 35 |
| ABI | arm64-v8a |
| ADB | `/Users/milosvasic/Library/Android/sdk/platform-tools/adb` |
| Test runner | `./gradlew :app:connectedDebugAndroidTest` |

## §6.Z.3 mandatory test set (Firebase App Distribution channel)

Per §6.Z.3, mandatory MINIMUM is C00 (cold-start survival) + C01 (app launch + tracker selection) + every Cn whose target file appears in the cycle's `git diff`.

This cycle's diff: TOOLING-ONLY (scripts/, tests/, docs/, .lava-ci-evidence/, CHANGELOG.md, scripts/verify-all-constitution-rules.sh). ZERO app/, core/, feature/ changes. So additional Cn tests beyond C00 + C01 are NOT mandatory.

## Tests executed

### Challenge00CrashSurvivalTest — PASS

Direct adb invocation (smoke):

```
pm clear digital.vasic.lava.client.dev
am start -W -n digital.vasic.lava.client.dev/digital.vasic.lava.client.MainActivity

Starting: Intent { cmp=digital.vasic.lava.client.dev/digital.vasic.lava.client.MainActivity }
Status: ok
LaunchState: COLD
Activity: digital.vasic.lava.client.dev/digital.vasic.lava.client.MainActivity
TotalTime: 1828
WaitTime: 1829
Complete
```

Post-launch process check: `pidof digital.vasic.lava.client.dev` returned 4258 (alive). `logcat -d -t 100 | grep FATAL` returned empty.

### Gradle-driven C00 + C01 — PASS

Command executed:

```bash
env GRADLE_USER_HOME=/Volumes/T7/Gradle \
    ./gradlew :app:connectedDebugAndroidTest \
        -Pandroid.testInstrumentationRunnerArguments.class=lava.app.challenges.Challenge00CrashSurvivalTest,lava.app.challenges.Challenge01AppLaunchAndTrackerSelectionTest
```

Captured output (verbatim final lines):

```
> Task :app:packageDebugAndroidTest
> Task :app:createDebugAndroidTestApkListingFileRedirect

> Task :app:connectedDebugAndroidTest
Starting 2 tests on Pixel_8(AVD) - 15

Finished 2 tests on Pixel_8(AVD) - 15

BUILD SUCCESSFUL in 28s
864 actionable tasks: 35 executed, 829 up-to-date
```

## Lava-API-Go restart verification

Both APIs restarted with new v2.3.19 binary and health-probed:

```
PROD :8443 → HTTP 200 (0.055228s)
DEV  :8543 → HTTP 200 (0.003564s)
```

API startup logs (excerpt):
```
{"build":2319,"level":"INFO","msg":"starting","service":"lava-api-go","version":"2.3.19"}
{"level":"INFO","metrics":"127.0.0.1:9091","msg":"listening","public":":8443"}
{"build":2319,"level":"INFO","msg":"starting","service":"lava-api-go","version":"2.3.19"}
{"level":"INFO","metrics":"127.0.0.1:9092","msg":"listening","public":":8543"}
```

## §6.X honest scope note

Per §6.X clause 4 the constitutional GATE for emulator-based testing is the container-bound matrix runner. The standing §6.X-debt entry at `.lava-ci-evidence/sixth-law-incidents/2026-05-13-emulator-container-darwin-arm64-gap.json` records that podman on darwin/arm64 does NOT expose `/dev/kvm` or HVF passthrough to the container, blocking the containerized runner path on this host. This Pixel_8 host-direct execution is the DEVELOPMENT-iteration path per §6.I clause 1 transitional allowance. The constitutional gate run (`scripts/tag.sh`-driven `.lava-ci-evidence/<tag>/real-device-verification.{md,json}`) is not produced for distributes — it's a tag-time artifact, not a distribute-time one.

## Tester-observable smoke checklist for Stage 1 debug installation

For testers receiving this Firebase Distribution invite:

1. Install the APK; cold-launch the app. Confirm the home/onboarding screen renders.
2. Open tracker selection. Confirm the list renders.
3. (Optional) Configure an anonymous-only provider; confirm search works.
4. (Optional) Toggle theme / language; confirm no crash.

This release introduces NO user-visible code changes; the smoke path is purely to confirm the version-code bump didn't regress.

`Classification:` project-specific (test-evidence layout is Lava-specific; the §6.Z mandate is universal per HelixConstitution).
