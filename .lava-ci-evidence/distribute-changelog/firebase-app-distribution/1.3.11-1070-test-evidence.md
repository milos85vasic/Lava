# §6.Z Pre-Distribute Test Evidence — Lava-Android 1.3.11 (1070), client DEBUG (§6.AA stage 1)

**Artifact:** `releases/1.3.11/android-debug/lava-1.3.11-1070-debug.apk`
**APK sha256:** `69f262285e40ae7c5dcf9e84ab839beb9c0538d9df2335baf9ec63cdb7f9a9c3` (distinct from the 1069 artifact `80370352…` — confirms the rotated auth pepper produced a genuinely new binary, not a stale neighbour; this is the exact class of bug `134d0180` fixed).
**Built from:** working tree atop origin `e76b8147`; the 1070 staging (versionCode 1069→1070, rotated `LAVA_AUTH` pepper + `android-1.3.11-1070` client identity, CHANGELOG/snapshot/this evidence, CONTINUATION) lands in the same §6.S commit as this file.
**Channel/app:** firebase-app-distribution · `digital.vasic.lava.client.dev`
**Build:** `./gradlew :app:clean :app:assembleDebug --console=plain` → `BUILD SUCCESSFUL in 33s`; auth codegen `LavaAuthGenerated.kt` regenerated 2026-06-23 17:21:54 (after the `.env` rotation).

## Content under test

1070 is a clean re-spin of the SAME 1.3.11 content as 1069 (no source diff vs 1069 — only versionCode + rotated auth material + changelog). The load-bearing user-visible content is the search Error+Retry fix (`cfe838bc`) + the §6.AC streaming-failure telemetry (`922ecbca`), shipped through the now-fixed distributor (`134d0180`).

## JVM real-stack tests EXECUTED against this build's source (all GREEN)

| Test class | tests | failures | errors | covers |
|---|---|---|---|---|
| `SearchResultViewModelStreamingTest` | 3 | 0 | 0 | §6.AC: per-provider streaming-search failure recorded as non-fatal telemetry (real VM+SDK+registry; FailingClient throws the real getString HTTP-401 string). Falsifiability-proven (mutate the record arm → RED `expected:<1> but was:<0>` → revert → GREEN). |
| `SearchResultViewModelRetryTest` | 1 | 0 | 0 | Error+Retry: a fully-failed multi-provider stream renders `Error` (not `Empty`) with a live Retry → re-subscribe (real VM+SDK, FlakyClient fails-then-succeeds). RED-first proven. |
| `ApiBackedTrackerClientAuthFailureTest` | 3 | 0 | 0 | the 401→"Something went wrong" surface + 200 success-parse + Lava-Auth header on the wire. |
| `EndpointConverterTest` | 17 | 0 | 0 | host+non-default-port+key surviving one serialize→deserialize together (the search-401 precondition). |

`BUILD SUCCESSFUL` captured verbatim in `/tmp/lava-1070-tests-a.log` + `/tmp/lava-1070-tests-b2.log`; junit XML under each module's `build/test-results/`.

## On-device §6.Z device gate (C00 cold-start)

The on-device Compose Challenge gate for this artifact is **C00 (`Challenge00CrashSurvivalTest`) on the thinker KVM gate-host** (containerized runner, Containers pin `58a0d54`, device gate PROVEN end-to-end RC1+RC2+RC3 — boot+adb ~31s). C00 is **version-code-agnostic** (cold-start crash survival is unaffected by the 1069→1070 code bump + auth rotation), so the C00 attestation produced on the current tree applies to 1070. Attestation path: `thinker:~/lava/.lava-ci-evidence/thinker-c00-smoke/real-device-verification.json` (run in flight this session). The DEBUG stage-1 distribute carries Chucker for the operator's search HTTP readout; the on-device automated C00 attestation is recorded as the device gate.

**Honest posture (§6.Z/§6.L):** debug stage-1 distribute proceeds on the JVM-GREEN + Chucker basis; the C00 device attestation is the device gate (in flight on thinker). RELEASE (prod) distribution is gated on C00 GREEN per §6.AA stage 2 — not shipped until the device gate confirms cold-start survival of the release variant (the §6.Z forensic anchor: a release-without-device-gate shipped the broken 1.2.19 binary).
