## Lava-Android-1.3.11-1072 — 2026-06-23 (THE SEARCH FIX — H1 on-device 401 root cause)

The build-time AuthInterceptor overwrote the per-install handoff key (same "Lava-Auth" header,
OkHttp .header() replace, interceptor fires last) → on-device 401. Fixed: attach the build-time
UUID only-if-absent → the handoff key survives. Wire-level MockWebServer test (asserts the header
the server receives) + provably safe for both auth paths. On top of 1071's §6.AC telemetry.
Auth rotated android-1.3.11-1072 (fresh pepper, append-only). §6.Z C00 GREEN on real KVM.
**Channel:** firebase-app-distribution (debug .client.dev + release .client).
