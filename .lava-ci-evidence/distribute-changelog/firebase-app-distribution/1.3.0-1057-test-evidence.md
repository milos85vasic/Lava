# §6.Z test-evidence — Lava-Android client 1.3.0-1057 (debug + release)

- **Date:** 2026-06-04
- **Commit SHA (HEAD distributed):** `848ce20c` (fixes landed in `dc547861`; on-device fix verification in `848ce20c`).
- **Build identity:** REBUILT this cycle after a §RELEASE-ROTATION auth rotation —
  fresh pepper + new UUID registered for client `android-1.3.0-1057` (all 22 prior
  clients kept ACTIVE, so the already-distributed 1.2.36-1056 is not regressed). Both
  variants embed the rotated identity (verified: `LavaAuthGenerated.kt` regenerated for
  debug + release).
- **Artifacts (aapt2-confirmed `versionCode='1057' versionName='1.3.0'`, signed):**
  - debug   `releases/1.3.0/android-debug/digital.vasic.lava.client-1.3.0-debug.apk` — sha256 `078e12f690ec440ccecf36769c6a3e12…`
  - release `releases/1.3.0/android-release/digital.vasic.lava.client-1.3.0-release.apk` — sha256 `9f65db4f0503577cabc2d1d4e67d2bca…` (R8-minified, 6.6 MB)

## Executed tests + results (this cycle, bounded `--no-daemon --max-workers=2`, BUILD SUCCESSFUL)

- `core.network.impl.AuthInterceptorTest` — **3/3, 0 fail**. Incl. *"intercept adds
  Lava-Auth header carrying base64 of decrypted UUID"* — the load-bearing post-rotation
  check: the freshly-rotated pepper + UUID **decrypt correctly** at runtime, so the client
  will not crash (`AEADBadTagException`) on its first authenticated request.
- `feature.onboarding.OnboardingViewModelTest` — **15/15, 0 fail**. Incl. the Bug-A
  regression (discovered on-device API host is the BARE IP, not `host:port`). Falsifiability
  (recorded earlier this cycle): reverting the host-strip → test FAILED, reverted → PASS.
- `api-app.control.ApiEngineControllerTest` — **7/7, 0 fail**. Incl. the Bug-B regression
  *"start is idempotent — redundant start does not re-bind or re-advertise"*. Falsifiability:
  disabling the guard → registerCalls==2, expected 1 → FAILED, reverted → PASS.

## On-device verification (carried forward) + what is deferred

- Bug A + Bug B were verified on the real Samsung S23 Ultra (`R5CW33CBVQV`, Android 16 / API 36)
  in commit `848ce20c`: discovered row shows single `192.168.31.119:8443`, selecting it
  connects + advances to "Pick your providers"; api-app double-start → no bind error, engine
  health=200. Evidence: `.lava-ci-evidence/app-linking/ondevice-dc547861/`.
- **Deferred to operator post-distribute testing (S23 offline this session):** cold-start
  canary on the freshly-rotated debug APK + on-device linking re-run against this exact build.
  Per the operator's chosen workflow ("any issues we spot we will be fixing, release new
  versions again") the operator exercises the Firebase-distributed build on the S23. The
  onboarding/port-strip logic is byte-identical to the on-device-verified `848ce20c` build
  (only the auth identity differs), and the AuthInterceptor crypto round-trip is unit-proven
  above, so the cold-start risk class is covered at the unit level.

## §6.Z honest caveat — cloud-search auth lag (operator-acknowledged)

The client's cloud-API option points at `LAVA_DEFAULT_CLOUD_API` (`https://lava.app:7777`).
The cloud server's allowlist (`lava-api-go` `LAVA_AUTH_ACTIVE_CLIENTS`) is updated by syncing
the rotated `.env` to `thinker.local` via `scripts/distribute-api-remote.sh` (RELEASE-ROTATION
step 7). **`thinker.local` is unreachable from this session**, so `android-1.3.0-1057` is NOT
yet registered server-side → picking the **cloud API** in onboarding will return **401** until
the operator runs step 7. The **on-device client↔api-app linking** (loopback + per-endpoint
key — the feature under test) is UNAFFECTED. The operator explicitly accepted shipping now and
iterating ("any issues we spot we will be fixing"). Closing action owed: run
`scripts/distribute-api-remote.sh` when `thinker.local` is reachable.
