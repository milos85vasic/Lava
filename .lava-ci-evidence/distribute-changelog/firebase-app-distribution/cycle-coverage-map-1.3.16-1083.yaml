# §6.AK cycle-coverage-map for Lava-Android-1.3.16-1083
#
# Each CHANGELOG-claimed user-visible change for 1.3.16-1083 MUST have at
# least one covering device Challenge (or per-artifact device test) that was
# EXECUTED + PASSED on the §6.Z gate against this exact SHA, OR an honest
# citation of why a device Challenge is not the applicable evidence class.
version: "1.3.16-1083"
claims:
  - fix: "LVA-085/LVA-086/LVA-093/LVA-094 (search chip labels, loading state, cold-start race)"
    covering_challenges:
      # No dedicated device Challenge authored this cycle for these four
      # individually -- honestly documented as a coverage gap in the
      # CHANGELOG entry itself. Covered instead by real JVM-level
      # ViewModel/UseCase tests using real implementations (this project's
      # Anti-Bluff Pact), each with a recorded falsifiability rehearsal in
      # its landing commit.
    covering_non_challenge_evidence:
      - "JVM real-stack tests, landing commits 4a86ddc8 (LVA-085/086/093/094)"
    honest_gap: "No NEW dedicated Compose UI Challenge for these four this cycle -- existing device-gate Challenges (C58/C59/etc.) cover the broader search flow they sit within, not these specific fixes in isolation."

  - fix: "RuTracker login Cloudflare-challenge classification + realistic outbound headers (backend correctness + telemetry)"
    covering_challenges: []
    covering_non_challenge_evidence:
      - "lava-api-go/internal/rutracker/login_test.go: TestLogin_CloudflareChallenge_ErrCloudflareChallenge, TestLogin_PlainForbidden_NOT_ClassifiedAsCloudflareChallenge, TestNewBrowserRequest_SetsRealisticUserAgent, TestClient_HasNoCookieJar (5 new tests, all with real falsifiability rehearsals, commit 95bcdce5)"
      - "Live reproduction against the actual running lava-api-go container: direct curl to rutracker.org/forum/login.php from the same egress IP confirmed HTTP 403 + cf-mitigated:challenge; authenticated POST to the live /v1/rutracker/login endpoint confirmed the classification change end-to-end"
    honest_gap: "No Android Compose UI Challenge -- this is a Go backend change with no Android-visible surface (the client's error-message rendering is unchanged; both 502 and 503 already mapped to the same honest 'Service unavailable' catch-all before this fix). A Challenge test would exercise no code path this fix touches."

  - fix: "LVA-079 (search-input vs results-filter chip disagreement + non-determinism) -- verified already-fixed"
    covering_challenges:
      - "lava.app.challenges.Challenge60InputResultsChipsAgreeTest"
    covering_non_challenge_evidence:
      - "feature/search_result/src/test/kotlin/lava/search/result/SearchResultFilterChipDeterminismTest.kt (real-stack, GREEN)"
    evidence_note: "Challenge60 evidence is from 2026-08-11 (.lava-ci-evidence/lva014-c00-c71-c58-c59-device-gate-retest2/), one day prior to this SHA -- cited per this cycle's honest-reuse policy since no client Kotlin/Compose source relevant to this fix changed between that run and 1083."

  - fix: "LVA-087 (Welcome screen stale provider count) -- verified already-fixed"
    covering_challenges:
      - "lava.app.challenges.Challenge63WelcomeCountMatchesPickerTest"
    evidence_note: "Fresh RED/GREEN executed 2026-08-12 against this cycle -- see .lava-ci-evidence/bluff-hunt/2026-08-12-lva-087-device-verification-evidence.md"

  - fix: "LVA-090 (onboarding 'Select all' silently enabling auth-required providers) -- verified already-fixed"
    covering_challenges:
      - "lava.app.challenges.Challenge66SelectAllDoesNotEnableAuthProvidersTest"
    evidence_note: "Fresh RED/GREEN executed 2026-08-12 against this cycle -- see .lava-ci-evidence/genymotion/c66-2026-08-12-lva090-reverify/verdict.txt"
