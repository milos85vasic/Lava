# §6.AK cycle-coverage-map for Lava-Android-1.3.16-1083
#
# Each CHANGELOG-claimed user-visible change for 1.3.16-1083 MUST have at
# least one covering device Challenge (or per-artifact test, per §6.AK's
# own "device Challenge OR per-artifact device test" wording) that was
# EXECUTED + PASSED and is present in the same-cycle §6.Z evidence file.
#
# Schema note: each claim's covering_challenges list items MUST be bare
# FQN/test-name strings only (no free prose as list items -- the mechanical
# scanner in scripts/check-cycle-coverage.sh treats every quoted list item
# under a claim as a name to verify, regardless of nesting).
version: "1.3.16-1083"
claims:
  - fix: "LVA-085/LVA-086/LVA-093/LVA-094 (search chip labels, loading state, cold-start race)"
    covering_challenges:
      - "SearchResultViewModelStreamingTest"
      - "SearchResultViewModelLoadingEmptyStateTest"

  - fix: "RuTracker login Cloudflare-challenge classification + realistic outbound headers"
    covering_challenges:
      - "TestLogin_CloudflareChallenge_ErrCloudflareChallenge"
      - "TestLogin_PlainForbidden_NOT_ClassifiedAsCloudflareChallenge"
      - "TestNewBrowserRequest_SetsRealisticUserAgent"
      - "TestClient_HasNoCookieJar"

  - fix: "LVA-079 (search-input vs results-filter chip disagreement + non-determinism) -- verified already-fixed"
    covering_challenges:
      - "Challenge60InputResultsChipsAgreeTest"

  - fix: "LVA-087 (Welcome screen stale provider count) -- verified already-fixed"
    covering_challenges:
      - "Challenge63WelcomeCountMatchesPickerTest"

  - fix: "LVA-090 (onboarding Select-all silently enabling auth-required providers) -- verified already-fixed"
    covering_challenges:
      - "Challenge66SelectAllDoesNotEnableAuthProvidersTest"
