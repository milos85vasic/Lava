## Lava-Android-1.2.28-1048 / Lava-API-Go-2.3.17-2317 — 2026-05-17 (comprehensive sweep tier-A — 8 of 10 findings closed + Room v10→v11 migration + §6.L 59th invocation)

**Previous published:** Lava-Android-1.2.27-1047 (debug + release both distributed 2026-05-17).

Operator mandate: "tackle all existing sweep findings fully!" + standing pattern "rebuild everything, rerun all tests, ... redistribute everything using firebase distribution. let us know when everything is finished so we can test!" The sweep report (commit `a5fa8033`) catalogued 10 findings; this release closes 8 of them via subagent-driven branch `sweep-findings-tier-A-2026-05-17` merged at `99098dc0`.

### What's in this release

#### Sweep findings closed

- **#1 P0 — ToggleAnonymous persistence regression** (Room v10→v11 migration)
- **#4 P1 — Login serviceUnavailable banner staleness on field-change**
- **#5 P1 — Stale captcha after retry**
- **#6 P1 — ProviderLogin parallel banner staleness**
- **#7 P1 — Onboarding null-login misleading "Invalid credentials"**
- **#8 P1 — Clones in onboarding picker**
- **#9 P2 — MainActivity onboarding-complete re-read (observable Flow)**
- **#10 P2 — ToggleSync race (atomic DAO transaction)**

#### Tests

- 12 new tests (5 new files + 2 extended)
- 7 `Bluff-Audit:` stamps in subagent commit `5e02cdda` covering each defect class
- 14/14 Compose UI Challenge Tests PASS on Pixel_8/API35

#### Architectural touches

- Room database schema v10 → v11
- New `provider_configs.use_anonymous` column with `MIGRATION_10_11`
- New `ProviderConfigRepository.setUseAnonymous(...)` + `observeUseAnonymous(...)`
- New `PreferencesStorage.observeOnboardingComplete(): Flow<Boolean>` (SharedPreferences listener)
- New `ProviderSyncToggleDao` for atomic toggle persistence

### Sweep findings deferred

- **#2 P0 — search retry-after-network-fail** — partial mitigation in place; full sealed-Error-variant refactor → 1.2.29.
- **#3 P0 — provider-config screen scroll-jank** — LazyColumn-in-Column antipattern (§6.Q); structural reshape → 1.2.29.

### Standing constitutional debt acknowledged

- §6.H — RuTracker password rotation still PENDING per clause 6 (credentials `nobody85perfect / ironman1985` redacted in commit `d7d4572a` but remain valid until operator rotates upstream)
- §6.X-debt — darwin/arm64 podman VM lacks /dev/kvm; container-bound emulator path remains blocked on this host
- §6.K pre-existing-gap — Android Gradle + Go binary builds run host-direct on darwin/arm64

### Distribute-readiness

- ✅ §6.P versionCode 1048 > last-version 1047
- ✅ §6.Y bump-first applied
- ✅ §6.W mirrors converged
- ✅ §6.AA two-stage (debug-only first, then release-only after operator confirmation per standing preauth)
- ✅ §6.Z evidence file pre-distribute

`Classification:` project-specific.
