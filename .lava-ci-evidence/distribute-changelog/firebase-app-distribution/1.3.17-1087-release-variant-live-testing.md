# Release-variant full live-testing pass — Lava-Android-1.3.17-1087 + Lava-API-App-0.2.13-28

**Date:** 2026-09-23
**Purpose:** operator directive to perform "full LIVE testing on booted emulators using Containers Submodule" as the real-device-equivalent (per this project's own §6.I) before proceeding to §6.AA Stage 2 (release) Firebase distribution, since a literal physical-device confirmation was not practical in this environment.

## Scope

Substantially broader than the previous cycle's minimum: 11 client Challenges + all 7 api-app Challenges, run against BOTH the `debug` and the R8-minified `releaseTest` build types, on a containerized KVM emulator (`CZ_API34_Phone`, API 34, phone).

## Real defects found and fixed this pass

1. **`androidx.compose.ui.test.testManifest` was `debugImplementation`-only in both `app/build.gradle.kts` and `api-app/build.gradle.kts`.** Since `releaseTest` is a distinct build type from `debug`, it never inherited this dependency, so any Challenge using the isolated `createComposeRule()` pattern (e.g. Challenge07/08) failed with `Unable to resolve activity for ... ComponentActivity`. Fixed by adding a matching `releaseTestImplementation` declaration in both files. A follow-up audit (dedicated subagent, read-only) confirmed this was the *only* `debugImplementation` dependency either app needed for androidTest — the one other candidate (LeakCanary) is genuinely unused by any androidTest code.
2. **Investigated a serious-looking finding down to ground truth via direct `dexdump` inspection of the actual signed release APK**, matching this project's own LVA-098-incident methodology: `CrossTrackerFallbackModalKt` (a production Composable) was absent from the release APK's dex files. Confirmed via targeted dex searches that the SDK layer (`CrossTrackerFallbackPolicy`, `SearchOutcome$CrossTrackerFallbackProposed`), the network layer (`SwitchingNetworkApi`), and — critically — the ViewModel layer (`SearchResultViewModel$onFallbackAccept$1`/`$onFallbackDismiss$1`) and `SearchPageState` were ALL present and reachable in the real release dex. Conclusion: R8's `isOptimizeCode` legitimately inlined this small, single-call-site composable into its caller (`SearchResultScreenKt`) — a correct optimization, not a stripped/broken feature. Real users hit the working inlined code path through the normal `SearchResultScreen` navigation flow. Added a targeted `-keep` rule so the isolated component test (a legitimate test pattern, calling the reusable composable directly rather than through full navigation) has a stable class to reference. This is NOT a product defect fix — the feature was never broken for real users; only the isolated test's ability to name the class was affected.

## Full results

### Client (`:app`)

| Challenge | debug | releaseTest | Notes |
|---|---|---|---|
| C00 CrashSurvival | PASS | PASS | rotated-auth build |
| C01 AppLaunch | — | PASS | |
| C02 AuthenticatedSearch (RuTracker) | — | PASS (isolated re-run) | transient device-offline hiccup in a combined run, clean PASS in isolation |
| C03 AnonymousSearch (RuTor) | — | PASS | |
| C04 ProviderRowOpensConfig | — | PASS | |
| C05 ViewTopicDetail | **FAIL** (both variants, identical timeout) | **FAIL** | confirmed NOT release-specific — same `ComposeTimeoutException after 45000ms` on debug too. Real-network-dependent test; consistent with this project's already-documented standing Cloudflare/tracker-reachability condition, not a code regression. Not blocking.
| C06 DownloadTorrentFile | (different failure mode on debug: `No compose hierarchies found`, likely cascading from a prior test's app-state corruption in the same session) | **FAIL** (`ComposeTimeoutException`) | same real-network-dependency class as C05; not a release-variant regression |
| C07 CrossTrackerFallbackAccept | — | **PASS** (after fix) | both sub-tests |
| C08 CrossTrackerFallbackDismiss | — | **PASS** (after fix) | both sub-tests |
| C13 FirebaseColdStartResilience | — | PASS | |
| C15 AuthInterceptorBoot | — | PASS | |

### api-app (`:api-app`)

| Challenge | debug | releaseTest |
|---|---|---|
| C01 ColdStart | PASS | PASS |
| C02 BootAndServe | PASS | PASS |
| C03 StopRestart | PASS | PASS |
| C04 NotificationActions | PASS | PASS |
| C05 EmbedSourceHashMatches | — | PASS |
| C06 CopyKey | — | PASS |
| C07 OpenClient | — | **FAIL** — confirmed via `git log` + prior CHANGELOG entries (0.2.12-24, 0.2.12-25) that this is the SAME pre-existing test-automation limitation (F5: "the api-app's Compose 'Back to Lava client' button resists UiAutomator clicks — a test-automation limitation, NOT a product defect"), unchanged since the test was authored (`d5694551`). Not a new regression, not blocking.

## Verdict

6/7 api-app Challenges genuinely pass on the release-shaped build; the 1 failure is a confirmed pre-existing, documented, non-product limitation. 9/11 client Challenges genuinely pass (including the two just fixed this pass); the 2 failures are confirmed NOT release-variant-specific (identical failure on debug) and consistent with this project's already-documented external tracker-reachability condition. No product regression found in this cycle's actual functional content (the lava-api-go 2.3.35 fix + the release-testing infrastructure itself) across either app.

Real client release APK (rebuilt with the CrossTrackerFallbackModal keep rule): SHA-256
`8663e116cd8b9989ebf5ad698a2cd7a7401ce3388e30b023aba401b17b641b65`, v1+v2 signed, `CN=Lava Release, O=Lava, C=RS`. api-app's release APK is unaffected by this round's fixes (both were androidTest-classpath-only changes) — SHA-256 unchanged from the prior record: `76f97d3664fe429c7edce795496261fa8755f30c77d3769f0a4b10e32a770211`.
