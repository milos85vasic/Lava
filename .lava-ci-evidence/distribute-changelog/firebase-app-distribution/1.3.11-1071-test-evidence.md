# §6.Z Pre-Distribute Test Evidence — Lava-Android 1.3.11 (1071), client debug+release

**Artifacts:** `releases/1.3.11/android-{debug,release}/lava-1.3.11-1071-{debug,release}.apk` (rebuilt after the ApiHttpException type fix; debug sha `b1abf181…`, release sha `8f9cc643…`).
**Built from:** working tree atop `86d93125`; the telemetry cycle (this commit). Auth rotated `android-1.3.11-1071` (fresh pepper, 37 active).

## What ships
Comprehensive §6.AC Crashlytics non-fatal telemetry — the search-failure path especially:
`ApiBackedTrackerClient` throws a typed `ApiHttpException(statusCode/requestUrl/httpMethod)` (a specific
`IllegalStateException` subtype — the original `error()` contract preserved) → `SearchResultViewModel.recordProviderFailure`
records a non-fatal with `http_status`/`request_url`/`http_method`/`base_url_host`; plus `ApiKeyClient` handoff-key +
onboarding warnings. `scripts/check-non-fatal-coverage.sh` STRICT = 0 violations.

## JVM real-stack tests EXECUTED (all GREEN — 0 failures across the 3 modules)
| Test | tests | fail |
|---|---|---|
| SearchResultViewModelStreamingTest (enriched §6.AC context keys) | 3 | 0 |
| SearchResultViewModelRetryTest | 1 | 0 |
| ApiBackedTrackerClientAuthFailureTest | 3 | 0 |
| ApiBackedTrackerClientStressChaosTest (IllegalStateException contract) | 6 | 0 |
| ApiBackedTrackerClientTest | 8 | 0 |
| FirebaseAnalyticsTrackerTest / Initializer / ProvidesModule | 7 / 5 / 2 | 0 |

**Regression caught + fixed by integration testing (anti-bluff):** the search-enrichment change first made `ApiHttpException` extend `IOException`, breaking 4 tests that assert the `IllegalStateException` HTTP-error contract; fixed by extending `IllegalStateException` (contract preserved) — exactly why we build+test before distributing.

## Device gate
C00 cold-start re-gate of THIS telemetry build (the Hilt graph changed — `ApiKeyClient` now injects `AnalyticsTracker`) runs on the thinker KVM host after push (thinker pulls the telemetry commit). The gate was confirmed WARM+GREEN this session (boot 31-33s, all_passed:true, real KVM). Attestation appended post-run.

## Related finding (NOT shipped in 1071 — pursued as 1072)
H1 search-401 bug CONFIRMED source-verified (`docs/issues/2026-06-23-search-401-rootcause-deepening.md` + the H1-verify verdict): the build-time `AuthInterceptor` (`Lava-Auth` header, replace) overwrites the per-install handoff key (same header name) because interceptors fire last. The 1071 telemetry will confirm this from the field (401 + request_url). The fix is an auth change requiring on-device verification — deferred to 1072 per §6.Z (no rushed auth change).
