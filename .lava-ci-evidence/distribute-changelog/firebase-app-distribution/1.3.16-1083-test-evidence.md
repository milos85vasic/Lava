# §6.Z Pre-Distribute Test-Execution Evidence — Lava-Android-1.3.16-1083

**Channel:** firebase-app-distribution (client)
**Artifact under test:** `digital.vasic.lava.client.dev` versionName 1.3.16 versionCode 1083
**Commit SHA:** 7344327d9cad17c8c7d8827bc8de6fa83bcc710b
**Evidence authored:** 2026-08-13
**combined-distribute-authorization:** n/a — §6.AA two-stage: this evidence backs Stage 1 (debug-only) only.

## Scope

This SHA's code-touching content over the prior distributed version (1082):
commit `4a86ddc8` (LVA-085/086/093/094, already covered by 1083's own earlier
CHANGELOG entry + JVM-level evidence) plus this cycle's genuinely new
production change, commit `95bcdce5` (RuTracker Cloudflare-challenge
classification fix, Go backend only, no Android source touched) plus 3
already-verified-already-fixed re-confirmations (LVA-079/087/090, no
production code change).

## Device gate

Runner: `containers-submodule` (host-direct `connectedDebugAndroidTest`
against an already-live Containers-orchestrated emulator; the AVD's ORIGIN
was Containers-orchestrated cold-boot, reused across this session's several
Challenge runs per §6.T.2 resource discipline rather than re-booting for
every single test) · AVD: `sdk_gphone64_x86_64 - 14` (API 34) · Gate host:
Linux x86_64, KVM.

| Test | Result | Timestamp |
|---|---|---|
| Challenge00CrashSurvivalTest | **PASS** (26.9s) | 2026-08-13T08:20:46 |
| Challenge60InputResultsChipsAgreeTest | **PASS** (cited, 2026-08-11 run — see note) | 2026-08-11 |
| Challenge63WelcomeCountMatchesPickerTest | **PASS** (RED/GREEN rehearsal) | 2026-08-12T23:19 |
| Challenge66SelectAllDoesNotEnableAuthProvidersTest | **PASS** (RED/GREEN rehearsal) | 2026-08-12T21:26 |

**Note on Challenge60's citation:** this specific test's freshest EXECUTED
evidence is from 2026-08-11 (`.lava-ci-evidence/lva014-c00-c71-c58-c59-device-gate-retest2/`),
one day prior to this SHA. Per this cycle's honest-reuse policy (§6.T.2 —
no wasted host resources re-running an unchanged code path), it is cited
rather than re-executed: `git log` confirms no commit between that evidence's
SHA and 7344327d touched `SearchPageState.kt`, `SearchResultScreen.kt`, or
`SearchInputViewModel.kt` (the files this test exercises).

## RuTracker backend fix — non-Challenge evidence

This fix has no Android-visible surface (see cycle-coverage-map for the
honest_gap citation). Covering evidence: `lava-api-go/internal/rutracker/login_test.go`
(5 new tests, real falsifiability rehearsals, commit `95bcdce5`) plus a live
reproduction against the actual running backend container (direct curl to
rutracker.org confirming the exact Cloudflare-challenge response, and an
authenticated POST to the live `/v1/rutracker/login` endpoint confirming the
classification end-to-end).

## Artifact identity

APK: `app/build/outputs/apk/debug/app-debug.apk`, rebuilt at this exact
commit SHA. Verified via `aapt dump badging`:

```
package: name='digital.vasic.lava.client.dev' versionCode='1083' versionName='1.3.16'
```

## What this does NOT cover

The full §6.AE.2 minimum matrix (API 28/30/34/latest × phone+tablet) was not
run this cycle — only API 34/phone, matching every prior distribute on this
host. Pre-existing, honestly-documented scope limitation, not new to 1083.
