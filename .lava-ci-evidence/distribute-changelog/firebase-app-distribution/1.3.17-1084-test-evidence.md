# §6.Z Pre-Distribute Test-Execution Evidence — Lava-Android-1.3.17-1084

**Channel:** firebase-app-distribution (client)
**Artifact under test:** `digital.vasic.lava.client` / `.client.dev` versionName 1.3.17 versionCode 1084
**Fix commits:** `843f8107` (SearchResultViewModel composition fix), `8634efd9` (SearchInputViewModel composition fix)
**Evidence authored:** 2026-08-13

## Scope

This cycle's only production-code change over 1083: the LVA-085 x LVA-093
composition-race fix, applied in two files (`SearchResultViewModel.kt`,
`SearchInputViewModel.kt`). Both are reordering fixes only — no new feature
surface, no other files touched. LVA-094 and the RuTracker Cloudflare
classification fix were re-audited this cycle (re-ran their existing tests)
but received NO code changes — no new coverage needed for them beyond
re-confirming their existing passing tests.

## Device gate — containerized KVM emulator (§6.X-conformant, `runner=containerized`)

Runner: `containers-submodule` · AVD: `CZ_API34_Phone` (API 34, phone) ·
Gate host: Linux x86_64 (nezha), KVM · Cold-boot, non-host-direct, gating=true.

| Test | Result | Boot | Test duration | Timestamp |
|---|---|---|---|---|
| Challenge61ResultsChipsShowDisplayNamesTest | **PASS** | 94.6s | 421.4s | 2026-08-13T17:31:03+02:00 |
| Challenge59SearchUsesOnboardedProvidersTest | **PASS** | 101.5s | 252.7s | 2026-08-13T17:41:49+02:00 |

Raw attestations:
- `.lava-ci-evidence/lva-085x093-composition-fix-1084/real-device-verification.json`
- `.lava-ci-evidence/lva-085x093-composition-fix-1084-searchinput/real-device-verification.json`

**Build-consistency note (honest, per §6.J):** the Challenge61 run's APK was
built from a working tree that already had fix #1 (`843f8107`, committed
before the run started) but the fix #2 edits were made to the working tree
concurrently with that background build. Fix #2 touches only
`SearchInputViewModel.kt`, a file Challenge61 (search-RESULTS screen) does
not exercise, so this does not affect Challenge61's validity for what it
actually tests. Fix #2 itself is separately, cleanly device-verified by the
Challenge59 run above, which started AFTER both fixes were committed to a
clean working tree (`git status` confirmed clean of both source files at
that point) — this is the unambiguous, fully-consistent verification for
fix #2.

## JVM-level coverage (fresh, this SHA)

```
SearchResultViewModelStreamingTest:              tests="8"  failures="0" errors="0" (2026-08-13T15:16:35)
SearchInputViewModelTest:                         tests="13" failures="0" errors="0" (2026-08-13T15:32:25)
RepopulateProvidersOnStartupUseCaseTest:          tests="12" failures="0" errors="0" (2026-08-13T15:16:58)
TestLogin_CloudflareChallenge_ErrCloudflareChallenge:        PASS (re-run, no code change)
TestLogin_PlainForbidden_NOT_ClassifiedAsCloudflareChallenge: PASS (re-run, no code change)
```

Both new regression tests (`streaming search resolves the correct display
name only after the cold-start registry catches up` in
`SearchResultViewModelStreamingTest`, and `onCreate_resolves_the_chip_display_name_only_after_the_cold_start_registry_catches_up`
in `SearchInputViewModelTest`) carry a performed falsifiability rehearsal —
see the Bluff-Audit stamps in commits `843f8107` and `8634efd9` for the
exact mutation + observed failure + revert for each.

## Full-suite regression check

`:feature:search_result:testDebugUnitTest :feature:search_input:testDebugUnitTest :core:domain:testDebugUnitTest`
— BUILD SUCCESSFUL, 0 failures, no regressions from the reordering in either
module.
