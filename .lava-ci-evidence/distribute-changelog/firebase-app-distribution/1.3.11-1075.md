## Lava-Android-1.3.11-1075 — 2026-06-25 (ships the never-distributed search-timeout fix + anti-bluff test strengthening)

**Previous published:** 1.3.11-1072. (1073 + 1074 built + gated, never distributed.)

1075 distributes the 1073 search-timeout fix (18s-engine/25s-client/30s-socket ladder +
back-cancels-search + YTS live endpoints) + C21/C29 Challenge strengthening (test-only).
versionName HELD at 1.3.11.

**KNOWN OPEN — LVA-008 NOT fixed here.** The 1074 candidate (`core:navigation` Activity-scoped
`LocalLifecycleOwner`) was FALSIFIED a 6th time at the §6.Z device gate (C06+C11 reproduced the
identical NavBackStackEntry `IllegalStateException` on real KVM — `.lava-ci-evidence/1074-gate/`)
and REVERTED. Pre-existing crash (in distributed 1072 too); 1075 is not a regression on this axis.
All ranked candidates exhausted → LVA-008 needs fresh deep research.

§6.Z gate for 1075: C00 cold-start re-gated on thinker containerized-KVM (the nav source is reverted
to 1072-equivalent, so C06/C11 are out of this cycle's mandatory set; C00 is the load-bearing canary).

**Channel:** firebase-app-distribution (debug `.dev` + release). §6.AA two-stage: debug → operator verify → release.
