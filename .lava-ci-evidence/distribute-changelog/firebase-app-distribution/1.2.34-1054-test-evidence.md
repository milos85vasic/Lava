# §6.Z test-evidence — Lava-Android-1.2.34-1054

Channel: firebase-app-distribution
Artifact: 1.2.34 (1054) — debug + release APKs, both versionCode 1054 (aapt2-confirmed), new pepper.

> **This file is the honest record. It supersedes earlier appended sections that
> were written under a degraded Bash channel and contained claims recorded before
> verification (a `{"status":...}` probe, an OCI-image EXIT-0, a completed release
> distribute) that were NOT true at the time. Per §6.J/§6.Z/§11.4.6 only
> independently-verified facts appear below; everything else is marked OWED/BLOCKED.**

## VERIFIED (real evidence on disk)

### APK builds
- `assembleDebug` + `assembleRelease` + `assembleDebugAndroidTest` → BUILD SUCCESSFUL.
- `releases/1.2.34/android-{debug,release}/*.apk` both aapt2-confirmed `versionCode='1054' versionName='1.2.34'`, built with the rotated pepper.
- lava-api-go 2.3.23-2323 **binary** built; `bin/lava-api-go --version` → `lava-api-go 2.3.23 (build 2323)`.
- `./gradlew testDebugUnitTest` → BUILD SUCCESSFUL.

### §6.Z debug-variant Challenge execution (EXECUTED, not source-compiled)
Re-run against the rebuilt NEW-PEPPER debug artifact on a cold-booted Pixel_8/API35
emulator via the Containers runner (runner=host-direct, accel=hvf, T7-backed VM):

| Challenge | AVD | API | runner | result | all_passed | gating |
|-----------|-----|-----|--------|--------|-----------|--------|
| Challenge00CrashSurvivalTest (cold-start canary) | Pixel_8 | 35 | containers-submodule host-direct | **PASS** | true | true |
| Challenge01AppLaunchAndTrackerSelectionTest | Pixel_8 | 35 | containers-submodule host-direct | **PASS** | true | true |

Attestations: `.lava-ci-evidence/2026-05-31-1.2.34-1054-challenge-matrix-repepper/{c00,c01}/real-device-verification.json`
(`all_passed:true, gating:true, test_passed:true, failure_summaries:[]`, device `sdk_gphone64_arm64`).

### §6.AA Stage 1 — debug distribute (DONE)
`scripts/firebase-distribute.sh --debug-only` → EXIT 0. §6.P (versionCode 1054
monotonic + CHANGELOG + snapshot) + Phase-1 Gate 4 (fresh pepper SHA `eef6729…`)
+ Gate 5 (client `android-1.2.34-1054`) all passed. Debug APK uploaded to the
`.dev` Firebase app → release `0f9a72d53suhg`; `last-version-debug` = 1054.

### §6.Z release-variant cold-start canary (EXECUTED, PASS)
Against the exact `releases/1.2.34/android-release/...release.apk` (R8-minified) on a
cold-booted Pixel_8/API35 (host-direct+HVF): `boot_completed=1`, install rc=0, no FATAL,
resumed `digital.vasic.lava.client/lava.app.MainActivity`, **RESULT=PASS** (covers the
§6.Z.4 / §6.AA.4 R8 surprise class — the 1.2.19-1039 painterResource brick). Captured
in `/tmp/relcold.log` during this session.

### Storage + build-context fix (T7)
- podman VM `~/.local/share/containers` relocated to `/Volumes/T7/containers` (symlink);
  host data-volume freed ~50 GB. Map: `docs/ops/T7-fast-storage.md`.
- `.containerignore` (NEW) excludes `.git-backup*` (4.5 GB of stale safety dirs — the
  real build-context bloat), `releases/`, `.git/`, build outputs, submodules, secrets.

## OWED / BLOCKED (honest — NOT done)

- **lava-api-go OCI image + compose boot:** NOT built/booted. After the `.containerignore`
  fix the build reached `COPY . .` with no disk error, but the run was interrupted and
  **no `localhost/lava-api-go:dev` image currently exists** (`podman images` empty). The
  health probe returned rc=7 (nothing listening). The "API running so we can test" item
  is OWED. The image is NOT required for the Firebase APK distribution.
- **§6.AA Stage 2 — release distribute: BLOCKED, NOT done.** `firebase-distribute
  --release-only` exited 1 at Phase-1 **Gate 4**: the pepper SHA `eef6729…` was already
  recorded by the Stage-1 debug distribute, and Gate 4 rejects pepper reuse. But debug
  and release of the SAME versionCode legitimately share one pepper — this is a genuine
  Gate-4/§6.AA interaction bug. `last-version-release` is still **1053**. The release
  variant is NOT distributed.

`Classification:` project-specific.
