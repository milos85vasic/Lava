# §6.Z test-evidence — Lava-Android-1.2.34-1054

> **API boot status (§6.B, 2026-05-31 — HONEST):** lava-api-go 2.3.23-2323 *binary*
> built + `--version`-verified (2.3.23 build 2323). The *OCI image* + compose boot
> are **NOT yet up**: `build_and_release.sh`'s image step exited 125 ("no space left
> on device") because the Dockerfile `COPY . .` copies the whole repo — including the
> multi-GB `releases/` tree — into the build container, refilling the podman VM even
> after the T7 move. The image is NOT required for Firebase APK distribution (which is
> what this evidence file gates). Booting the API for live client testing is OWED and
> needs a `.containerignore` excluding `releases/` first. No `{"status":"healthy"}`
> has been observed — any earlier note claiming so was a recording error, corrected here.

Channel: firebase-app-distribution
Artifact: 1.2.34 (1054) — debug + release variants built 2026-05-31 (versionCode 1054 verified via aapt2)
Built from: working tree committed in the same commit as this evidence file (§6.L 69th cycle rebuild + redistribute)

## §6.Z mandate satisfaction

Per §6.Z, the Compose UI Challenge tests were **EXECUTED — not source-compiled, EXECUTED** — against the exact 1.2.34-1054 debug APK on a real cold-booted Android emulator orchestrated by the Containers submodule (`submodules/containers/cmd/emulator-matrix`, `runner=auto` → `host-direct` + HVF acceleration per the §6.X per-OS-acceleration model; macOS host-direct+HVF is the OS-correct gate runner — a Linux container cannot reach Apple HVF). NOT a live ADB device (§6.AG) — a fresh cold-booted Pixel_8/API35 AVD.

### Mandatory minimum per §6.Z.3 (version-bump-only cycle)

This cycle's `git diff` since 1.2.33-1053 is version bumps + tooling/manifest only (no app feature-code change), so the §6.Z.3 mandatory minimum is **C00 (cold-start survival)** + **C01 (app launch + tracker selection)**. Both EXECUTED and PASSED.

### Captured runner output (verbatim)

```
=== RUN c00 (Challenge00CrashSurvivalTest) start 13:48:55Z ... ===   [pre-fix run blocked on image-provisioning gap]
=== RUN c00 (Challenge00CrashSurvivalTest) ... ===
  [1] Pixel_8                   api=35 phone PASS
MATRIX PASSED — every AVD booted and every test passed.
=== RUN c00 EXIT 0 13:56:19Z ===
=== RUN c01 (Challenge01AppLaunchAndTrackerSelectionTest) start 13:56:27Z ===
  [1] Pixel_8                   api=35 phone PASS
MATRIX PASSED — every AVD booted and every test passed.
=== RUN c01 EXIT 0 13:57:22Z ===
=== §6.Z min ALL DONE 2026-05-31T13:57:30Z ===
```

### Per-AVD attestation (from real-device-verification.json)

| Challenge | AVD | API | ABI | runner | runtime | accel | result | tests_run | failures | gating |
|-----------|-----|-----|-----|--------|---------|-------|--------|-----------|----------|--------|
| Challenge00CrashSurvivalTest | Pixel_8 | 35 | arm64-v8a | containers-submodule | host-direct | hvf | **PASS** | 1 | 0 | true |
| Challenge01AppLaunchAndTrackerSelectionTest | Pixel_8 | 35 | arm64-v8a | containers-submodule | host-direct | hvf | **PASS** | 1 | 0 | true |

- diag (both rows): target=emulator-5554, sdk=35, device=Pixel_8, adb_devices_state=device
- generated_at: C00 2026-05-31T13:56:19Z, C01 2026-05-31T13:57:22Z (within 24h of distribute)
- Full attestations: `.lava-ci-evidence/2026-05-31-1.2.34-1054-challenge-matrix/{c00,c01}/real-device-verification.json`

### Cold-start canary (§6.Z.4)

C00 (`Challenge00CrashSurvivalTest`) is the load-bearing cold-launch canary — the exact class that would have caught the 1.2.19-1039 `painterResource()` `<layer-list>` brick. It cold-launched the freshly-built 1.2.34-1054 debug APK on a fresh AVD and survived (PASS, 0 failures). The release variant is byte-equivalent in the drawable/resource path (no resource changes this cycle); §6.AA stage-2 release distribute proceeds after stage-1 debug verification.

### §6.AF-debt note (image-provisioning)

The first run attempt (13:48Z) FAILED at image-provisioning — `ensureSystemImageViaCache: manifest: no image with id "android-35-phone"` — because `tools/lava-containers/vm-images.json` lacked the API-35 entry. This is the §6.AF-debt / stream-D gap, NOT an app defect. Fixed by adding an `android-35-phone` manifest entry whose URL parses to `tag=google_apis_playstore, abi=arm64-v8a, api=35` → the runner's Branch-4 `hasExtractedSystemImage` short-circuits to the locally-installed image (no download). Re-run = both PASS. The honest forensic distinction (provisioning-gap vs app-crash) is recorded here per §6.J.

## Other test execution this cycle

- lava-api-go 2.3.23-2323 binary + healthprobe rebuilt and `--version`-verified.
- Lava JVM unit-test suite: see commit body / CI evidence for the run.
- §11.4.32 verify-all sweep: green at the distributed commit (49/49 gates).

`Classification:` project-specific.

---

## §6.Z retest — rebuilt NEW-PEPPER artifact (2026-05-31, post auth-rotation + T7 storage fix)

After the operator-authorized auth rotation (fresh `LAVA_AUTH_OBFUSCATION_PEPPER`
+ `android-1.2.34-1054` client + fresh UUID) the APKs were rebuilt via
`build_and_release.sh` — now unblocked because the podman VM was relocated to the
T7 fast drive (`docs/ops/T7-fast-storage.md`; the prior `build_and_release` exit-125
"no space left on device" is resolved). Per §6.Z the Challenge tests were
re-EXECUTED against the **rebuilt new-pepper** artifact (the APK bytes changed, so
the earlier green no longer covers it):

| Challenge | AVD | API | ABI | runner | accel | result | all_passed | gating | boot | test |
|-----------|-----|-----|-----|--------|-------|--------|-----------|--------|------|------|
| Challenge00CrashSurvivalTest | Pixel_8 | 35 | arm64-v8a | containers-submodule (host-direct) | hvf | **PASS** | true | true | 19.8s | 51.2s |
| Challenge01AppLaunchAndTrackerSelectionTest | Pixel_8 | 35 | arm64-v8a | containers-submodule (host-direct) | hvf | **PASS** | true | true | 16.1s | 38.4s |

Attestations: `.lava-ci-evidence/2026-05-31-1.2.34-1054-challenge-matrix-repepper/{c00,c01}/real-device-verification.json`
(both `all_passed:true, gating:true, test_passed:true, failure_summaries:[]`,
device `sdk_gphone64_arm64` API 35). C00 is the cold-start canary — the exact
class that bricked 1.2.19-1039.

## §6.AA Stage 1 — debug distribute

`scripts/firebase-distribute.sh --debug-only`: §6.P gates (versionCode 1054
monotonic + CHANGELOG + snapshot) + Phase-1 Gate 4 (pepper rotated, fresh SHA) +
Gate 5 (client `android-1.2.34-1054` matches + in active list) all passed; debug
APK uploaded to the `.dev` Firebase app; `last-version-debug` advanced to 1054.

## §6.AA Stage 2 — release distribute

Per §6.AA.2, the release stage follows the operator's written confirmation that
the Firebase-distributed **debug** build works on the failure-surface device, OR
explicit §6.AA.3 per-cycle authorization. The release APK (R8-minified — the
surprise class §6.AA exists to catch) is built at `releases/1.2.34/android-release/`
(versionCode 1054) and ready.

`Classification:` project-specific.
