Lava 1.2.9 (1029) — Theme fix + anti-bluff onboarding wizard

This build fixes the dark-mode readability crash where text was invisible
because the Lava theme never wired MaterialTheme.colorScheme. All screens
now render readable text in both light and dark modes.

Onboarding wizard bugs fixed:
- Welcome screen now shows provider count
- Back press from Configure goes to Providers (per spec)
- Hardcoded status colors replaced with theme-aware accents
- Anonymous providers skip unnecessary auth check

New: 16 anti-bluff ViewModel tests + 3 onboarding Challenge Tests (C20-C22).

Go API bumped to 2.3.0.
