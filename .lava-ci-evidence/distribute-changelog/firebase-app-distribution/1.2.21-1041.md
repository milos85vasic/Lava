## Lava-Android-1.2.21-1041 / Lava-API-Go-2.3.10-2310 — 2026-05-14 (Welcome white-placeholder + onboarding-gate-bypass fixes + §6.AB Anti-Bluff Test-Suite Reinforcement)

**Previous published:** Lava-Android-1.2.20-1040 / Lava-API-Go-2.3.9-2309 (debug-only stage 1; release stage 2 never proceeded — 1.2.20 surfaced two non-crashing defects on the operator's Galaxy S23 Ultra)

**Compensating distribute** — 1.2.20 release stage 2 was never proceeded because of the bugs surfaced. Per §6.AA, this 1.2.21 cycle goes debug-only first, then release after operator verification.

### Fixed (Android client) — both passed all existing tests on 1.2.20-1040

- **Welcome screen brand mark renders in full color (was white placeholder).** `WelcomeStep` now uses `androidx.compose.foundation.Image(painter = painterResource(R.drawable.ic_lava_logo))` instead of `Icon(icon = LavaIcons.AppIcon)` (Material3 `Icon` applies `LocalContentColor` tint, designed for monochrome glyphs only).

- **Onboarding gate enforced — back-from-Welcome closes app, cannot reach home without ≥1 probed provider.** New `OnboardingSideEffect.ExitApp`. `MainActivity` calls `finishAffinity()` on ExitApp. `onFinish()` validates ≥1 provider has `configured && tested` before posting Finish; otherwise re-enters Configure with error message.

### Constitutional — §6.AB added (27th §6.L invocation)

Anti-Bluff Test-Suite Reinforcement: per-feature anti-bluff completeness checklist + defect-driven bluff-hunt cadence escalation + discrimination test mandatory per Challenge Test. Forensic anchor: 1.2.20-1040 white-icon + gate-bypass — both passed all existing tests but failed for the user. Operator: "all existing tests and Challenges do work in anti-bluff manner — they MUST confirm that all tested codebase really works as expected!"

Propagated to root CLAUDE.md / AGENTS.md / lava-api-go × 3 docs / 16 submodules × 3 docs = 53 docs total. §6.AB-debt deferred.

### §6.Z test-evidence (this distribute)

See companion file `1.2.21-1041-test-evidence.md`. Summary:
- JVM unit suite (OnboardingViewModelTest 9/9 + LavaIconsAppIconColorRegressionTest 4/4) — verbatim gradle BUILD SUCCESSFUL captured
- Falsifiability rehearsals: revert ExitApp→Finish in onBackStep → AssertionError on `back step from Welcome emits ExitApp side effect` (caught); restore → pass
- Instrumentation: operator-on-real-device verification on Samsung Galaxy S23 Ultra (operator-authorized substitute for containerized emulator on this darwin/arm64 host per §6.X-debt incident JSON)

### Distribute readiness

- ✅ §6.P gates: versionCode 1041 > last-version 1040; CHANGELOG entry; per-version snapshot
- ✅ §6.Y bump-first: this commit started with versionCode + versionName bump
- ⏳ §6.Z evidence: JVM unit-test section complete; instrumentation section pending operator real-device verification
- ⏳ §6.AA: stage 1 debug-only first; release stage 2 only after operator verifies stage 1
- ✅ §6.W mirrors converged at this commit's SHA
- ✅ §6.AB: 2 new VM tests cover gate behavior with falsifiability rehearsal; 3 new Challenge Tests cover the user-visible flows; structural test extended to assert WelcomeStep uses Image()

### Operator-input checklist

- Stage 1: I run `firebase-distribute.sh --debug-only` after this commit lands.
- Operator installs Firebase-distributed debug APK on Samsung Galaxy S23 Ultra.
- Operator confirms:
  - ✅ No crash on cold launch (the §6.Z fix continues to hold)
  - ✅ Welcome screen renders the **colored red Lava logo** (NOT the white placeholder) — this verifies the §6.AB white-placeholder fix
  - ✅ Pressing back from the Welcome screen closes the app entirely
  - ✅ Re-launching the app re-enters onboarding (because no provider was configured)
  - ✅ Tapping through onboarding without configuring a provider does NOT escape to the home screen
- Operator replies with one-line confirmation; agent appends to evidence file.
- Stage 2: I run `firebase-distribute.sh --release-only` after operator confirmation.

### What's NOT in this version

- §6.X-debt remains open
- §6.Y-debt + §6.Z-debt + §6.AA-debt + new §6.AB-debt all open — operator + reviewer enforced until next phase touching `scripts/firebase-distribute.sh` + `.githooks/pre-push` + `scripts/check-constitution.sh` closes them
- HelixConstitution submodule incorporation deferred to next cycle (per the priority decision earlier in this session)
