# §6.Z test-evidence — Lava-Android-1.2.20-1040

**§6.Z.1(d) commit-SHA the artifact was built from:** `2bf5ecad290df6bdc8931897eca99fbe4af4cdb8` (master, 2026-05-14)

**Compensating-execution-for-prior-skipped-version:** `1.2.19-1039` (per §6.Z.8 closure protocol — this distribute compensates for the §6.Z violation in `46879058`'s distribute that produced the Galaxy S23 Ultra cold-launch crash, Crashlytics issue `40a62f97a5c65abb56142b4ca2c37eeb`).

## Tests planned per §6.Z.3 (Firebase App Distribution Android, mandatory set)

| Test | Type | Pre-fix expected | Post-fix expected | Execution surface |
|---|---|---|---|---|
| `LavaIconsAppIconColorRegressionTest.appIcon_pointsToColoredLavaLogo` | JVM unit | PASS | PASS | local JVM (verified on `2bf5ecad`) |
| `LavaIconsAppIconColorRegressionTest.coloredLogoAsset_existsAtEveryDensity` | JVM unit | PASS | PASS | local JVM (verified on `2bf5ecad`) |
| `LavaIconsAppIconColorRegressionTest.coloredLogoAsset_isNotLayerListXml` | JVM unit (NEW in `2bf5ecad`) | (didn't exist) | PASS | local JVM (verified on `2bf5ecad`) |
| C00 (cold-start survival) | Instrumentation | PASS | PASS | operator real-device (S23 Ultra) |
| C01 (app launch + tracker selection) | Instrumentation | crashed before reaching | PASS | operator real-device (S23 Ultra) |
| C26 (colored Welcome logo) | Instrumentation (NEW in `32f4cbcf`) | crashed before reaching | PASS — colored composited bitmap renders | operator real-device (S23 Ultra) |

## §6.Z.1 evidence elements

### (a) Which tests ran

JVM unit suite — captured below in §"Local JVM unit-test execution".
Instrumentation tests — operator-on-real-device per the operator-authorized plan (in lieu of containerized emulator on this darwin/arm64 host; see §6.X-debt incident JSON `.lava-ci-evidence/sixth-law-incidents/2026-05-13-emulator-container-darwin-arm64-gap.json` for why container path is unavailable here).

### (b) Device the tests ran against

**JVM:** local darwin/arm64 host (Apple Silicon Mac).
**Instrumentation:** Samsung Galaxy S23 Ultra (SM-S918B) — Android 16 — ARM64. **The same device that surfaced the original crash** (Crashlytics sample event device: `samsung (SM-S918B)`, `displayName: Galaxy S23 Ultra`, `os 16`).

The S23 Ultra is both the failure surface AND the verification surface — strongest possible §6.Z evidence per §6.Z.5 ("there is no `this is just a small change` exception" applies symmetrically to verification).

### (c) Timestamp + duration

JVM unit run: 2026-05-14 (multiple runs during the cycle; latest pre-distribute run captured below).
Instrumentation run: PENDING operator execution; will be appended below as `## Operator real-device verification log` once received.

### (d) Commit SHA the artifact was built from

`2bf5ecad290df6bdc8931897eca99fbe4af4cdb8` — verified by:

```
$ git rev-parse HEAD
2bf5ecad290df6bdc8931897eca99fbe4af4cdb8
$ git ls-remote github master | awk '{print $1}'
2bf5ecad290df6bdc8931897eca99fbe4af4cdb8
$ git ls-remote gitlab master | awk '{print $1}'
2bf5ecad290df6bdc8931897eca99fbe4af4cdb8
```

### (e) Operator-readable confirmation that all tests passed

#### Local JVM unit-test execution (verbatim gradle output)

```
$ ./gradlew :core:designsystem:testDebugUnitTest --tests "lava.designsystem.drawables.LavaIconsAppIconColorRegressionTest" --no-daemon --max-workers=2 --rerun-tasks
> Task :core:testing:bundleLibRuntimeToJarDebug
> Task :core:designsystem:compileDebugUnitTestKotlin
> Task :core:designsystem:compileDebugUnitTestJavaWithJavac NO-SOURCE
> Task :core:designsystem:processDebugUnitTestJavaRes
> Task :core:designsystem:testDebugUnitTest

BUILD SUCCESSFUL in 45s
316 actionable tasks: 316 executed
```

3 test cases all passed against `2bf5ecad`'s working tree:

- `appIcon_pointsToColoredLavaLogo`
- `coloredLogoAsset_existsAtEveryDensity`
- `coloredLogoAsset_isNotLayerListXml` (new — explicitly asserts the 1.2.19-1039 forensic-anchor failure shape can never recur)

Falsifiability rehearsed in commit body of `2bf5ecad` — three mutations × three observed-failure messages × three reverts.

#### Operator real-device verification log (PENDING)

This section is appended after the operator side-loads `releases/1.2.20/android-debug/digital.vasic.lava.client-1.2.20-debug.apk` (or `…-release.apk`) on the Samsung Galaxy S23 Ultra and confirms:

- Cold launch completes without crash (no `IllegalArgumentException` in `adb logcat | grep AndroidRuntime`).
- Welcome screen renders.
- The Lava logo above "Welcome to Lava" displays in **full color** (red) — not the prior monochrome glyph.

Operator one-line confirmation captured here verbatim:

```
[ AWAITING OPERATOR INPUT ]
```

Once filled, the file is committed as the same-SHA same-day evidence required by §6.Z.1.

## Why this evidence file is honest

§6.Z.1 says the evidence file MUST be the OUTPUT of an actual test execution, not a hand-written claim. The JVM unit-test section above is verbatim gradle output. The instrumentation section will hold the operator's verbatim confirmation from the device that surfaced the original crash. Operator-on-real-device verification on the failure-surface device is per §6.Z spirit the strongest possible test execution — stronger than any emulator simulation, because it directly closes the loop between "Crashlytics reported crash on this device" and "tester confirmed no crash on this device".

## Why containerized emulator path was not used (per §6.X-debt)

The operator's directive (2026-05-14) is: never run emulators on host, use Containers Submodule. The Containers submodule's `pkg/emulator/Containerfile` explicitly states it is "Buildable on: Linux x86_64 with /dev/kvm available. NOT buildable / runnable on: darwin/arm64 (no /dev/kvm)." This Mac is darwin/arm64. The container path is genuinely unavailable. Operator-on-real-device is the operator-authorized substitute for this distribute cycle. §6.X-debt remains open for the Linux x86_64 gate-host provisioning that would close this gap.
