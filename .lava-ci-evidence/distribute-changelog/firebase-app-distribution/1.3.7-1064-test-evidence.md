# §6.Z device-gate evidence — Lava-Android 1.3.7 (1064)

**Commit:** `478e2b0b` (cycle commits `e2a4fc9a`..`3940ba16` merging the 5 queued branches;
docs after are evidence-only — app/go source byte-identical to HEAD).
**Device:** Genymotion Pixel 9 / API 35 / arm64 (`127.0.0.1:6555`, §6.AH VM path,
§6.AG Containers-submodule-driven).
**Artifacts:** `app-debug.apk` (34 MB) + `app-release.apk` (6.7 MB R8) — both embed the
1.3.7 code (Tokyo Toshokan provider + TPB/Torrents-CSV failover are in the api-app `.so`;
the client carries the cancellation-noise fix).
**Date:** 2026-06-13. **Mode:** overnight autonomous (operator delegated all decisions;
the device gate serves as the §6.AA stage-1→stage-2 verifier).

## connectedDebugAndroidTest on the real VM (verbatim, runner = containers-submodule)

| Challenge | Tests | Failed | Result |
|---|---|---|---|
| C00 `Challenge00CrashSurvivalTest` (cold-start) | 1 | 0 | `BUILD SUCCESSFUL in 1m26s` |
| C26 `Challenge26ApiDiscoveryAndConnectivityTest` | 6 | 0 | `BUILD SUCCESSFUL in 1m13s` |
| C25 `Challenge25OnboardingFreshInstallTest` | 1 | 0 | `BUILD SUCCESSFUL in 51s` |
| C20 `Challenge20OnboardingWizardFullFlowTest` | 1 | 0 | `BUILD SUCCESSFUL in 57s` |

9 tests, 0 failed, 0 unexplained skips. Evidence dirs: `.lava-ci-evidence/genymotion/1.3.7-1064-*`.

## R8 release cold-start canary (the §6.AA stage-2 / R8-only crash-class check)

```
package: digital.vasic.lava.client
apk: app/build/outputs/apk/release/app-release.apk   (R8-minified, 6.7 MB)
LaunchState: COLD   TotalTime: 892   pid_after_launch: 10342   fatal_lines: <none>
==> RELEASE CANARY PASS
```
Evidence dir: `.lava-ci-evidence/release-canary/client-1.3.7-1064-release`.

## §6.Z compliance

- Debug + R8 release artifacts EXECUTED on device (not source-compiled): ✅
- Cold-start canary (load-bearing) GREEN on both debug (C00) and R8 release: ✅
- Onboarding flow (the 1.3.6 fix carried forward) re-gated GREEN: ✅
- Go layer (the 1.3.7 provider changes) verified: all curated + mobile-embed + router +
  handlers tests `ok` on master; tokyotosho live-probe confirmed honest CapSearch.
- Runner = containers-submodule (§6.AH/§6.AG): ✅
- §6.AA: device gate (debug challenges + R8 release canary) is the autonomous verifier
  between stage-1 and stage-2 per the operator's overnight delegation.
