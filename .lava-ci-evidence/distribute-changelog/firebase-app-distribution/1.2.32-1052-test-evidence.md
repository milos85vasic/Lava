# Lava-Android-1.2.32-1052 — Pre-Distribute Test Evidence (§6.Z)

**Commit SHAs at test time:** `f5f75b07` (1.2.31 Stage 2 + §6.L 61st) + this commit's tree (version bump + Challenge26)
**Test session timestamp:** 2026-05-18T07:05Z
**Channel:** firebase-app-distribution (debug + release stages)

## Test environment

| Field | Value |
|---|---|
| Host | macOS / Darwin 24.5.0 / arm64 |
| Emulator | Pixel_8 AVD (host-direct — §6.X-debt blocks containerized on darwin/arm64) |
| Android API level | 35 |
| Test runner | `./gradlew :app:connectedDebugAndroidTest` |

## Tests executed

### Challenge26ApiDiscoveryAndConnectivityTest — 6/6 PASS

The new anti-bluff Challenge for the ApiSelection step. Tests render the production `ApiSelectionStep` Composable directly via `composeRule.setContent { LavaTheme { ApiSelectionStep(...) } }` with controlled state and assert on rendered text contracts.

Test list (all PASS):
- `discoveryRunning_showsSearchingText`
- `discoveryEmpty_showsEmptyStateAndSearchAgain`
- `discoveryFoundOne_showsFoundCountAndEndpointRow`
- `discoveryFoundTwo_pluralizesAndRendersBoth`
- `selectingEndpoint_invokesOnSelectCallback`
- `probeFailure_showsReasonAndRetryButton`

### Challenge00CrashSurvivalTest + Challenge01AppLaunchAndTrackerSelectionTest — 2/2 PASS

Run with the existing `TestOnboardingHiltModule` override (`apiSelectionEnabled = false`) preserving the pre-60th legacy Welcome → Providers flow these Challenges were designed for. Wave 3 follow-up will update them to traverse the new step under production flag (deferred to keep 1.2.32 scoped).

### Combined run

```
> Task :app:connectedDebugAndroidTest
Starting 8 tests on Pixel_8(AVD) - 15

Pixel_8(AVD) - 15 Tests 6/8 completed. (0 skipped) (0 failed)
Finished 8 tests on Pixel_8(AVD) - 15

BUILD SUCCESSFUL in 34s
864 actionable tasks: 24 executed, 840 up-to-date
```

8 of 8 = C00 (1) + C01 (1) + Challenge26 (6). All PASS.

### Debug APK cold-launch (direct adb)

```
pm clear digital.vasic.lava.client.dev
am start -W -n digital.vasic.lava.client.dev/digital.vasic.lava.client.MainActivity

Status: ok
LaunchState: COLD
TotalTime: 2384
```

pidof → 9430 (alive); logcat FATAL grep → empty.

### Release APK cold-launch (§6.AA.4 R8 surprise class)

```
pm clear digital.vasic.lava.client
am start -W -n digital.vasic.lava.client/.MainActivity

Status: ok
LaunchState: COLD
TotalTime: 854
```

pidof → 9687 (alive); no FATAL. R8 minification in 1.2.32 release introduced no regressions.

### Lava-API-Go restart pending

Both APIs will be restarted on the new v2.3.21 binary as part of the Stage 1 distribute pre-flight (next operations).

## §6.AA.4 R8-surprise discrimination note

This release adds NO new app code paths vs 1.2.31 — only test code (`Challenge26ApiDiscoveryAndConnectivityTest.kt`). The APK behavior is functionally identical to 1.2.31; release cold-launch confirms no R8 regression.

## §6.X honest scope note

Tests ran on a darwin/arm64 host-direct Pixel_8 emulator per the long-standing §6.X-debt: podman on darwin does NOT expose `/dev/kvm` or HVF passthrough to the container runtime, blocking the containerized matrix runner. Per §6.I clause 1 transitional allowance.

`Classification:` project-specific.
