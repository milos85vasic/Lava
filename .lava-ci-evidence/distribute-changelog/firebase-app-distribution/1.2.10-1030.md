Lava 1.2.10 (1030) — Docker auth fix + distribution artifacts

Fixes docker-compose.yml to pass required auth environment variables
(LAVA_AUTH_FIELD_NAME, LAVA_AUTH_HMAC_SECRET, LAVA_AUTH_ACTIVE_CLIENTS)
to the lava-api-go container, which previously crashed on startup.

Go API bumped to 2.3.1.
