## Lava-Android-1.2.29-1049 / Lava-API-Go-2.3.18-2318 — 2026-05-17 (sweep tier-B — deferred findings #2 + #3 closed)

**Previous published:** Lava-Android-1.2.28-1048 (debug + release both distributed 2026-05-17 evening).

This release closes the two remaining sweep findings deferred from 1.2.28's tier-A. With this, 10 of 10 sweep findings from the comprehensive sweep at `.lava-ci-evidence/sweep-reports/2026-05-17-comprehensive-sweep.md` (commit `a5fa8033`) are now closed.

### What's in this release

#### Sweep findings closed

- **#2 P0 — search Error variant + retry** (`SearchResultContent.Error(reason)` + screen render + dispatch-by-state RetryClick)
- **#3 P0 — provider-config LazyColumn refactor** (Column(verticalScroll) → LazyColumn with item() sections — eliminates scroll-jank with many providers/mirrors)

#### Architectural touches

- New `SearchResultContent.Error(reason: String)` sealed-interface variant in `feature/search_result`
- New strings: `search_screen_result_error_{title,subtitle,retry}` in EN + RU
- `SearchResultViewModel.onRetryClick()` becomes dispatch-by-state (Error → re-subscribe stream; else → Paging3 retry)
- `ProviderConfigScreen` root container: Column-with-verticalScroll → LazyColumn-with-item()

### Standing constitutional debt acknowledged (carried from 1.2.28)

- §6.H — RuTracker password rotation still PENDING per clause 6 (credentials redacted in `d7d4572a` but valid until operator rotates upstream)
- §6.X-debt — darwin/arm64 podman VM lacks /dev/kvm; container-bound emulator path remains blocked on this host
- §6.K pre-existing-gap — Android Gradle + Go binary builds run host-direct on darwin/arm64

### Distribute-readiness

- ✅ §6.P versionCode 1049 > last-version 1048
- ✅ §6.Y bump-first applied
- ✅ §6.W mirrors converged
- ✅ §6.AA two-stage (debug-only first, then release-only after operator confirmation per standing preauth)
- ✅ §6.Z evidence file pre-distribute

`Classification:` project-specific.
