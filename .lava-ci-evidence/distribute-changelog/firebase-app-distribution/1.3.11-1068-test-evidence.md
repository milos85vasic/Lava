# §6.Z test-evidence — Lava-Android 1.3.11 (1068) — STAGE-1 DEBUG re-spin

**Commit:** `cd6ae704cb926254ff54095666b1d05fcc78b1a9` (current HEAD at build time; debug APK
produced from this tree).
**Channel:** `firebase-app-distribution` → debug app-id `LAVA_FIREBASE_ANDROID_DEV_APP_ID`
(application id `digital.vasic.lava.client.dev`).
**Artifact:** `releases/1.3.11/android-debug/lava-1.3.11-1068-debug.apk`
(sha256 `81be6d5940300d5861c8c8e893d9aca1a2d645b24697f526c8ef17d5047eb381`, 33,961,114 bytes;
embeds Chucker for on-device HTTP inspection + the latest client fixes).
**Date:** 2026-06-23. **Mode:** §6.AA Stage 1 (debug-only) for OPERATOR on-device verification.

**Pepper-rotation re-spin (REAL captured evidence).** An earlier 16:18 APK (sha256
`af7cc910edd277c14be77dcf7ae1ce083054ae5bce8df0898b3c276f8bdce09c`, 36,103,216 bytes) embedded the
PRIOR auth pepper — it was built (14:13 codegen / 16:14 APK) before `.env` was updated to the
rotated `LAVA_AUTH_CURRENT_CLIENT_NAME=android-1.3.11-1068` identity (`.env` mtime 16:40). That stale
APK was therefore overwritten in the releases dir and re-distributed is required. `:app:assembleDebug`
was re-run against the CURRENT `.env` (`:app:generateLavaAuthClassDebug` + `:app:compileDebugKotlin`
re-executed, NOT up-to-date → LavaAuthGenerated.kt regenerated at 16:45, now newer than `.env`),
producing the new artifact above. The new APK SHA (`81be6d59…`) differs from the stale SHA
(`af7cc910…`), proving the rotated pepper is embedded.

## What this debug re-spin carries (the client-side fixes under verification)

- `cfe838bc` — `fix(search): failed multi-provider stream renders Error+Retry, not dead-end
  "Nothing found"`. A failed multi-provider search stream now renders an explicit Error state
  with a Retry action instead of the misleading empty "Nothing found" state.
- `922ecbca` — `fix(§6.AC search): record per-provider streaming-search failures instead of
  dropping them`. Per-provider streaming-search failures are now captured as §6.AC non-fatal
  telemetry instead of being silently dropped.

## What WAS verified for this build (REAL captured evidence)

`:app:assembleDebug` → **BUILD SUCCESSFUL in 27s** (`export GRADLE_USER_HOME=/Volumes/T7/Gradle`,
re-spin against the rotated `.env`), APK at `app/build/outputs/apk/debug/app-debug.apk`, copied to
the releases dir above (overwriting the stale 16:18 APK).

JVM / unit suite GREEN (real test-report XML counts, `testDebugUnitTest`, 0 failures / 0 errors):

| Test class | module | tests | skipped | fail | err |
|---|---|---:|---:|---:|---:|
| `lava.search.result.SearchResultViewModelRetryTest` (cfe838bc) | feature:search_result | 1 | 0 | 0 | 0 |
| `lava.search.result.SearchResultViewModelStreamingTest` (922ecbca §6.AC telemetry) | feature:search_result | 3 | 0 | 0 | 0 |
| `lava.search.result.SearchResultViewModelFallbackTest` | feature:search_result | 3 | 0 | 0 | 0 |
| `lava.search.result.ApplyMultiSearchEventTest` | feature:search_result | 6 | 0 | 0 | 0 |
| `lava.search.result.SearchResultNavigationProviderIdsRoundtripTest` | feature:search_result | 3 | 0 | 0 | 0 |
| `lava.search.result.categories.CategorySelectionViewModelTest` | feature:search_result | 7 | 0 | 0 | 0 |
| `lava.securestorage.EndpointConverterTest` (key-roundtrip) | core:preferences | 17 | 0 | 0 | 0 |
| `lava.securestorage.EndpointConverterMalformedTest` | core:preferences | 6 | 0 | 0 | 0 |
| `lava.tracker.client.ApiBackedTrackerClientAuthFailureTest` | core:tracker:client | 3 | 0 | 0 | 0 |

`:feature:search_result:testDebugUnitTest :core:preferences:testDebugUnitTest` → **BUILD SUCCESSFUL in 1m 8s**.
`:core:tracker:client:testDebugUnitTest` → **BUILD SUCCESSFUL in 45s**.

## What was NOT verified (honest §6.Z / §6.J posture — no bluff)

On-device automated Compose UI Challenge execution (the thinker KVM C00/Cnn gate) is **OWED** —
the device gate is being unblocked in parallel and is NOT yet green for this build. This build is
therefore distributed as **§6.AA stage-1 debug for OPERATOR on-device verification per §6.Z.5**
(the operator installs the Firebase-distributed debug APK and observes the search readout —
specifically the new Error+Retry surface on a failing search). No on-device automated Challenge
execution is claimed for this artifact; the RELEASE variant is NOT distributed (§6.Z-gated until
the device gate is green).
