# §6.Z device-gate evidence — Lava-Android 1.3.10 (1067)
**Commit:** `1d4c42b8`. **Device:** Genymotion Pixel 9 / API 35 / arm64 (`127.0.0.1:6555`, §6.AH VM).
**Date:** 2026-06-14. Adds P0-1 (Auth-Token), P1-4 (remote-key guard), P2-1 (permission-suffix) over 1.3.9.

## connectedDebugAndroidTest (verbatim) — C00+C41+C42+C43+C44
```
BUILD SUCCESSFUL in 57s
```
C00 cold-start + C41 select-all + C42 password + C43 server-dedup + C44 api-search-auth — all PASS, 0 fail.
Evidence: `.lava-ci-evidence/genymotion/1.3.10-1067-client-gate/`.

## R8 release canary
```
package: digital.vasic.lava.client   apk: releases/1.3.10/android-release/lava-1.3.10-1067-release.apk (R8)
==> RELEASE CANARY PASS — process alive, no fatal
```

## §6.Z compliance
- Debug + R8 release EXECUTED on device (not source-compiled): ✅
- Cold-start (C00) + 4 fix Challenges GREEN; R8 release canary GREEN: ✅
- P0-1 Auth-Token is ADDITIVE + unit/falsifiability-verified (`ProviderSessionTokenEndToEndWiringTest`);
  the no-auth search path (C44 + the 1.3.9 device verify) is unchanged. **OWED (documented):** on-device
  RuTracker login+search verify with real creds — shipped for tester verification (subagent rate-limit blocked the auto-run).
