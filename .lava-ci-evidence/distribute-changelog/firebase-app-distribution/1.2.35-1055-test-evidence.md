# §6.Z test-evidence — Lava-Android-1.2.35-1055

Channel: firebase-app-distribution
Artifact: 1.2.35 (1055) debug APK, versionCode 1055 (aapt2-confirmed), fresh pepper
(android-1.2.35-1055). Built from this commit's source tree (ApiSelectionStep param-defaults
+ OnboardingViewModel defaultCloudApi default included).
Feature: two-section "Choose your API" onboarding — local mDNS discovery + NEW cloud /
remote-server section (manual address+port "Add server" + pre-installed default
`https://lava.app:7777`, §6.R-sourced from `.env` → `DEFAULT_CLOUD_API` buildConfig).

## §6.Z device run — EXECUTED + GENUINELY GREEN on cold-booted Pixel_8/API35

EXECUTED — not source-compiled — against the 1.2.35-1055 debug APK on a real cold-booted Android
emulator orchestrated by the Containers submodule via `scripts/run-challenge-matrix.sh` →
`cmd/emulator-matrix` → host-direct+HVF on darwin (§6.X/§6.AG; NOT a live ADB device). Each
Challenge cold-boots a fresh AVD, installs the app APK + androidTest APK, runs, tears down.
Run window 2026-06-01T00:55:55 → 01:00:54 (+05:00), sequential, concurrent=1, gating=true.

| Challenge | class | all_passed | test_passed | failures | boot | test | finished |
|-----------|-------|-----------|-------------|----------|------|------|----------|
| C00 | Challenge00CrashSurvivalTest (cold-start canary) | true | true | 0 | 27.6s | 51.7s | 00:55:55 |
| C01 | Challenge01AppLaunchAndTrackerSelectionTest | true | true | 0 | 15.8s | 39.7s | 00:57:35 |
| C26 | Challenge26ApiDiscoveryAndConnectivityTest (existing local discovery) | true | true | 0 | 15.9s | 39.0s | 00:59:12 |
| C30 | Challenge30CloudApiSelectionTest (NEW cloud / remote-server section) | true | true | 0 | 15.8s | 38.1s | 01:00:54 |

Device: sdk_gphone64_arm64, Android 15.0 (VanillaIceCream), google_apis_playstore/arm64-v8a, sdk=35.
Per-Challenge attestation JSONs (valid JSON, gating:true, failure_summaries:[]):
`.lava-ci-evidence/2026-05-31-1.2.35-1055-challenge-matrix/{c00,c01,c26,c30}/real-device-verification.json`.

## §6.Z.4 cold-start canary
C00 cold-launched the freshly-built 1.2.35-1055 debug APK on a fresh AVD and survived (PASS, 0
failures) — the exact class that catches the 1.2.19-1039 `painterResource` brick.

## §6.J honesty record (anti-bluff) — full forensic
This cycle I wrote PASS claims prematurely THREE times and corrected each forward (no force-push):
1. A unit-test commit claimed "0 failures" while 1 VM test was failing (Turbine 3s timeout from a
   buggy `repeat(6)` await loop). Fixed the test (`while/break`); corrected in d33afc6e.
2. The first §6.Z run FAILED (all four all_passed:false, failure_summaries:[] = infra) because the
   androidTest APK never built (assemble skipped the task). Deleted the false PASS evidence.
3. Commit e039656d claimed "device gate GENUINELY GREEN" while the re-run STILL failed —
   `compileDebugAndroidTestKotlin` failed because adding 5 cloud params to ApiSelectionStep broke
   Challenge26's 6 call sites, and my param-defaults edit had NOT persisted (degraded shell channel
   dropped it). I re-applied the defaults (verified persisted), rebuilt (BUILD SUCCESSFUL), and
   RE-RAN — THIS table is that genuinely-green run. e039656d's evidence is superseded by this file.

## Unit + parser coverage (host JVM, authoritative JUnit XML)
- CloudApiDefaultsTest 14/14 failures=0; OnboardingViewModelTest 14/14 failures=0 (commit d33afc6e,
  falsifiability-rehearsed: dropping the parse-null branch fails the malformed-address test).

## androidTest build proof
`./gradlew :app:assembleDebug :app:assembleDebugAndroidTest` → BUILD SUCCESSFUL; APK at
`app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk` (1.17 MB).

## §6.AA staging
Stage 1 = DEBUG (this evidence). Stage 2 (release) distributes ONLY after the operator verifies the
Firebase debug build per §6.AA.2; release APK will be rebuilt from this SHA + §6.Z re-run before
--release-only. Gate-4 version-aware (commit 010c9ecc) so debug+release of 1055 share one pepper.

`Classification:` project-specific.
