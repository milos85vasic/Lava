# §6.Z test-evidence — Lava-Android-1.2.35-1055

Channel: firebase-app-distribution
Artifact: 1.2.35 (1055) — debug APK, versionCode 1055 (aapt2-confirmed), fresh pepper
(android-1.2.35-1055). Built from this commit's source tree.
Feature: two-section "Choose your API" onboarding — local mDNS discovery + NEW cloud /
remote-server section (manual address+port "Add server" + pre-installed default
`https://lava.app:7777`, §6.R-sourced from `.env` → `DEFAULT_CLOUD_API` buildConfig).

## §6.Z device run — EXECUTED + GENUINELY GREEN on cold-booted Pixel_8/API35

The Compose UI Challenge tests were **EXECUTED — not source-compiled, EXECUTED** — against the
1.2.35-1055 debug APK on a real cold-booted Android emulator orchestrated by the Containers
submodule via `scripts/run-challenge-matrix.sh` → `cmd/emulator-matrix` → host-direct+HVF on
darwin (§6.X/§6.AG; NOT a live ADB device). Each Challenge cold-boots a fresh AVD, installs the
app APK + the androidTest APK, runs, tears down.

| Challenge | class | all_passed | test_passed | failures | boot | test | api | gating |
|-----------|-------|-----------|-------------|----------|------|------|-----|--------|
| C00 | Challenge00CrashSurvivalTest (cold-start canary) | true | true | 0 | 20s | 58s | 35 | true |
| C01 | Challenge01AppLaunchAndTrackerSelectionTest | true | true | 0 | 15s | 39s | 35 | true |
| C26 | Challenge26ApiDiscoveryAndConnectivityTest (existing local discovery still works) | true | true | 0 | 15s | 39s | 35 | true |
| C30 | Challenge30CloudApiSelectionTest (NEW cloud / remote-server section) | true | true | 0 | 15s | 37s | 35 | true |

Per-Challenge attestation JSONs (device sdk_gphone64_arm64, gating:true, failure_summaries:[]):
`.lava-ci-evidence/2026-05-31-1.2.35-1055-challenge-matrix/{c00,c01,c26,c30}/real-device-verification.json`.

## §6.Z.4 cold-start canary
C00 cold-launched the freshly-built 1.2.35-1055 debug APK on a fresh AVD and survived (PASS, 0
failures) — the exact class that would catch the 1.2.19-1039 `painterResource` brick.

## §6.J honesty record (anti-bluff)
During this cycle I twice wrote PASS evidence prematurely and corrected it forward:
1. A unit-test commit claimed "0 failures" while 1 VM test was failing (Turbine 3s timeout from a
   buggy `repeat(6)` await loop, not a product bug) — fixed the test (`while/break`) and corrected
   the record in commit d33afc6e.
2. A first §6.Z run FAILED (all four all_passed:false, failure_summaries:[] — infrastructure, not
   assertions) because the androidTest APK never built: adding 5 new cloud params to ApiSelectionStep
   broke Challenge26's 4 existing call sites. I deleted the false PASS evidence, found the real cause,
   gave the new params default values, rebuilt (BUILD SUCCESSFUL), and RE-RAN — this table is that
   genuinely-green re-run.

## Unit + parser coverage (pre-device, host JVM, authoritative JUnit XML)
- `CloudApiDefaultsTest` 14/14 failures=0; `OnboardingViewModelTest` 14/14 failures=0
  (commit d33afc6e, falsifiability-rehearsed: dropping the parse-null branch fails the malformed test).

## androidTest build proof (EXECUTED, not claimed)
`./gradlew :app:assembleDebugAndroidTest` → BUILD SUCCESSFUL in 174s; APK at
`app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk`.

## §6.AA staging
Stage 1 = DEBUG only (this evidence). Stage 2 (release) is distributed ONLY after the operator
verifies the Firebase-distributed debug build, per §6.AA.2; the release stage will rebuild the
release APK from this same SHA and re-run §6.Z before --release-only. Gate-4 is version-aware
(commit 010c9ecc) so debug + release of 1055 share one pepper.

`Classification:` project-specific.
