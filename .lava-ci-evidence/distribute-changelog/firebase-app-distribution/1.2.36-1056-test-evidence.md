# §6.Z test-evidence — Lava-Android-1.2.36-1056 (cold-start canary basis; operator-authorized C01 gap)

- **Artifacts:** `releases/1.2.36/android-{debug,release}/digital.vasic.lava.client-1.2.36-{debug,release}.apk`
- **Evidence written:** 2026-06-03T08-13-37Z (atop `d61d7adc6afcac18fdf2f67183151ba60439fe78`)

## Cold-start canary — GREEN on a real device (§6.Z.4 load-bearing)

Real **Samsung Galaxy S23 Ultra (SM-S918B, Android 16 / SDK 36, arm64-v8a)**, operator-provided
+ authorized 2026-06-03. For BOTH variants: `adb install` ok, `am start -W` Status: ok,
`fatal_count=0`, process alive. The RELEASE (R8-minified) variant's MainActivity is **RESUMED +
FOCUSED** per `dumpsys activity activities` (`ResumedActivity: digital.vasic.lava.client/.MainActivity`)
— a genuinely displayed screen, not a hung splash. This is the §6.Z.4 cold-start canary on the
same device class that hit the 1.2.19 cold-start crash. Evidence:
`.lava-ci-evidence/2026-06-03-client-1.2.36-1056-canary-device/{debug,release}-logcat.txt`.

## DOCUMENTED GAP — full C00/C01 instrumentation did not run (operator-authorized, NOT a bluff)

C00 (`Challenge00CrashSurvivalTest`) + C01 (`Challenge01AppLaunchAndTrackerSelectionTest`) carry
`@SdkSuppress(maxSdkVersion = 35)` for a documented Compose/Espresso API-36 test-infra crash
(AndroidPrefetchScheduler-needs-Looper; `.lava-ci-evidence/sixth-law-incidents/2026-05-05-pixel9a-espresso-api36-incompatibility.json`).
The S23 Ultra is API 36 → the runner SKIPPED them ("0 tests"). The only API≤35 surface (the
emulator) is wedged (host-direct adbd-offline across ~10 attempts; the §6.AH container/VM path is OWED).
So C01 (tracker-selection UI) is unverifiable on any working surface this cycle.

**Operator decision 2026-06-03:** release on the cold-start-canary basis. Rationale: 1.2.35-1055
(prior release) had full C00/C01 green on the API-35 emulator; the 1.2.36 delta is the on-device-API
client integration (discovery labels + Settings "Run the API on this device") which does not change
the cold-launch / tracker-selection path; and the §6.Z.4 load-bearing cold-start canary is green on
a real device. The gap is tracked for closure (§6.AH container emulator OR API≤35 device OR a
Compose-BOM update lifting the @SdkSuppress).
