# §6.Z pre-distribute device-gate evidence — Lava-Android 1.3.4 (1061)

**Commit SHA built from:** `34c06119` (= git HEAD at gate time; auth rotation
touches only the gitignored `.env`, not the committed source, so the SHA is
unchanged by rotation/rebuild).
**Device:** Genymotion **Google Pixel 9**, **Android API 35**, **arm64-v8a**,
serial `127.0.0.1:6555` (§6.AH-compliant VM path — NOT host-direct).
**Runner:** `:app:connectedDebugAndroidTest` (real instrumented Compose UI on the
real VM), `--no-daemon --max-workers=2 nice` (§6.T.2).
**Timestamp:** 2026-06-13 (this session).

## Challenge Tests EXECUTED + PASSED (not source-compiled — EXECUTED on device)
| Challenge | Surface | Result |
|-----------|---------|--------|
| `Challenge00CrashSurvivalTest` | cold-start survival (exercises the new `LavaApplication.onCreate` provider re-populate) | **PASS** — `Tests 1/1 completed (0 failed) BUILD SUCCESSFUL` |
| `Challenge01AppLaunchAndTrackerSelectionTest` | app launch + tracker/provider selection | **PASS** — `BUILD SUCCESSFUL` |
| `Challenge16ApiSupportedFilterTest` | apiSupported provider filtering | **PASS** — `BUILD SUCCESSFUL` |
| `Challenge25OnboardingFreshInstallTest` | onboarding fresh-install flow | **PASS** — `BUILD SUCCESSFUL` |

## §6.Z compliance notes
- The cold-start canary (C00) is the load-bearing §6.Z gate; it PASSES against
  the exact debug artifact, confirming this session's `onCreate` provider
  re-populate change does NOT crash launch (forensic: 1.2.19 shipped un-gated and
  bricked every cold start — this gate is exactly that prevention).
- LVA-074 mitigation applied: the VM screen was hard-woken + keyguard-dismissed +
  `svc power stayon true` immediately before each run (a sleeping Genymotion
  screen yields "No compose hierarchies" false-RED; verified the first RED was the
  render flake, NOT a crash — the app process stayed alive `pid 4394`, no FATAL in
  logcat — then re-ran GREEN after the wake).
- This evidence covers the DEBUG client (§6.AA stage 1). The release client is the
  same source (R8 differences aside); a release cold-start canary is run before
  the §6.AA stage-2 release distribute.

This file is the §6.Z pre-distribute test-evidence required before
`scripts/firebase-distribute.sh` operates on 1.3.4-1061.
