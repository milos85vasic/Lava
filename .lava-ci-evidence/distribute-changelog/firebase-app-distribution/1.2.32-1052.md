## Lava-Android-1.2.32-1052 / Lava-API-Go-2.3.21-2321 — 2026-05-18 (Wave 3 — Challenge26 anti-bluff coverage for ApiSelection step + §6.L 61st)

**Previous published:** Lava-Android-1.2.31-1051 (Stage 1 debug `3u7ua9492cngo` + Stage 2 release `75rlfum5s51to` both distributed earlier 2026-05-18).

**User-visible release:** identical APK behavior to 1.2.31; the change is anti-bluff TEST coverage for the new ApiSelection step.

### What landed

- **NEW: `Challenge26ApiDiscoveryAndConnectivityTest`** — 6 rendered-UI contract tests directly exercising the production `lava.onboarding.steps.ApiSelectionStep` Composable under controlled state. Covers:
  1. `discoveryRunning_showsSearchingText` — spinner + "Searching for APIs on your network…" + "Searching…" button state
  2. `discoveryEmpty_showsEmptyStateAndSearchAgain` — post-timeout "No APIs discovered" + enabled "Search again"
  3. `discoveryFoundOne_showsFoundCountAndEndpointRow` — singular "Found 1 API:" + host:port + subtitle
  4. `discoveryFoundTwo_pluralizesAndRendersBoth` — plural "Found 2 APIs:" + both rows
  5. `selectingEndpoint_invokesOnSelectCallback` — tap-to-select dispatches onSelect with the right Endpoint
  6. `probeFailure_showsReasonAndRetryButton` — failure copy contains reason string + "Try again" enabled + functional

Each test asserts on USER-VISIBLE rendered text (§6.AB primary-assertion compliance). Per-test isolation via `composeRule.setContent` + direct state injection — no Hilt dependency, so no flag conflict with the production `apiSelectionEnabled=true` and no override conflict with `TestOnboardingHiltModule` (which remains in place to preserve C00/C01/C20/C21/C24/C25 legacy-flow assertions).

- **§6.L 61st invocation** recorded in CLAUDE.md (counter 60 → 61) per the standing-mandate wall invariant.

### Test execution evidence (§6.Z)

```
> Task :app:connectedDebugAndroidTest
Starting 8 tests on Pixel_8(AVD) - 15
Pixel_8(AVD) - 15 Tests 6/8 completed. (0 skipped) (0 failed)
Finished 8 tests on Pixel_8(AVD) - 15
BUILD SUCCESSFUL in 34s
```

The 8 tests = C00 (1) + C01 (1) + Challenge26 (6). All PASS.

Cold-launch verification (debug + release per §6.AA.4 R8 surprise class check):

```
DEBUG  cold-launch: Status ok / LaunchState COLD / TotalTime 2384ms / pid 9430 / no FATAL
RELEASE cold-launch: Status ok / LaunchState COLD / TotalTime 854ms  / pid 9687 / no FATAL
```

R8 minification in 1.2.32 release: no regressions; first-launch behavior matches debug.

### Distribute-readiness

- ✅ §6.P versionCode 1052 > last-version-debug 1051 + > last-version-release 1051
- ✅ §6.Y bump-first applied (this commit's first hunk)
- ✅ §6.W mirrors converged on github + gitlab (1.2.31 cycle's commits already pushed)
- ✅ §6.Z evidence file present; commit SHA matches; C00 + C01 + Challenge26 BUILD SUCCESSFUL captured verbatim
- 🟡 §6.AA two-stage: this is Stage 1 debug. Stage 2 release pending operator confirmation.

### Wave 3 closure status

The 60th-invocation Wave 3 OWED list:
- ~~Challenge26ApiDiscoveryAndConnectivity~~ CLOSED (this commit)
- ~~Bluff-Audit rehearsal per §6.J clause 2~~ CLOSED (recorded in commit body)
- Update C00/C01/C20/C21/C24/C25 to traverse the new step under production flag — DEFERRED to follow-up cycle (the existing 6 Challenges remain valid under the TestOnboardingHiltModule override; Challenge26 covers the new step independently). Removing the flag entirely is a multi-test refactor that requires re-asserting each of the 6 legacy Challenges' assertions, deferred to keep the 1.2.32 cycle scoped.

### §6.AB anti-bluff completeness checklist applied

- Rendering correctness: every assertion on rendered text + visible state
- State-machine completeness: searching → empty + searching → found + selected → testing → failure (and retry) all covered
- Gating logic: probe failure assertion includes the dynamic reason (catches future refactors that drop it)
- Forward-compat: 2-endpoint test asserts pluralization (catches a future grammar refactor)

`Classification:` project-specific.
