## Lava-Android-1.3.11-1069 — 2026-06-23 (Corrected re-spin of 1.3.11 — ships the right binary)

**Previous:** 1.3.11-1068 — which published the WRONG (stale, pre-pepper, pre-fix) client debug
binary (Firebase release `3r986p5gnfujo`, app-id `digital.vasic.lava.client.dev`) and advanced
`last-version-debug`→1068, so §6.P forbids re-publishing 1068. 1069 is a **corrected re-spin** of the
SAME 1.3.11 content. The user-facing version name is unchanged (still 1.3.11); only the binary that
ships under it is corrected. Release identity: **1.3.11 (1069)**.

The correct binary carries the client-side fixes that 1068 was supposed to ship but did not:

- `cfe838bc` — `fix(search): failed multi-provider stream renders Error+Retry, not dead-end
  "Nothing found"`. A failed multi-provider search stream now renders an explicit Error state with a
  Retry action instead of the misleading empty "Nothing found" state.
- `922ecbca` — `fix(§6.AC search): record per-provider streaming-search failures instead of dropping
  them`. Per-provider streaming-search failures are now captured as §6.AC non-fatal telemetry instead
  of being silently dropped.
- Rotated `LAVA_AUTH_OBFUSCATION_PEPPER` + `android-1.3.11-1069` client identity registered in the
  allowlist (the new pepper is embedded in the APK — proven by the regenerated
  `LavaAuthGenerated.kt` and the changed APK SHA vs the stale 1068 artifact).
