# Lava-Android-1.3.16-1083 — 4 parallel-subagent fixes + RuTracker Cloudflare-classification fix

**Previous published:** Lava-Android-1.3.15-1082.

- **LVA-085 — search-results filter chips no longer show raw provider ids.** Chips previously showed strings like "archiveorg"/"torrentdownloads" instead of proper display names.
- **LVA-086 — search results now show a loading indicator / empty-state instead of a blank screen.**
- **LVA-093/LVA-094 — cold-start search race + silent provider-repopulation failure closed.** A search fired immediately after app launch could silently resolve against the wrong tracker client.
- **RuTracker login now correctly classified when blocked by a Cloudflare bot-mitigation challenge.** Backend detection + telemetry fix (503 + realistic outbound headers instead of a misleading generic 502). Login will still fail on a Cloudflare-blocked network, but the cause is now honestly surfaced rather than generic.
- **Re-verified with fresh device evidence this cycle (no code change needed):** search-input/results chip agreement, Welcome screen provider count, onboarding "Select all" auth-provider filtering.

## Build info
- App versionName: 1.3.16
- App versionCode: 1083
- Distribution channel: firebase-app-distribution

## Evidence
- Device gate: C00 fresh + C60/C63/C66 (fresh or honestly cited): `.lava-ci-evidence/distribute-changelog/firebase-app-distribution/1.3.16-1083-test-evidence.md`
- Cycle-coverage-map: `.lava-ci-evidence/distribute-changelog/firebase-app-distribution/cycle-coverage-map-1.3.16-1083.yaml`
