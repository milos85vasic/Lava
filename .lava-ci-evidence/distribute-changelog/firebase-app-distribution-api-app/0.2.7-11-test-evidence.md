# §6.Z device-gate evidence — Lava-API-App 0.2.7 (11)

**Commit:** `478e2b0b` (cycle: Tokyo Toshokan provider `3940ba16` + TPB/Torrents-CSV
failover `0a459d8b` — both in the embedded `.so`).
**Device:** Genymotion Pixel 9 / API 35 / arm64 (`127.0.0.1:6555`, §6.AH VM path,
§6.AG Containers-driven via `scripts/run-release-canary.sh`).
**Artifacts:** `api-app-debug.apk` (190 MB) + `api-app-release.apk` (167 MB R8).
**Date:** 2026-06-13. **Mode:** overnight autonomous.

## R8 release cold-start canary (verbatim)

```
package: digital.vasic.lava.api
apk: api-app/build/outputs/apk/release/api-app-release.apk   (R8, 167 MB)
LaunchState: COLD   TotalTime: 917   pid_after_launch: 10519   fatal_lines: <none>
==> RELEASE CANARY PASS
```
Evidence dir: `.lava-ci-evidence/release-canary/api-app-0.2.7-11-release`.

## Server-side proof (the load-bearing proof for the 1.3.7 provider changes)

The 0.2.7-11 change adds the Tokyo Toshokan provider + TPB/Torrents-CSV mirror-failover
to the embedded `.so`. Verified off-device on master (the embed uses the exact same
source):
- All 8 curated provider packages + the curated registry + the `internal/mobile` embed
  registration test + `router` + `handlers` tests `ok` (`go test`, real `router.Build`).
- Tokyo Toshokan: live-probe confirmed `terms=` genuinely filters (naruto 93% / bleach
  98% / empty-query 0%) — honest CapSearch, 3 falsifiability mutations RED→GREEN.
- TPB + Torrents-CSV: failover architecture with honest single-element mirror slices
  (live-probed: each has exactly 1 working endpoint now — no fabricated mirrors);
  `TestSearch_FailsOverToHealthyMirror` + `…AllMirrorsDownSurfacesError` falsified.
- All 7 prior curated providers re-confirmed LIVE-reachable this cycle (0 broken).

## §6.Z compliance

- EXACT R8 release artifact EXECUTED on device, cold-start GREEN: ✅
- Embedded-provider changes verified against the real `router.Build` + live probes: ✅
- Runner = Containers-submodule genymotion CLI (§6.AH/§6.AG): ✅
