## Lava-API-App-0.2.11-17 — 2026-06-23 (Foreground-service failure telemetry)

**Previous:** 0.2.11-16 (distributed debug 7esmk60v7ki0o + release 00m6ja0b4evpg).

0.2.11-17 adds §6.AC telemetry to the on-device API engine: `ApiEngineService` records a Crashlytics
non-fatal on `ForegroundServiceStartNotAllowedException` (the `dataSync` FGS budget-exhaustion edge
case) instead of swallowing it. §6.H-safe (no credentials in telemetry).

**Channel:** firebase-app-distribution-api-app (debug `digital.vasic.lava.api.dev` + release `digital.vasic.lava.api`).
