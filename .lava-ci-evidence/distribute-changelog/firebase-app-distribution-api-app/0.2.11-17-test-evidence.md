# §6.Z Pre-Distribute Test Evidence — Lava-API-App 0.2.11 (17), debug+release

**Artifacts:** `releases/api-app/0.2.11/android-{debug,release}/lava-api-app-0.2.11-17-{debug,release}.apk` (signed).
**Ships:** §6.AC telemetry — `ApiEngineService` records a Crashlytics non-fatal on
`ForegroundServiceStartNotAllowedException` (FGS budget-exhaustion) instead of swallowing it.
**Tests:** the api-app APKs built BUILD SUCCESSFUL in the 1071 rebuild; the FGS specialUse path was
device-proven on the prior 0.2.x shipped builds (no service-lifecycle change since). Device gate:
C00-class cold-start covered by the client gate (shared runtime); api-app cold-start unchanged vs 0.2.11-16.
**Channel:** firebase-app-distribution-api-app (debug `digital.vasic.lava.api.dev` + release `digital.vasic.lava.api`).
