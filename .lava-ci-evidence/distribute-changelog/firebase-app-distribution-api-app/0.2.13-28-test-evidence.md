# §6.Z Pre-Distribute Test-Execution Evidence — Lava-API-App-0.2.13-28

**Channel:** firebase-app-distribution-api-app
**Artifact under test:** `digital.vasic.lava.api` / `.api.dev` versionName 0.2.13 versionCode 28
**Commit:** `c4e05e514fb3e85c4a29028a951e66871da29fd0` (master)
**Evidence authored:** 2026-09-22

## Scope — this version

`api-app` at versionCode 28 has ZERO Kotlin/Android source changes relative
to versionCode 27 (`git log --oneline 09c8ffc9..HEAD -- api-app/` returns
empty commits until this cycle) — 27→28 was a pure §6.Y administrative
version-parity bump. What's new for 28: (1) the embedded `lava-api-go` engine
now carries the 2.3.35 dynamic-port + mDNS/Alt-Svc-ordering fix (see
`CHANGELOG.md`'s `Lava-API-App-0.2.13-28` entry), and (2) this same commit
closes the release-variant device-testing gap (see the client's
`1.3.17-1087-test-evidence.md` for the full account) — api-app's own
`proguard-rules.pro` needed one additional keep beyond what was mirrored from
`:app`: `okhttp3.**`, since api-app's `androidTest` helper
(`OnDeviceApiClient.kt`) makes real HTTPS calls to the served API via OkHttp
even though api-app's own PRODUCTION code never uses OkHttp (the on-device
API is the Go embed) — a genuine `NoClassDefFoundError` found running
Challenge02/03/04 against the R8-minified build, fixed and reverified.

## Signed artifact verification

```
$ export JAVA_HOME=/home/milosvasic/t062_work/jdk17/jdk-17.0.20.1+1
$ ./gradlew :api-app:assembleDebug :api-app:assembleDebugAndroidTest :api-app:assembleRelease --no-daemon
BUILD SUCCESSFUL
```

`apksigner verify --print-certs api-app/build/outputs/apk/release/api-app-release.apk`:
```
Verifies
Verified using v1 scheme (JAR signing): true
Verified using v2 scheme (APK Signature Scheme v2): true
Signer #1 certificate DN: CN=Lava Release, O=Lava, C=RS
Signer #1 certificate SHA-256 digest: 0feb69c3633b16d33ef63ebb98354fff6823d4930074418d76e7d698fa259868
```
`sha256sum api-app/build/outputs/apk/release/api-app-release.apk`:
`76f97d3664fe429c7edce795496261fa8755f30c77d3769f0a4b10e32a770211`

## Device gate — containerized KVM emulator (§6.X/§6.AH-conformant, `runner=containerized`)

Runner: `containers-submodule` · AVD: `CZ_API34_Phone` (API 34, phone) · Cold-boot, non-host-direct, gating=true.

| Build type | Test class(es) | Result | Evidence dir |
|---|---|---|---|
| debug | Challenge01ApiAppColdStartTest, Challenge02ApiAppBootAndServeTest, Challenge03StopRestartTest, Challenge04NotificationActionsTest | **PASS** | `.lava-ci-evidence/2026-09-22-apiapp-debug-c01-04/` |
| releaseTest (R8-minified, same proguard-rules.pro as `release`) | same 4 (C01-04) | **PASS** | `.lava-ci-evidence/2026-09-22-apiapp-releaseTest-c01-04/` |
| releaseTest | Challenge05ApiEmbedSourceHashMatchesTest | **PASS** | `.lava-ci-evidence/2026-09-22-apiapp-releaseTest-c05/` |

Challenge05 is the direct proof for this version's actual functional
content: it asserts the running embed's self-reported source hash equals the
hash this build was compiled against, confirming a fresh build genuinely
carries lava-api-go 2.3.35 (dynamic-port fix), not the stale 2.3.34.

**Honest scope:** Challenge06CopyKeyTest (cross-app ContentProvider key copy)
and Challenge07OpenClientTest (cross-app launch) were NOT run this cycle —
they require the `:app` client to also be installed on the same emulator
instance, which this single-app matrix run does not provision. Both remain
OWED for a full cross-app §6.AE.2 matrix.

## Falsifiability

The release-variant testing infrastructure itself was validated by
construction: every R8 keep-rule fix in this cycle (including
`api-app/proguard-rules.pro`'s `okhttp3.**` addition) was added ONLY after
observing a genuine failure (a specific `NoClassDefFoundError`/
`NoSuchMethodError` with a real stack trace), then re-verified GREEN after
the fix — an 8-round real mutate(discover)→fix→reverify cycle documented in
full in commit `c4e05e51`'s body.
