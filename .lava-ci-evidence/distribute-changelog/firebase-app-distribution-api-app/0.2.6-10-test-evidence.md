# §6.Z device-gate evidence — Lava-API-App 0.2.6 (10)

**Commit:** `2ea37a02` (code-bearing: `9ae9ab90` router fix — the embedded `.so` source).
**Device:** Genymotion Pixel 9 / API 35 / arm64 (`127.0.0.1:6555`, §6.AH VM path,
§6.AG Containers-submodule-driven via `scripts/run-release-canary.sh`).
**Artifact under test:** `api-app/build/outputs/apk/debug/api-app-debug.apk`
(sha256 `0c803fde0ea67bd1bb62183138fc19015e7fa0b97c8d2502a77fe187afbcd662`; embeds the
router-fixed `.so`, api-source.hash `dbe891a4…` matches committed Go source).
**Date:** 2026-06-13.

## Cold-start canary (verbatim, sanctioned wrapper)

```
launcher: digital.vasic.lava.api.dev/lava.api.app.MainActivity
LaunchState: COLD
TotalTime: 3218
pid_after_launch: 8499
resumed_activity: topResumedActivity=ActivityRecord{… digital.vasic.lava.api.dev/lava.api.app.MainActivity …}
fatal_lines: <none>
==> RELEASE CANARY PASS — process alive (pid 8499), no fatal
```

Evidence dir: `.lava-ci-evidence/release-canary/api-app-0.2.6-10-debug`.

## Server-side fix proven off-device (the load-bearing proof for THIS change)

The 0.2.6-10 change is the public-`/providers` router fix in the embedded `.so`. The
embed uses the EXACT `router.Build` construction. The real-binary e2e
`TestProviders_PublicCatalogue_PerProviderStillGated`
(`lava-api-go/tests/contract/providers_public_auth_boundary_test.go`, commit `132d1b07`)
boots that same `router.Build` with the full production auth chain + real native
registry and proves: unauth `GET /providers` → 200 + catalogue; unauth
`GET /v1/rutracker/search` → 401; authed `/v1/…` → crosses the gate. Falsifiability
re-performed by the main stream: gate `/providers` → sub-test A 401. This is the
honest on-the-wire proof that the embedded api-app now serves the public catalogue
while keeping per-provider ops gated.

## §6.Z compliance

- EXACT artifact EXECUTED on device (not source-compiled): ✅ cold-start canary
- Cold-start survival (load-bearing): ✅ pid alive, 0 fatal
- Server fix proven against the real `router.Build` (the embed's construction): ✅
- Runner = Containers-submodule genymotion CLI (§6.AH/§6.AG): ✅
- Stage: §6.AA **stage-1 (debug)** evidence.

## Note (test-environment)

The on-device install required first uninstalling a prior production
`digital.vasic.lava.api` build squatting the shared custom permission
`digital.vasic.lava.permission.READ_API_KEY` (cross-variant permission redeclare) —
a VM housekeeping step, not a code issue. After cleanup the `.dev` debug installed +
launched cleanly.
