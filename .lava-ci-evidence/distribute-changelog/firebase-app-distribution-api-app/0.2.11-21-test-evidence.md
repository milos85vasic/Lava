# §6.Z Pre-Distribute Test Evidence — Lava-API-App 0.2.11 (21), debug+release

**Commit (SHA being distributed):** `40301014f73d8f40ff01d65cf089a57786c5bd9e`
**Artifacts (aapt-verified versionCode 21):**
- `releases/api-app/0.2.11/android-debug/digital.vasic.lava.api-0.2.11-debug.apk` — `package='digital.vasic.lava.api.dev' versionCode='21'`
- `releases/api-app/0.2.11/android-release/digital.vasic.lava.api-0.2.11-release.apk` — `package='digital.vasic.lava.api' versionCode='21'`
Built locally (signed) from `40301014` via `build_and_release.sh` (fresh auth: pepper rotated, client-name `android-1.3.11-1075`).

## §6.Z cold-start gate — EXECUTED on a real containerized-KVM emulator (thinker)
| Check | Verdict | boot_s | runner / runtime | image |
|---|---|---|---|---|
| **api-app cold-start** (`Challenge01ApiAppColdStartTest`-equivalent) | **PASS** | 24.33 | containerized (accel=kvm) / podman | `localhost/lava-android-emulator:api34-x86_64` |

- AVD `default` / API 34 / phone, `all_passed=true`, `gating=true`.
- Genuine cold start: `pm clear` → `am start -W` → **Status: ok, LaunchState: COLD, TotalTime 2554ms**.
- **User-visible assertions:** `topResumedActivity = digital.vasic.lava.api.dev/lava.api.app.MainActivity`; process alive (PID 3351); **zero FATAL EXCEPTION** for the package in logcat; screenshot shows the rendered launcher ("Lava API DEV" / "Stopped" / Start·Stop·Restart / "Open Lava client").
- Attestation: `.lava-ci-evidence/1075-apiapp-gate/real-device-verification.json` (finished 2026-06-25T15:23:11Z); full logcat (1.88MB) + screenshot.png alongside.

## Honest notes (real findings, not bluffs)
1. The dedicated `Challenge01ApiAppColdStartTest` could NOT run because `:api-app:assembleDebugAndroidTest` is blocked by an okhttp version conflict (`4.12.0` androidTest dep vs `strictly 3.12.13` consistent-resolution pin) — a tracked workable item. The gate fell back to the install + `am start` + logcat-fatal-scan + screenshot path (still real on-device evidence, replicating the Containers `containerized.go` boot recipe with `--device /dev/kvm`).
2. The api-go `:core:apiengine:buildCshared` step defaulted `ANDROID_NDK_HOME` to a macOS path; resolved on the Linux gate host via env override (no source edit) — a tracked build-config workable item.

## What ships in 21
Version-parity bump alongside the client 1075 cycle; no api-app source change. versionName held (0.2.11).

**§6.AA two-stage:** this evidence gates `--app api-app --debug-only` (stage 1). Operator verifies the Firebase-distributed debug build, then `--app api-app --release-only` (stage 2) on the same SHA.
