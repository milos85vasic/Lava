# §6.Z test-evidence — Lava-API-App 0.2.0-4 (debug + release)

- **Date:** 2026-06-04
- **Commit SHA (HEAD):** `848ce20c` (Bug-B fix `dc547861`; on-device verification `848ce20c`).
- **Device for on-device checks:** Samsung Galaxy S23 Ultra `R5CW33CBVQV`, Android 16 / API 36.
- **Artifacts (aapt2-confirmed `package='digital.vasic.lava.api' versionCode='4' versionName='0.2.0'`, signed):**
  - debug   `releases/api-app/0.2.0/android-debug/digital.vasic.lava.api-0.2.0-debug.apk` — distributed (Firebase release `4hd1ci7i9hovg`).
  - release `releases/api-app/0.2.0/android-release/digital.vasic.lava.api-0.2.0-release.apk` — sha256 `693ca77c2fcea1288eaa2f1169cf23aa…`, R8-minified, 164 MB.

## Executed tests + results (this cycle, bounded `--no-daemon --max-workers=2`, BUILD SUCCESSFUL)

- `api-app.control.ApiEngineControllerTest` — **7/7, 0 fail**. Incl. the Bug-B regression
  *"start is idempotent — redundant start does not re-bind or re-advertise"* (asserts
  `registerCalls == 1` after two `start()` calls). Falsifiability: disabling the idempotency
  guard → FAILED (registerCalls==2), reverted → PASS.

## On-device verification (carried forward)

- Bug B: `am force-stop` → start → start-again produced NO real bind error
  (`address already in use`/`EADDRINUSE`/`listen…bind`); engine still serves
  `https://127.0.0.1:8443/health` → **200**. Evidence: `.lava-ci-evidence/app-linking/ondevice-dc547861/`
  (`bugB-doublestart-binderrors.txt` empty, `bugB-engine-still-serving.txt` health=200).
- api-app cold-start survives; `EXTRA_START_API` auto-starts a serving engine (foreground
  service + HTTPS:8443); the signature key-provider denies the shell user (§6.H).

## Notes

- The api-app is the SERVER side of the auth scheme — it does NOT embed the client
  AuthInterceptor pepper, so the §RELEASE-ROTATION Gates 4+5 are inapplicable (skipped by
  `firebase-distribute.sh` for `--app api-app`). No cloud-auth-lag caveat applies to it.
- **Deferred to operator post-distribute testing (S23 offline this session):** cold-start of
  the release-variant APK on the device. The Bug-B fix is unit-proven (above) and the
  on-device behavior was verified on the debug build of the same SHA.
