# Lava-API-App-0.2.11-24

**Previous published:** Lava-API-App-0.2.11-23.

Embedded-engine update: lava-api-go `proxy.go` gains upstream-proxy support → `api-source.hash` recomputed to `eed490bc…`; the cshared `liblavaapi.so` is rebuilt against it. §6.Y versionCode bump 23→24; versionName held 0.2.11 (diagnostics-only app, no user-facing UI change). See CHANGELOG.md for full detail.
