# Lava-API-App-0.1.2-3 — 2026-06-03 (DEV launcher background → green, like the client DEV)

**Previous published:** Lava-API-App-0.1.1-2 (debug, 2026-06-03).

## What's new for testers

- **Debug ("Lava API DEV") launcher background is now GREEN (#00FF00)** — matching the
  Lava client DEV convention, so the debug build is visually distinct from the release at
  a glance. Implemented exactly like the client: a `src/debug` adaptive-icon override whose
  `<background>` points at a green vector (`@drawable/ic_launcher_background`, fillColor
  #00FF00), foreground = the Lava logo.
- **Release ("Lava API") keeps the current RED background** (the main `mipmap` PNG) —
  unchanged. Prod stays red; only DEV turns green.
- Icon background hint only; no functional/server change vs 0.1.1-2.

## §6.Z device-gate evidence (GREEN on real device)

- **Debug C01–C04 EXECUTED green ×2 (deterministic)** on a real **Samsung Galaxy S23 Ultra (SM-S918B, Android 16/SDK 36)** — the operator-provided physical device (authorized 2026-06-03; §6.AH governs emulators, a physical device is out of its scope; §6.AG's "never *silently* redirect" is satisfied — explicit operator direction). Evidence: `.lava-ci-evidence/2026-06-03-api-app-0.1.2-3-gate/device-SM-S918B-cleanstate{,-run2}/`.
- C02 was hardened to assert the auth-gate VERDICT via a short-read probe (no rutracker-reachability dependency) — falsifiable: a gate that wrongly rejects the real key still fast-401s. C01's earlier flake was prior-run state-leakage (clean `pm clear` → green ×2).
- **RELEASE cold-start canary PASS** on the same S23 Ultra: R8-minified release APK installs + MainActivity Displayed +409ms, fatal=0, no crash (renamed icon resolves). Evidence: `.lava-ci-evidence/2026-06-03-api-app-0.1.2-3-release-canary-device/canary.json`.
- Launcher icon (debug green #00FF00 / release red) verified by operator on-device visual check of the Firebase debug build.
