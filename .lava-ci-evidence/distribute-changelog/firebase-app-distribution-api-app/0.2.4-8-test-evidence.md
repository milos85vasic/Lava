# §6.Z pre-distribute device-gate evidence — Lava-API-App 0.2.4 (8)

**Commit SHA built from:** `34c06119` (HEAD; auth/.env not committed).
**Device:** Genymotion Google Pixel 9, Android API 35, arm64-v8a, serial 127.0.0.1:6555 (§6.AH VM path).
**Timestamp:** 2026-06-13.

## §6.Z gate (api-app = the on-device API server app; the canary is the cold-start gate)
- **Release R8 cold-start canary** (`run-release-canary.sh`) on the EXACT release APK
  `releases/api-app/0.2.4/android-release/digital.vasic.lava.api-0.2.4-release.apk`:
  **PASS** — cold-launch reaches `lava.api.app.MainActivity` (TotalTime 1390ms),
  process alive (pid 5506), **no FATAL** in 25s observation. This confirms the NEW
  Crashlytics wiring (commit 07f83eef) + the 7 embedded curated providers do NOT
  crash the api-app on launch.
- The embedded api-go binary (2.3.26) + image built clean in the same build_and_release run.

This is the §6.Z evidence required before `firebase-distribute.sh --app api-app` operates on 0.2.4-8.
