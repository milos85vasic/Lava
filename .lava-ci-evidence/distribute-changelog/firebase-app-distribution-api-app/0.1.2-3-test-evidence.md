# §6.Z test-evidence — Lava-API-App-0.1.2-3 (DEBUG stage; green DEV bg; release HELD)

- **Artifact:** `releases/api-app/0.1.2/android-debug/digital.vasic.lava.api-0.1.2-debug.apk`
- **Evidence written:** 2026-06-03T06-31-02Z (atop commit `45e72485c95c8a41f0e18e8be8e17a18f121ca1a` + uncommitted green-icon edits, committed right after)

## Green DEV background — VERIFIED via aapt2 (not assumed)

The debug APK's adaptive-icon background drawable was decoded:
`aapt2 dump xmltree api-app-debug.apk --file res/drawable/ic_launcher_background.xml`
→ `android:fillColor=#ff00ff00` (opaque #00FF00 GREEN) on a 108x108 full-bleed path.
The debug `src/debug/res/mipmap-anydpi-v26/ic_launcher.xml` override points
`<background>` at this green `@drawable/ic_launcher_background` (foreground = Lava logo).
The RELEASE APK keeps the main red PNG background (R8 resource-renamed; debug override does
NOT apply to release). Debug label "Lava API DEV"; release label "Lava API".

## Debug runtime — TRANSFERRED green ×2

api-app RUNTIME code git-diff-unchanged since item-1's green ×2 gate (60d7e0d6); 0.1.2-3
changes only the debug icon background + version. Item-1 attestations:
`.lava-ci-evidence/phase-e-api-app/2026-06-02T11-3{2,5}-*-gate/` (C01–C04, all_passed:true).

## Fresh gate + RELEASE canary — infra-BLOCKED

Emulator adbd-offline boot wedge persists (~9 attempts; the new Containers runner reap is
macOS-broken via /proc). Forensics:
`.lava-ci-evidence/sixth-law-incidents/2026-06-03-emulator-boot-offline.json`.
RELEASE HELD per §6.Z (R8 variant needs a cold-start canary).
