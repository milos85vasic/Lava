# §6.Z device-gate evidence — Lava-API-App 0.2.8 (12)

**Commit:** `9751e1ef` (FGS fix `ed03cac2`; api-app rebuilt with the Defect-B telemetry
google-services correction — gitignored, on-disk).
**Device:** Genymotion Pixel 9 / API 35 / arm64 (`127.0.0.1:6555`, §6.AH VM path).
**Artifacts:** `api-app-debug.apk` (190 MB) + `api-app-release.apk` (167 MB).
**Date:** 2026-06-14.

## FGS specialUse cold-start + foreground-promotion proof (the load-bearing check)

The dataSync→specialUse fix is verified by the OS-reported foreground type, not "no crash":

```
    startForegroundCount=1
    infoAllowStartForeground=[callingPackage: digital.vasic.lava.api; callingUid: 10175; uidState: TOP ; uidBFSL: [BFSL]; intent: Intent { act=lava.api.app.action.START cmp=digital.vasic.lava.api/lava.api.app.service.ApiEngineService }; code:PROC_STATE_TOP; tempAllowListReason:<null>; targetSdkVersion:35; callerTargetSdkVersion:35; startForegroundCount:0; bindFromPackage:null: isBindService:false]; startForegroundDelayMs:64472
    isForeground=true foregroundId=6778 types=0x40000000 foregroundNoti=Notification(channel=lava.api.app.server shortcut=null contentView=null vibrate=null sound=null defaults=0 flags=ONGOING_EVENT|FOREGROUND_SERVICE color=0x00000000 actions=2 vis=PRIVATE)
```
`types=0x40000000` == `FOREGROUND_SERVICE_TYPE_SPECIAL_USE` (1073741824). isForeground=true,
startForegroundCount=1, no ForegroundServiceStartNotAllowed / DidNotStopInTime in logcat.
Engine confirmed running: `liblavaapi_jni.so` loaded + mDNS `_lava-api._tcp` "Lava API" :8443
{engine=go, version=2.3.30}. Full verdict: `.lava-ci-evidence/release-canary/0.2.8-12-apiapp-fgs/FGS-VERDICT.md`.

## Release cold-start canary
```
package: digital.vasic.lava.api
apk: releases/api-app/0.2.8/android-release/lava-api-app-0.2.8-12-release.apk
resumed: lava.api.app.MainActivity  pid_after_launch: 14210  fatal_lines: <none>
==> RELEASE CANARY PASS
```

## Defect-B telemetry attribution (verified)
Firebase SessionLifecycleService runs under `digital.vasic.lava.api` (the api-app's own
package) — confirming the corrected google-services maps to the .api Firebase app, so
api-app crashes now report to the api-app dashboard, not the client.

## §6.Z compliance
- Release artifact EXECUTED on device (not source-compiled): ✅
- FGS specialUse foreground-promotion proven by OS type bitmask: ✅
- No FGS exception class (the dataSync crash) reproduced: ✅
- Paired with client 1.3.8-1065.
