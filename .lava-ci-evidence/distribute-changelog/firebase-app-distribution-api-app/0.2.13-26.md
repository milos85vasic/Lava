# Lava-API-App-0.2.13-26 — embedded engine: Cloudflare-challenge classification + realistic outbound headers

**Previous published:** Lava-API-App-0.2.12-25.

- Embedded `lava-api-go` engine (`liblavaapi.so`, rebuilt for all 3 ABIs) now correctly classifies a Cloudflare bot-mitigation challenge on RuTracker login as a distinct 503 (with non-fatal telemetry) instead of a misleading generic 502, and every outbound RuTracker request now carries realistic browser headers.
- Real root-cause investigation performed on a device-gate test failure this cycle (`Challenge06CopyKeyTest`) traced it to a stale 13-hour-old emulator container, not a regression — confirmed clean PASS on a fresh cold-boot after reproducing, investigating, and ruling out this cycle's actual changes as the cause.

## Build info
- App versionName: 0.2.13
- App versionCode: 26
- Distribution channel: firebase-app-distribution-api-app

## Evidence
- `.lava-ci-evidence/distribute-changelog/firebase-app-distribution-api-app/0.2.13-26-test-evidence.md`
