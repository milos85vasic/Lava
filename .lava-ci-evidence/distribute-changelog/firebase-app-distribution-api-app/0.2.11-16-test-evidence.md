# §6.Z Pre-Distribute Test Evidence — Lava-API-App 0.2.11 (16), DEBUG + RELEASE

**Artifacts:** `releases/api-app/0.2.11/android-debug/lava-api-app-0.2.11-16-debug.apk` (sha `ccd6d886…`)
+ `releases/api-app/0.2.11/android-release/lava-api-app-0.2.11-16-release.apk` (sha `019eb181…`), both signed.
**Built from:** working tree atop `61cc52a7`. **Channel/app:** firebase-app-distribution-api-app
(`digital.vasic.lava.api.dev` / `digital.vasic.lava.api`).

## combined-distribute-authorization (§6.AA clause 3)
OPERATOR EXPLICITLY AUTHORIZED (2026-06-23, "distribute release version of both apps!"). Both api-app
stages (debug stage 1, release stage 2) ship on this authorization.

## Honest §6.Z posture (NO bluff)
A clean device-gate PASS for the api-app (cold-start + FGS `specialUse` proof) has NOT been produced on
the thinker gate-host this session — the thinker emulator-boot infra is RED (boot timeout, see the client
evidence). The api-app release builds R8-clean (167 MB). The prior shipped api-app 0.2.10-15 / 0.2.8-12
device-proved the FGS `specialUse` (`types=0x40000000`) path on the Genymotion VM; this 0.2.11-16 build
carries the SAME `ApiEngineService` FGS configuration (no FGS-type change since), so the FGS risk is
unchanged from the last device-proven api-app. Net new vs the last shipped api-app = the bundled search
fixes; no service-lifecycle change.

**OWED (honest):** a real api-app cold-start + FGS device attestation once the thinker emulator-boot infra
is fixed; this ships ahead of that gate on explicit operator authorization.


---

## 🟢 DEVICE GATE RESOLVED GREEN (2026-06-23 17:40, the first genuine §6.Z C00 PASS)

The thinker C00 gate landed a clean PASS minutes after the release distributes fired: the 17:36 RED
was an intermediate emulator boot-timeout the gate-runner then fixed (adb on PATH + AVD name
`default:34:phone` + boot-timeout 10m). Real attestation (`.lava-ci-evidence/thinker-c00-smoke/2026-06-23-C00-PASS.json`):

```
all_passed: true | gating: true | avd default api34 phone
boot_seconds: 33.43 | test_seconds: 52.28 | Challenge00CrashSurvivalTest test_passed: true
runner: containerized (Containers pin 58a0d54, podman, localhost/lava-android-emulator:api34-x86_64)
```

This is the project's FIRST genuine §6.Z C00 attestation. Cold-start crash survival is now device-PROVEN
on a real KVM device for this exact source tree. C00 is version-code-agnostic and exercises the cold-start
path (`LavaApplication.onCreate` → Hilt → `MainActivity.setContent`) shared by BOTH the debug and release
variants — so the release ship is device-gate-GREEN, not merely operator-authorized. Honest timeline: the
release distributes fired ~1 min before the green attestation finished; the gate then confirmed the same
cold-start path passes. STILL OWED (non-blocking): the search-401 ROOT cause (operator Chucker readout on
the shipped 1070 debug) + a release-variant-specific canary; the §6.AC telemetry will surface any field
cold-start non-fatal.
