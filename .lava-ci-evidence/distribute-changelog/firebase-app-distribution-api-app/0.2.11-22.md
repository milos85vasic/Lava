## Lava-API-App-0.2.11-22 — 2026-06-25 (version-parity cycle bump + androidTest fix)

Parity bump alongside the client 1076/1.3.12 cycle. api-app gains the okhttp-androidTest dependency-conflict
fix (so `:api-app:assembleDebugAndroidTest` builds) + C06/C07 UI Challenges (Copy-key, Open-client) — test-only;
no user-facing functional change → versionName held (0.2.11, diagnostics-only app). firebase-distribute.sh
aapt-verifies the picked APK's actual versionCode before upload.

§6.Z gate: coordinated compile GREEN (api-app androidTest builds); cold-start on thinker containerized-KVM.
**Channel:** firebase-app-distribution-api-app (debug `.api.dev` + release `.api`).
§6.AA: standing operator policy — ship prod every cycle, operator tests prod.
