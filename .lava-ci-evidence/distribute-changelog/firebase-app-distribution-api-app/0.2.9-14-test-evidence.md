# §6.Z device-gate evidence — Lava-API-App 0.2.9 (14)

**Commit:** `78cd48ea` (current HEAD at gate time; key-handoff fix base `462f8359`,
search-cycle landing `646474c0`).
**Device:** Genymotion Pixel 9 / API 35 / arm64 (`127.0.0.1:6555`, §6.AH VM path,
§6.AG Containers-submodule-driven).
**Artifacts:** `lava-api-app-0.2.9-14-release.apk` (167 MB R8) embedding the
`ApiKeyProvider` lazy key/port resolution (the on-device-API half of the search fix).
**Date:** 2026-06-14.

## R8 release cold-start canary

```
package: digital.vasic.lava.api
apk: releases/api-app/0.2.9/android-release/lava-api-app-0.2.9-14-release.apk
apk_sha256: c67a632a1cf158951e7058f910481223139861b911d674c14ae255b1ae34c0e9
LaunchState: COLD   TotalTime: 2639
pid_after_launch: 29440   fatal_lines: <none>
==> RELEASE CANARY PASS — process alive, no fatal
```
Evidence: `.lava-ci-evidence/release-canary/0.2.9-14-apiapp/`.

## FGS specialUse foreground-promotion proof (release variant)

Engine started via the app's own "Start" control (UI = "Running" after tap); the OS-reported
foreground type confirms specialUse is intact:

```
startForegroundCount=1
isForeground=true foregroundId=6778 types=0x40000000 foregroundNoti=Notification(channel=lava.api.app.server … flags=ONGOING_EVENT|FOREGROUND_SERVICE …)
```
`types=0x40000000` == `FOREGROUND_SERVICE_TYPE_SPECIAL_USE` (1073741824). No
`ForegroundServiceStartNotAllowed` / `DidNotStopInTime`. Engine confirmed serving:
`_lava-api._tcp` "Lava API" advertised; loopback `:8443` LISTEN with an ESTABLISHED client
connection observed during the matched-pair search.

## Key-handoff fix — device-confirmed via the matched-pair search

The `ApiKeyProvider` lazy-resolution fix is the on-device-API half of the end-to-end search
fix. Its effect is proven by the RELEASE client (READ_API_KEY granted=true) completing a
live "prince" search that rendered real Internet Archive results — see the client channel's
`1.3.9-1066-test-evidence.md`. The §6.J falsifiability rehearsal of the fix
(`ApiKeyProviderRealHolderTest`: caching pre-fix → empty cursor `AssertionError expected:<1>
but was:<0>`; lazy fix → PASS) is recorded at
`.lava-ci-evidence/search-verification/2026-06-14-keyhandoff-fix-verify.md`.

## §6.Z compliance
- R8 release artifact EXECUTED on device (not source-compiled): ✅
- FGS specialUse foreground-promotion proven by OS type bitmask: ✅
- Key handoff device-verified via the matched release-pair search (real results rendered): ✅
- Paired with client 1.3.9-1066.
- READY for §6.AA two-stage distribute.
