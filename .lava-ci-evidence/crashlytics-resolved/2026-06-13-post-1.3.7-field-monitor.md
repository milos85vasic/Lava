# Post-1.3.7 Field Monitor — 2026-06-13 (tonight's double-release crash watch)

Real Firebase Crashlytics data pulled via the Firebase MCP (project
`lava-vasic-digital`). Pull timestamp: **2026-06-13 (evening, post-distribute)**.
Window queried: **2026-06-12 00:00Z → 2026-06-13 23:59Z** (2 days), widened to
**2026-06-07 → 2026-06-13** (7 days) on the client release for version-coverage
confirmation. ANALYSIS + EVIDENCE ONLY — no production code changed.

Per §11.4.6: proven facts stated as facts; anything unconfirmed marked
`UNCONFIRMED:` / `PENDING_FORENSICS:`.

## Verdict

**FIELD CLEAN — NO DATA YET for the tonight-shipped versions.**

The two client releases (**1.3.6 / 1063**, **1.3.7 / 1064**) and the two api-app
releases (**0.2.6 / 10**, **0.2.7 / 11**) have produced **ZERO** crash / ANR /
non-fatal events in the field. None of those four version codes appears anywhere in
the Crashlytics version reports. This is the EXPECTED state this soon after a
distribute — testers have not yet installed and run the new builds. It is NOT a gap.

No new crash was introduced by tonight's releases (nothing observed; the corollary
that "no new crash exists" is **UNCONFIRMED** until testers actually run the builds —
absence of field events ≠ proven stability).

## What we are watching for (the only thing that would be a blocker)

Any issue whose first-seen OR last-seen version is **1.3.6 / 1064 / 1.3.7 / 0.2.6 /
0.2.7 / 11** → a NEW crash from tonight. **None found.** The new version codes are not
even present as zero-event rows in the `topVersions` report (Crashlytics has not yet
ingested a single session from them).

## Per-variant real data

### client RELEASE (`digital.vasic.lava.client` — `…456475e2…`)

`topVersions` (FATAL+ANR+NON_FATAL), 7-day window 2026-06-07 → 2026-06-13:

| Version | Events |
|---------|--------|
| 1.3.5 (1062) | 3 |
| 1.3.4 (1061) | 3 |
| 1.3.3 (1060) | 1 |
| 1.3.0 (1057) … 1.2.3 (1023) | 0 |
| **1.3.6 (1063)** | **ABSENT — not in report (0 sessions ingested)** |
| **1.3.7 (1064)** | **ABSENT — not in report (0 sessions ingested)** |

`topIssues` (2-day window) returned exactly **one** issue:

| Issue id | Title / top frame | Events/Users | first→last ver | State | Class |
|----------|-------------------|--------------|----------------|-------|-------|
| `47b000d54ff647802df7577ca12a1741` | `ProviderCatalogRepository$fetchProviders` — `IllegalStateException: provider discovery failed: HTTP 401 for https://192.168.0.107:8443/providers` | 6 / 1 | **1.3.4 → 1.3.5** | OPEN | **OLD/known — Defect-B auth-gate 401.** Not on 1.3.6/1.3.7. |

ANRs: NONE. FATALs in the 2-day window: NONE (the 2 OLD FATALs from baseline —
`40a62f97` 1.2.19, `39469d3b` 1.2.21 — produced 0 events in-window).

**Sample-event verification of the one present issue** (3 events pulled via
`crashlytics_list_events`): ALL three are `version: 1.3.5 (1062)`, `build_type:
release`, device **samsung SM-S918B (Galaxy S23 Ultra), Android 16**, ARM64,
`eventTime` 2026-06-13 **17:50** (before tonight's distribute), `customKeys.error =
provider_catalog_fetch_failed`, blameFrame `ProviderCatalogRepository.kt:112`. Issue
`get_issue` reports `firstSeenVersion: 1.3.4`, `lastSeenVersion: 1.3.5` — **never seen
on 1.3.6 or 1.3.7.** This is the same already-known Defect-B 401 from the baseline
snapshot, with one additional event-day on 1.3.5, NOT a tonight regression.

Source confirmation (analysis-only, no edit): `core/data/.../ProviderCatalogRepository.kt`
line 112 is exactly `error("provider discovery failed: HTTP ${response.code} for $url")`
inside the `if (!response.isSuccessful)` branch — blame frame and source agree.

### client DEBUG (`digital.vasic.lava.client.dev` — `…54ca2ca3…`)
`topIssues` returned **no results**. Debug builds are not distributed to testers —
EXPECTED, not a gap.

### api-app RELEASE (`digital.vasic.lava.api` — `…d57b960e…`)
**HTTP 404** ("Requested entity was not found") — Crashlytics has NO data for this app
yet. Same as baseline: api-app Crashlytics was only recently wired and no
Crashlytics-instrumented + field-run api-app build has reported. The tonight releases
(0.2.6/10, 0.2.7/11) have not produced events. HONEST, not a gap.

### api-app DEBUG (`digital.vasic.lava.api.dev` — `…2932451e…`)
**HTTP 404** — same as api-app release. No Crashlytics data yet. HONEST, not a gap.

## Cross-check against baseline (`2026-06-13-crashlytics-fleet-snapshot.md`)

| Issue | Baseline | Now | Delta |
|-------|----------|-----|-------|
| `47b000d5` (Defect-B 401) | NEW/actionable, 3/1, 1.3.4 | 6/1, last-seen 1.3.5 | Same issue; +3 events on 1.3.5; **still NOT on 1.3.6/1.3.7** |
| `40a62f97` FATAL (layer-list) | OLD/fixed, 1.2.19 | 0 events in-window | Unchanged — dormant |
| `39469d3b` FATAL (okhttp url) | OLD/fixed, 1.2.21 | 0 events in-window | Unchanged — dormant |
| 4 other non-fatals (jobcancel, parseuserid, certpath, sse-host) | OLD/fixed-in-code | not in 2-day topIssues | Unchanged — not recurring |
| api-app (both) | 404 / no data | 404 / no data | Unchanged |

**No NEW issue appeared relative to baseline.** The only live issue is the same
already-known Defect-B 401, which has NOT migrated onto any tonight-shipped version.

## Honest disposition

- **0 NEW crashes/ANRs/non-fatals from 1.3.6, 1.3.7, 0.2.6, or 0.2.7.** Those four
  version codes have produced **zero field events** — Crashlytics has not ingested a
  session from them yet.
- The single live issue (`47b000d5`) is the OLD/known Defect-B auth-gate 401, last seen
  on **1.3.5**, confirmed by 3 sample events + `get_issue` first/last-seen. NOT a
  tonight regression.
- api-app variants: HTTP 404 = no Crashlytics data yet (freshly wired). HONEST.
- **No false "all clear":** the lack of 1.3.6/1.3.7/0.2.6/0.2.7 data is reported as the
  expected too-soon-after-distribute state, not as proof of stability. Stability of the
  tonight builds remains **UNCONFIRMED** until testers run them and the surface reports.

**Recommended follow-up:** re-run this field monitor in 12–48h once testers have
installed the new builds; only then will 1.3.6/1.3.7/0.2.6/0.2.7 stability be field-proven.
