# Crashlytics Field-Monitoring Report — 1075 cycle

§11.4.152 / §6.O continuous field monitoring of the 2026-06-25 distribution.

- **Date:** 2026-06-25
- **Distribution under watch:** client `digital.vasic.lava.client.dev` **1.3.11 (1075)** + api-app `digital.vasic.lava.api.dev` **0.2.11 (21)**
- **Firebase project:** `lava-vasic-digital` (project number 815513478335)
- **Query window:** 2026-06-11T00:00:00Z → 2026-06-25T23:59:59Z (14 days)
- **Error types queried:** FATAL, NON_FATAL, ANR
- **Data source:** Firebase Crashlytics MCP (`crashlytics_get_report topIssues`, `crashlytics_get_issue`, `crashlytics_list_events`, `crashlytics_list_notes`) — **real API data, no fabrication.**

## App-ID resolution (from `.env` / `firebase_list_apps`)

| Role | App ID | 14-day result |
|---|---|---|
| Client release (`digital.vasic.lava.client`) | `1:815513478335:android:456475e2ef4039d8cfd20a` | **8 issues** (see §A) — NOTE: stream is co-mingled with api-app events |
| Client dev (`digital.vasic.lava.client.dev`) | `1:815513478335:android:54ca2ca31e6c4f42cfd20a` | **1 issue** (LVA-008 nav crash, FRESH on 1075) |
| API-app release (`digital.vasic.lava.api`) | `1:815513478335:android:d57b960e955645f6cfd20a` | **0 events** (report: "contains no results") |
| API-app dev (`digital.vasic.lava.api.dev`) | `1:815513478335:android:2932451e07ca80a7cfd20a` | **0 events** (report: "contains no results") |

> Honest empty-result note: both dedicated api-app registrations returned `This report response contains no results` for the full 14-day FATAL+NON_FATAL+ANR window. This is a genuine API zero, not an assumed "no crashes". See §D for why api-app crashes are nonetheless present in the field (they land under the client-release registration).

---

## §A — Client RELEASE app (`...456475e2`) — 8 open issues

App-ID-level observation: this registration's event stream contains BOTH client (`lava.*`, package `digital.vasic.lava.client`) AND api-app (`lava.api.app.*`, package `digital.vasic.lava.api`, version 0.2.x) events. Triage verdicts below account for this.

| # | Issue | Type | Events | Users | First→Last ver | Verdict |
|---|---|---|---|---|---|---|
| 1 | `ProviderCatalogRepository$fetchProviders$2` — IllegalStateException `provider discovery failed: HTTP 401` | NON_FATAL | 6 | 1 | 1.3.4→1.3.5 | **pre-existing / older build** — last seen 1.3.5, not on 1075. 401 against local API `/providers`. Pre-existing-retired-build; monitor for recurrence on 1075. |
| 2 | `CredentialsKeyHolder.require` — IllegalStateException "credentials key holder is locked" | FATAL | 5 | 1 | 1.3.10→1.3.10 | **known-tracked, fix landed** — operator note (2026-06-24): fix in `CredentialsEntryRepositoryImpl.observe()` runCatching wrap, ships 1.3.11-1073. §6.O log `2026-06-24-credentials-keyholder-locked.md`. Challenge C47 OWED before close-mark. Verify no recurrence on 1075. |
| 3 | `ApiEngineService.onStartCommand` — ForegroundServiceStartNotAllowedException (dataSync time limit) | FATAL | 5 | 1 | 0.2.6→0.2.6 | **api-app issue, old build** (0.2.6, current is 0.2.11/21). FGS dataSync time-limit. NEW-NEEDS-TRIAGE for api-app: is the 0.2.6 FGS-timeout class still present in 0.2.11? Not seen on 21 in window, but no api-app data is flowing to its own registration (§D) — triage owed. |
| 4 | `InlineClassHelperKt.throwIllegalStateException` — "Vertically scrollable component measured with infinity max height" (nested LazyColumn/Column.verticalScroll) | FATAL | 2 | 1 | 1.2.3→**1.3.10** | **known class (§6.Q nested-scroll), but last-seen 1.3.10** — a §6.Q-antipattern crash recurring as recently as 1.3.10. NEEDS-TRIAGE: which screen? §6.Q structural test should have caught it. Confirm absent on 1075. |
| 5 | `RealConnection.connectTls` — CertPathValidatorException "Trust anchor not found" | NON_FATAL | 1 | 1 | 1.3.3→1.3.3 | **pre-existing / old build** — TLS trust-anchor (self-signed local API cert). Not on 1075. |
| 6 | `TopicPageDto.<init>` — MissingFieldException [id,title,author,category,torrentData,commentsPage] | NON_FATAL | 1 | 1 | 1.3.9→1.3.9 | **known-tracked, fix landed** — operator note (2026-06-24): `commentsPage` default added, ships 1.3.11-1073. §6.O log `2026-06-24-topicpagedto-missing-field.md`. Verify on 1075. |
| 7 | `Http2Stream$StreamTimeout` — SocketTimeoutException "timeout" | NON_FATAL | 1 | 1 | **1.3.11**→1.3.11 | **on current build 1075** — single network socket timeout against local API over HTTP/2. Has 1 note. Likely transient network/local-API reachability, NON_FATAL. Monitor; not a code regression on its own (1 event, 1 user). |
| 8 | `generateForegroundServiceDidNotStopInTimeException` — ForegroundServiceDidNotStopInTimeException (dataSync, `lava.api.app.service.ApiEngineService`) | FATAL | 1 | 1 | 0.2.6→0.2.6 | **api-app issue, old build** (0.2.6). FGS-did-not-stop-in-time, sibling of #3. NEEDS-TRIAGE for api-app 0.2.x FGS lifecycle; confirm against 0.2.11/21. |

## §B — Client DEV app (`...54ca2ca3`) — 1 open issue (the headline)

| Issue | Type | Events | Users | Sessions | Ver | Signal |
|---|---|---|---|---|---|---|
| `LifecycleRegistryKt.checkLifecycleStateTransition` — IllegalStateException "State must be at least 'CREATED' to be moved to 'DESTROYED' in component NavBackStackEntry(...) route=`search/search_input?...`" | **FATAL** | **4** | **4** | 4 | **1.3.11** | **SIGNAL_FRESH (first appeared today)** |

**Sample event (real):**
- eventTime `2026-06-25T16:06:24Z`, received `2026-06-25T16:06:31Z`
- package `digital.vasic.lava.client.dev`, version **1.3.11 (1075)**, OS Android 14, Google emulator `sdk_gphone64_x86_64`
- breadcrumbs: `session_start` → `screen_view {MainActivity}` (crash within ~6s of launch, on `MainActivity` destroy)
- blameFrame: `LifecycleRegistry.kt:92` `checkLifecycleStateTransition`
- top exception: `RuntimeException: Unable to destroy activity {...MainActivity}: IllegalStateException: State must be at least 'CREATED' to be moved to 'DESTROYED' in component NavBackStackEntry(...) destination=Destination(0xe36e02dd) route=search/search_input?query={query}&categories={categories}&author_name={author_name}&author_id={author_id}&sort={sort}&order={order}&period={period}`
- crash chain: `ActivityThread.performDestroyActivity` → `NavBackStackEntryImpl.updateState` → `LifecycleRegistry.moveToState` → throw at `LifecycleRegistry.kt:92`. processState BACKGROUND.

**Verdict: KNOWN-TRACKED == LVA-008 nav-teardown crash, CONFIRMED IN THE FIELD ON THE JUST-DISTRIBUTED 1.3.11 (1075).** The route is `search/search_input` — exactly the search/search_input IllegalStateException the LVA-008 item describes. It is FRESH (first seen today, the distribution day), 4 events / 4 users on the dev channel. This is NOT a retired-build artifact — it is a live regression/non-fix on the current build.

## §C — API-app apps — 0 events
Both api-app registrations (`...d57b96` release, `...2932451e` dev) returned genuine empty `topIssues` reports for the 14-day window. Real API zero. See §D for the caveat.

---

## §D — App-ID stream co-mingling caveat (configuration finding, not a crash)
The client-RELEASE registration `...456475e2` is receiving api-app events (`lava.api.app.service.ApiEngineService`, package `digital.vasic.lava.api`, versions 0.2.6) — issues #3 and #8 — while the two dedicated api-app registrations report zero. This means api-app field crashes are landing under the wrong Crashlytics app registration (the api-app's `google-services.json` / app-id wiring appears to point at the client-release app, or shares its mapping). **Impact on this monitoring task:** "api-app = 0 crashes" is misleading; api-app FGS-lifecycle FATALs (0.2.6) DID occur, just under the client registration. This is a wiring issue to flag (not fixed here per scope).

---

## NEW / untracked issues needing §11.4.152 follow-up (systematic-debug + regression test — flagged, NOT fixed here)

1. **LVA-008 nav-teardown crash STILL FATAL on 1.3.11 (1075)** [§B] — highest priority. Known item but **not resolved in the field**; FRESH today on the distributed build, `search/search_input` route, on `MainActivity` destroy. Needs the systematic-debug + falsifiable regression test (Compose nav back-stack lifecycle teardown) that LVA-008 mandates; the current build did not fix it.
2. **api-app FGS-lifecycle FATALs (#3 + #8)** [§A] — `ApiEngineService` `ForegroundServiceStartNotAllowedException` + `ForegroundServiceDidNotStopInTimeException` (dataSync), last seen 0.2.6. Untracked w.r.t. a §6.O log. Needs triage against current 0.2.11 (21): confirm the FGS dataSync time-limit handling is fixed; if not, regression test owed. (Compounded by the §D co-mingling — true api-app field state is obscured.)
3. **§6.Q nested-scroll FATAL recurring to 1.3.10 (#4)** [§A] — `LazyColumn`-in-`verticalScroll` measurement crash seen as recently as 1.3.10 despite the §6.Q structural-test mandate. Needs: identify the offending screen, confirm absent on 1075, and verify the §6.Q structural test actually covers it.
4. **`Http2Stream` socket timeout on 1075 (#7)** [§A] — single NON_FATAL on the current build; monitor for volume growth (could indicate local-API reachability flakiness on the new build). Low priority unless it climbs.
5. **App-ID co-mingling (§D)** — config/wiring follow-up: api-app events are reported under the client-release registration. Not a crash, but it breaks per-app field monitoring honesty.

## Already-tracked, fix-landed (verify-no-recurrence on 1075, do NOT close-mark prematurely)
- #2 CredentialsKeyHolder locked (FATAL) — fix in 1.3.11-1073, §6.O log present, C47 Challenge OWED.
- #6 TopicPageDto MissingField (NON_FATAL) — fix in 1.3.11-1073, §6.O log present.

## No issues modified
Per task constraint: no Crashlytics issue was closed, updated, or note-modified during this monitoring pass. Read-only queries + `list_notes` only.
