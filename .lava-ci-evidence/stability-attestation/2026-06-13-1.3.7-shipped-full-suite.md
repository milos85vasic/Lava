# Stability attestation — 1.3.7-1064 / api-app 0.2.7-11 (shipped) — 2026-06-13

Definitive full-suite verification of the shipped 1.3.7 code (master @ the 1.3.8 §6.Y bump,
whose app/go source == the shipped 1.3.7 artifacts). Evidence-only, zero product change.

- **Kotlin full unit suite:** `./gradlew testDebugUnitTest --max-workers=2` → **BUILD SUCCESSFUL in 4m13s** (all feature + core modules).
- **Go full suite:** `go test ./...` → **48 ok packages, 0 FAIL, 3 no-test-files** (real Postgres e2e self-skips cleanly without `-Pintegration`).

Combined with the §6.Z device gate (both 1.3.6 + 1.3.7: C00 + C26/C25/C20 + R8 release
canaries, 0 failed) and the per-cycle falsifiability rehearsals, the 1.3.7 build is
attested stable: unit-green on both codebases AND cold-start + onboarding-flow gated on a
real Genymotion VM. No open failures. 1.3.7 is the best + most stable build, shipped to
both Firebase channels.
