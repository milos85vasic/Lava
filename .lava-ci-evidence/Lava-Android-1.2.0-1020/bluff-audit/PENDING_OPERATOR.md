# Bluff-Audit (Phase 5 evidence pack)

status: PENDING_OPERATOR

The operator MUST run `./scripts/bluff-hunt.sh` against this commit
before tagging. The script writes its JSON evidence into this
directory; this stub will be replaced by the actual run output.

See `scripts/bluff-hunt.sh` for the protocol.
