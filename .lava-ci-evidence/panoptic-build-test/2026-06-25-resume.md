# Panoptic — Build & Test Evidence (2026-06-25)

Captured by the panoptic build/test stream. REAL captured output only; no bluffs.
Scope: `panoptic/` only. Bounded resources used throughout: `GOMAXPROCS=2 nice -n 19`.
Host: darwin/arm64. podman v5.8.2 available (NOT needed — see below).

## Environment

- `go version go1.26.2 darwin/arm64`
- Module: `panoptic`, declares `go 1.25.0` (go.mod)
- Build system: `build.sh` (`go build -o panoptic main.go`). **No Makefile.**
- **No Postgres / database dependency.** `grep -rinE "postgres|pgx|database/sql"` over `*.go` → 0 hits.
  Panoptic is a CLI UI-automation framework (cobra + go-rod browser automation). No DB container was
  needed or started.

## 1. Build — PASS (exit 0)

```
$ GOMAXPROCS=2 nice -n 19 go build ./...      → exit 0
$ GOMAXPROCS=2 nice -n 19 go build -o panoptic main.go   → exit 0
$ GOMAXPROCS=2 nice -n 19 go build -o <evidence>/panoptic-launcher ./cmd/launcher  → exit 0
```

Binaries produced:
- `panoptic/panoptic` — Mach-O 64-bit arm64, 22,445,106 bytes (main CLI)
- `panoptic/cmd/launcher` → `panoptic-launcher` — Mach-O 64-bit arm64, 2,686,802 bytes
- Log: `.lava-ci-evidence/panoptic-build-test/logs/build.log`

## 2. Default test suite — PASS (exit 0)

`GOMAXPROCS=2 nice -n 19 go test ./... -count=1` → **exit 0**. Authoritative per-package counts
parsed from `-json` (`logs/test-counts.json`; human log `logs/test.log`):

| status | package | pass | fail | skip |
|--------|---------|------|------|------|
| skip | panoptic (root) | 0 | 0 | 0 (no test files) |
| skip | panoptic/challenges/runner | 0 | 0 | 0 (no test files) |
| pass | panoptic/cmd | 27 | 0 | 0 |
| skip | panoptic/cmd/launcher | 0 | 0 | 0 (no test files) |
| pass | panoptic/internal/ai | 86 | 0 | 0 |
| pass | panoptic/internal/cloud | 71 | 0 | 0 |
| pass | panoptic/internal/config | 34 | 0 | 0 |
| pass | panoptic/internal/enterprise | 211 | 0 | 0 |
| pass | panoptic/internal/executor | 175 | 0 | 0 |
| pass | panoptic/internal/launcher | 15 | 0 | 0 |
| pass | panoptic/internal/logger | 19 | 0 | 0 |
| pass | panoptic/internal/ocr | 10 | 0 | 0 |
| pass | panoptic/internal/platforms | 186 | 0 | 0 |
| pass | panoptic/internal/recvalidate | 15 | 0 | 0 |
| pass | panoptic/internal/vision | 49 | 0 | 0 |
| pass | panoptic/pkg/i18n | 3 | 0 | 0 |
| **TOTAL** | | **901** | **0** | **0** |

(`internal/platforms` is the slow package at ~164s; everything else < 5s.)

## 3. go vet — PASS (exit 0)

`GOMAXPROCS=2 nice -n 19 go vet ./...` → **exit 0**, no diagnostics.
Log: `logs/vet.log`.

## 4. Tag-gated suites under `tests/` (OPTIONAL, beyond default `./...`)

These four suites are hidden behind Go build tags (`//go:build integration|functional|e2e|security`)
and are NOT part of the default `go test ./...` run. They were attempted for completeness. They drive
the real CLI binary + a real browser (go-rod auto-provisioned Chromium). Results reported honestly —
several FAIL, none are core-logic/default-suite regressions:

- **integration** (`-tags=integration`): **5 pass / 1 fail**, exit 1.
  Failing: `TestPanoptic_ReportGeneration` — STALE TEST: asserts the generated HTML report contains
  the literal `"Total Tests:"`, but the current report template emits `"Total Apps"` instead. The
  report itself generates correctly; the assertion is out of date. Log: `logs/test-integration.json`.

- **e2e** (`-tags=e2e`): **2 pass / 2 fail**, exit 1.
  Failing: `TestE2E_FullWorkflow`, `TestE2E_PerformanceMetrics`. Causes are (a) the same stale
  `"Total Tests:"` report assertion, and (b) macOS host-environment expectations the test hard-codes —
  `application not found at path: /Applications/Calculator.app`, and a web form selector
  `input[value='medium']` not found. Log: `logs/test-e2e.json`.

- **functional** (`-tags=functional`): **BUILD FAILED** (0 tests run), exit 1. The TEST FILE itself does
  not compile: `tests/functional/panoptic_test.go:10 "io" imported and not used`;
  `:236 assert.Contains(...) || assert.Contains(...) (value of type bool) is not used`;
  `:409 declared and not used: err`. Log: `logs/test-functional.json`.

- **security** (`-tags=security`): **BUILD FAILED** (0 tests run), exit 1. The TEST FILE does not compile:
  `tests/security/panoptic_test.go` references `context` and `bytes` without importing them
  (`undefined: context`, `undefined: bytes`, multiple sites) plus `declared and not used: err`.
  Log: `logs/test-security.json`.

These four optional suites are pre-existing drift (stale assertions, missing test-file imports,
macOS-specific path assumptions). They are recorded honestly as BLOCKED/FAILING; none were faked.
They do not affect the green default suite or the clean build/vet.

## Verdict

- `go build ./...`: **PASS (0)** — 2 binaries produced.
- Default `go test ./...`: **PASS (0)** — 901/901 tests pass across 13 test-bearing packages.
- `go vet ./...`: **PASS (0)**.
- Optional tag-gated suites: integration 5p/1f, e2e 2p/2f (stale report assertion + macOS env),
  functional & security do-not-compile (stale test files). Reported, not faked.

Raw logs: `.lava-ci-evidence/panoptic-build-test/logs/`.
