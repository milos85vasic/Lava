# Curated provider live-reachability audit — 2026-06-13

Operator no-bluff mandate: the shipped providers must ACTUALLY work for users.
YTS already proved a real domain can silently rotate out (yts.mx → NXDOMAIN), so
this audit confirms each curated provider is reachable against its REAL upstream
RIGHT NOW via its `-tags realtrackers` live test (each asserts real 40-hex
info_hashes + magnet links + genuine 2-query filtering — not a fixture).

Command: `GOMAXPROCS=2 go test -tags realtrackers -count=1 ./internal/provider/curated/<p>/...`

| Provider | Upstream | Live test | Disposition |
|----------|----------|-----------|-------------|
| thepiratebay | apibay.org | ✅ ok (2.3s) | WORKS — real magnets, query filters |
| yts | yts.mx→yts.bz/lt/am (mirror failover) | ✅ ok (2.4s) | WORKS — reaches a live mirror past dead yts.mx |
| torrentscsv | torrents-csv.com | ✅ ok (1.3s) | WORKS — `q` genuinely filters |
| bitsearch | bitsearch.eu | ✅ ok (2.6s) | WORKS — `q` genuinely filters |
| knaben | api.knaben.org | ✅ ok (1.7s) | WORKS — minimal-body query filters (no search_type trap) |
| nyaa | nyaa.si (RSS) | ✅ ok (2.1s) | WORKS — RSS `q` filters, infoHash present |
| torrentdownloads | torrentdownloads.pro (RSS, ISO-8859-1) | ✅ ok (1.8s) | WORKS — latin1 decode + `search` filters |

**Result: 7/7 curated providers LIVE-reachable + functional.** No domain rotations
beyond yts.mx (already mitigated by failover). No CF-gating regressions. The
on-device `/providers` catalogue's 12 entries = 5 bundled natives + these 7
working curated providers.

(This audit was dispatched to a subagent which died instantly on a server-side
rate-limit; the main stream ran it directly — same real evidence, no bluff.)
