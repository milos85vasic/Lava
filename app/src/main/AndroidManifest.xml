<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Task 3.1 (2026-06-03): signature-level permission to read the API
         app's key ContentProvider. Both apps share the same keystore, so
         only the genuine Lava client is granted this at install time. -->
    <uses-permission android:name="digital.vasic.lava.permission.READ_API_KEY" />

    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- Task 3.6 (2026-06-03): package visibility for Android 11+ (API 30).
         Without these <queries> entries, getLaunchIntentForPackage returns
         null even when the API app IS installed — silent false-not-installed
         → Play Store redirect loop (the exact defect §4 of the design warns
         about). Both release + debug package ids are declared. -->
    <queries>
        <package android:name="digital.vasic.lava.api" />
        <package android:name="digital.vasic.lava.api.dev" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="market" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".LavaApplication"
        android:banner="@drawable/tv_banner"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupOnly="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="false"
        android:theme="@style/Theme.Lava.Splash"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="TIRAMISU">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleInstance"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter tools:ignore="IntentFilterUniqueDataAttributes">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- For "rutracker.org/forum/viewtopic.php" -->
                <data
                    android:host="rutracker.org"
                    android:path="/forum/viewtopic.php"
                    android:scheme="http" />
                <data
                    android:host="rutracker.org"
                    android:path="/forum/viewtopic.php"
                    android:scheme="https" />

                <!-- For "rutracker.org/forum/viewforum.php" -->
                <data
                    android:host="rutracker.org"
                    android:path="/forum/viewforum.php"
                    android:scheme="http" />
                <data
                    android:host="rutracker.org"
                    android:path="/forum/viewforum.php"
                    android:scheme="https" />

                <!-- For "rutracker.org/forum/tracker.php" -->
                <data
                    android:host="rutracker.org"
                    android:path="/forum/tracker.php"
                    android:scheme="http" />
                <data
                    android:host="rutracker.org"
                    android:path="/forum/tracker.php"
                    android:scheme="https" />
            </intent-filter>
        </activity>

        <activity
            android:name=".TvActivity"
            android:exported="true"
            android:theme="@style/Theme.Lava.Leanback">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>
</manifest>
